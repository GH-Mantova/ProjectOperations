import { MapLocationsTab } from "../admin/MapLocationsTab";

// SLICE 14 (settings-restructure): extracted from the Map locations inline
// tab on AdminSettingsPage into a standalone Administration page.
export function MapLocationsPage() {
  return <MapLocationsTab />;
}
