# SoT Sweep — 2026-08-18 (STATION 05 · sot-keeper)

Run start: **2026-08-18 00:14 AEST** (2026-08-17 14:14 UTC).
Verdict: **DRIFT FOUND — REPORT-ONLY. No doc-reconcile PR staged this run.** Reasons in §6.

`/sot/` has not been touched in **14 days**. Last sot-reconcile prompt processed: **2026-07-28**.

---

## 1. LEAD — automation health

**The watcher is DOWN, deliberately, as of 2 minutes before this run.**

`C:\po-watcher\STOP-WATCHER` exists and contains, verbatim:

```
stopped by 00-supervisor 2026-08-18T00:15+10:00 - crash-loop diagnosis
```

A supervisor chat stopped it on purpose. **Nothing is dequeuing.** 4 armed `*-ready.md` prompts
are starving. This is a live, human-driven incident — not a fault for this station to repair.

### The crash loop is real, and the supervisor script lies about it

From `C:\po-watcher\watcher-launch.log` (42 MB), the final cycle, byte-for-byte:

```
[2026-08-17T14:14:03.474Z] [watcher] poll-every:  60 s
[2026-08-17T14:14:10.5628493+10:00] Watcher exited with code -1
Add-Content : Stream was not readable.
At C:\po-watcher\ProjectOperations\scripts\pr-watcher\supervise-watcher.ps1:86 char:5
[2026-08-18T00:14:10.7657499+10:00] Watcher exited cleanly (exit 0). Treating as a deliberate stop.
[launcher] supervise-watcher exited (code=-1) at 2026-08-18T00:14:10.8065713+10:00
[launcher] restarting supervise-watcher in 10s
```

Lifetime: **~7 seconds**, then relaunch on a 10 s timer.

**DOCTRINE §7 finding.** Two lines apart the same script prints `exited with code -1` and then
`exited cleanly (exit 0)`. `supervise-watcher.ps1` **misreports its own child's exit code**, and its
logging call at line 86 throws `Stream was not readable` — so the failure never reaches
`supervisor.log`. That is why `restart-watcher-if-wedged.ps1` reports HEALTHY against a process that
has died 7 seconds into every cycle: the health check samples a PID that genuinely exists, in a
process that is about to die, and the supervisor's own log has been scrubbed of the evidence.

**A broken measurement of a broken system.** Report-only — the fix is a code PR in
`scripts/pr-watcher/`, outside this station's scope.

### Liveness NOT independently verified — instrument unavailable

`restart-watcher-if-wedged.ps1` is the **only** sanctioned liveness check (DOCTRINE §3, guard 4).
This station runs in a **Linux sandbox with no PowerShell** (`which pwsh powershell` → nothing).
`bring-up-to-speed.ps1` could not be run either.

Everything above is read from **log bytes and sentinel files**, which is evidence of *last
activity*, not of *current liveness*. Per §7: **"I could not measure it" is not "it is down."**
The STOP-WATCHER sentinel is the load-bearing fact — it is a deliberate stop, stated in writing.

### Preflight warning the watcher raised before it died — genuine, still unfixed

```
[WARN] untracked-ready-prompt: 1 ready/HOLD prompt(s) in C:\ProjectOperations2\docs\pr-prompts
are UNTRACKED in git ... can be STASHED AWAY by start-watcher.ps1 (git stash push
--include-untracked) when the tracked tree is dirty. Files: pr-sor-s9-register-to-progress-claim-HOLD.md
```

Independently confirmed by `git status --porcelain`:
`?? docs/pr-prompts/pr-sor-s9-register-to-progress-claim-HOLD.md`. Commit it via a docs-only PR or
it will vanish. `docs/`-class → a development chat, not this station.

---

## 2. ENVIRONMENT DISAGREEMENT (Rule Zero) — NONE

| Check | Local (sandbox, CRLF tree) | Real CI conclusion | Agree? |
|---|---|---|---|
| `build-relationship-map.mjs --check` | **exit 0** — `OK: generator ran cleanly against schema.prisma (282 models, 66 enums, 459 edges)` | `Data model — generator sanity` = **success** on PR #1160 and PR #1151 | ✅ |
| `metadata-catalog.json` parses | **OK** (4 top-level keys) | n/a | ✅ |

Exit code captured directly (`node … ; echo $?`), **not** through a pipe — the first attempt this run
piped into `tail`, which returns `tail`'s status, not the generator's. Re-run clean: `REAL_GEN_CHECK_EXIT=0`.

**`metadata-catalog.json` is VALID this run.** The "unterminated string @ ~offset 407816" that stood
for four consecutive sweeps is gone. It is however **modified-uncommitted** in the working tree
(` M docs/data-model/metadata-catalog.json`) — someone's fix is sitting unpushed. Commit it.

### ⚠️ CI cannot see the drift in §3

The CI job is named *"generator sanity (**schema.prisma parses cleanly**)"*. It proves the generator
**runs**; it does **not** compare `sot/04` against the schema. **`sot/04` can rot indefinitely and CI
stays green** — and has, for 3 weeks. A gate comparing `sot/04`'s header triple to a fresh generator
run would have caught this on 2026-07-29. Recommended as a development-chat item.

---

## 3. HIGH — `sot/04-data-model.md` generated section is 3 weeks stale

| | Models | Enums | FK edges | Domains | schema sha256 | Stamp |
|---|---|---|---|---|---|---|
| `sot/04` header (committed) | **242** | **48** | **383** | 23 | `a70857f1feef` | 2026-07-27 14:20 UTC |
| Fresh generator run (this sweep) | **282** | **66** | **459** | 23 | — | 2026-08-18 |
| `docs/data-model/relationship-map.md` (committed) | **282** | **66** | **459** | 23 | `4ccf49d1faeb` | 2026-08-17 08:38 UTC |
| Independent `grep -c '^model '` / `'^enum '` | **282** | **66** | — | — | — | — |

**Drift: +40 models, +18 enums, +76 FK edges.**

**§7 calibration.** Three independent instruments agree on 282/66 — the generator, the committed
generated artifact, and a raw `grep` that shares no code with either. The generated artifact is
current (regenerated yesterday); only the **re-merge into `sot/04` was never done**. Positive control
holds: `--check` returns exit 0 against the fresh committed artifact, so the check *can* pass — the
`sot/04` mismatch is a real absence, not a broken reader.

Schema.prisma is **byte-identical** between the local tree HEAD and `origin/main`
(`git diff --name-only HEAD origin/main -- apps/api/prisma/schema.prisma` → empty), so this reading
is valid for `main`, not an artifact of the tree being behind.

**This is the one item on the auto-fix allowlist.** Section-scoped re-merge between
`<!-- SOT04-GENERATED:BEGIN -->` (line 18) and `<!-- SOT04-GENERATED:END -->` (line 3024); the
MERGED SOURCES seam at line 3027 onward must stay byte-identical. Not executed — see §6.

Resolving command (for the reconcile PR, once the pipeline is running):

```powershell
node scripts/data-model/build-relationship-map.mjs
# then re-merge ONLY the body between SOT04-GENERATED:BEGIN/END into sot/04-data-model.md
# and refresh the three header lines (Last updated / Generated from sha256 / Models|Enums|FK|Domains)
```

---

## 4. HIGH — `sot/02` §2 "In-PR" is factually false (curated prose → human chat)

`sot/02` **Last updated: 2026-08-04** — 14 days stale.

**§2 claims "🔧 In-PR — open right now (2)": #895 and #894.**
**Neither is open.** `list_pull_requests(state=open)` returns exactly four, none of them:

| PR | Title | Labels | Substantive CI |
|---|---|---|---|
| #1151 | feat(cfx-5): Xero file-based contact import — dry-run preview + confirm | `do-not-merge` | **all green** (API, Web, tendering-e2e, data-model) |
| #1156 | feat(assets): F-7 asset usage readings | `do-not-merge` | — |
| #1158 | feat(sor-s8): AR office review lane — price, approve, send-back | `do-not-merge` | — |
| #1160 | feat(api): finalise handover → create job (B-HW-11) | `do-not-merge` | **all green** |

This is the **exact 2026-07-13 failure the station brief names**: `02` listing a long-merged set as
In-PR while entirely different PRs are open.

**Board-state correction worth recording:** on both PRs checked, the *only* red is
`PR gates — diff checks`, which fails **by design** while the `do-not-merge` label is present
(CP-26, shipped in #1142). #1151's real defect — the TS2352 Prisma delegate casts — was fixed by the
#1155 fix-forward and its API / e2e / web jobs are now green. These PRs are **not broken; they are
held**. Awaiting Marco removing the label, which is the designed human approval.

**§3 also asserts "the queue is currently drained (0 armed)".** Reality: **4 armed** —
`pr-retire-tenderclientnote-s1`, `pr-settings-home-slice0`, `pr-tenant-mt4-sharing-slice0`,
`pr-theme-system-slice0` — plus 52 `*-HOLD`, 20 `failed/`, 15 `needs-marco/`, 3 `blocked/`.

**Curated prose + roadmap status semantics → explicitly on the NEVER-auto-fix list. Human chat.**

---

## 5. HIGH — `sot/03-progress-log.md` has recorded nothing since 2026-07-07

Header: `Last updated: 2026-07-07 09:26 AEST`. Last git touch: **2026-07-24**. Zero `2026-08-*`
date strings anywhere in its 11,198 lines.

Grep count of the per-PR ledger for every recent merge:

```
#1136 0   #1138 0   #1139 0   #1140 0   #1141 0   #1142 0   #1145 0
#1148 0   #1152 0   #1153 0   #1154 0   #1157 0   #1161 0
```

**§7 positive control** (mandatory before believing a negative): the same grep against the same file
returns `#80 → 17` and `#87 → 19`. The instrument finds PR references when they exist. **The zeros
are real absences**, not a broken reader.

`03` is append-only chronological history and the registry calls it the per-PR ledger. It is
**~6 weeks behind**. Appending is a judgement call about what "shipped" means → **human chat**.

---

## 6. Why NO reconcile PR was staged (fix path deliberately aborted)

S7 was **not** the blocker — no reconcile prompt is pending; the last one processed 2026-07-28. The
fix aborted on three independent grounds, any one sufficient:

1. **The pipeline is stopped for an active incident.** `STOP-WATCHER` was written by 00-supervisor at
   00:15, ~2 min before this run. DOCTRINE 5b: *"Arming a prompt IS the decision to run it."* Arming
   into a pipeline a human deliberately halted mid-diagnosis is wrong, and the prompt would sit inert.
2. **A concurrent actor is mid-write in the shared tree.** `C:\ProjectOperations2` is dirty —
   13 `*-ready.md` deleted-but-uncommitted, `?? .queue-sync-ledger.txt`, `?? queue-watch-state.md` —
   and is **8 commits behind `origin/main`** (`2d293b81` vs `f069e9b3`). Concurrency rule 1 is *claim
   before you act*; a tree someone else is mid-operation in cannot be claimed. `git fetch` could not
   even take a ref lock (`unable to unlink .git/refs/remotes/origin/main.lock`).
3. **New untracked files in that tree get stashed.** The watcher's own preflight (§1) states
   `start-watcher.ps1` runs `git stash push --include-untracked` when the tracked tree is dirty. It is
   dirty. A freshly staged prompt could be silently swallowed — the failure mode this sweep is
   supposed to catch, not cause.

**No `/sot/` file was modified this run. No PR, branch, prompt, or merge was created.**
Verified: `git status --porcelain -- docs/data-model/ sot/` after the generator run shows only the
pre-existing ` M docs/data-model/metadata-catalog.json`. The `--check` run did not dirty the tree.

---

## 7. Handover — what a human chat should action, in order

| # | Item | Owner | Class |
|---|---|---|---|
| 1 | Watcher crash loop + `supervise-watcher.ps1` misreporting exit −1 as exit 0 (line 86 `Add-Content` throws) | 00-supervisor / 03-machine-minder | code |
| 2 | Commit `pr-sor-s9-register-to-progress-claim-HOLD.md` — untracked, will be stashed | dev chat | docs |
| 3 | Commit the pending `metadata-catalog.json` fix sitting uncommitted | dev chat | docs |
| 4 | Re-merge `sot/04` generated section (§3) — **allowlisted, deterministic; stage once the watcher is up** | 05-sot-keeper, next run | sot |
| 5 | Rewrite `sot/02` §2 In-PR (#894/#895 → #1151/#1156/#1158/#1160) and §3's false "0 armed" | dev chat | sot, curated |
| 6 | Backfill `sot/03` with ~6 weeks of merged PRs | dev chat | sot, curated |
| 7 | Add a CI gate comparing `sot/04`'s header triple to a fresh generator run — today nothing catches §3 | dev chat | code |
| 8 | Pull `C:\ProjectOperations2` up to `origin/main` (8 behind) once the concurrent actor is finished | dev chat | ops |

---

## 8. Commands run

```bash
git log -1 --format='%H %ci %s'                                  # 2d293b81 2026-08-17 16:42 (#1148)
git log -1 --format='%H %ci %s' origin/main                      # f069e9b3 2026-08-17 21:41 (#1161)
git diff --name-only HEAD origin/main -- apps/api/prisma/schema.prisma   # (empty)
git status --porcelain
node -e "JSON.parse(require('fs').readFileSync('docs/data-model/metadata-catalog.json','utf8'))"  # OK
node scripts/data-model/build-relationship-map.mjs --check       # exit 0; 282 models, 66 enums, 459 edges
grep -c '^model ' apps/api/prisma/schema.prisma                  # 282
grep -c '^enum '  apps/api/prisma/schema.prisma                  # 66
which pwsh powershell                                            # (none — no PowerShell in sandbox)
cat  /mnt/po-watcher/STOP-WATCHER
tail /mnt/po-watcher/watcher-launch.log
```

GitHub reads via the github connector (writes are 403 by design):
`list_pull_requests(state=open)`, `list_commits(main)`, `pull_request_read(get_check_runs, #1160)`,
`pull_request_read(get_check_runs, #1151)`.

File bytes were read with `node` / `grep` in the sandbox — never PowerShell `Get-Content`, which
decodes BOM-less UTF-8 as Windows-1252 and invents corruption (§7 lie #2).
