# SoT Keeper sweep — 2026-08-20

- **Run:** STATION 05 — SoT Keeper (scheduled, non-interactive)
- **Started:** 2026-08-19T14:11:45Z · **Finished:** 2026-08-19T14:17Z (2026-08-20 00:11–00:17 Brisbane)
- **origin/main at run time:** `171b5f80` (`ci: cache Playwright browser binaries in tendering-e2e workflow (#1243)`) [MEASURED — `get_commit(sha=main)`]
- **Local dev tree HEAD:** `5f1ddb4a` — **2 commits behind `origin/main`** [MEASURED — `git rev-list --count HEAD..origin/main` → 2]
- **Verdict:** **REPORT-ONLY.** No reconcile PR staged — **S7 fired** (a prior sot/ reconcile is still pending). Real drift found in `sot/04` and `sot/02`.

---

## LEAD FINDINGS

### F1 — 🔴 QUEUE DIVERGENCE: 10 prompts armed on `main`, **0 armed in the tree the watcher reads**

[MEASURED]

```
git ls-tree --name-only origin/main docs/pr-prompts/ | grep -- '-ready.md$' | wc -l   ->  10
Get-ChildItem 'C:\ProjectOperations2\docs\pr-prompts' -Filter '*-ready.md' -File      ->  ready_count=0
                                                                                          hold_count=51
```

The watcher reads the **dev tree** (`supervise-watcher.ps1:52` defaults
`PR_WATCHER_PROMPT_DIR = C:\ProjectOperations2\docs\pr-prompts`; `start-watcher.ps1:177` actively
REFUSES a path inside the clone). All 10 tracked `-ready.md` are staged as **working-tree deletions**
plus 2 `-HOLD.md` deletions — 12 deletions total, **uncommitted**.

This is the **inverse** of the 2026-08-17 materialise incident: there, main was armed and the dev tree
was empty because the file was never pulled. Here the dev tree has been **swept** and the sweep was
never pushed, so `main` still advertises 10 armed prompts that **cannot run**.

8 of the 10 are legitimately consumed (they match merged PRs #1233 / #1234 / #1236 / #1238 / #1243) —
so the local sweep was *correct*, it just never landed. The two that are **not** consumed:

| Prompt (armed on `main`) | Dev-tree state | Premise |
|---|---|---|
| `pr-sot-02-reconcile-2026-08-19-ready.md` | renamed to `-HOLD.md` (untracked) | **HOLDS — work NOT done** |
| `pr-estimate-density-ratetable-ready.md` | deleted | (known REJECT `MISSING_FIELD`, per prior scanner) |

**Owner: supervisor / machine-minder, not this station.** The resolving action is to commit the
dev-tree sweep to `main` (a `docs/pr-prompts/**`-only PR), preserving the `-HOLD` rename for
`pr-sot-02-reconcile-2026-08-19`.

### F2 — ⚪ Automation health: watcher chain **ALIVE**; scheduled tasks **CANNOT MEASURE**

[MEASURED — PowerShell on the Windows host, 2026-08-19T14:15:36Z]

```
PID=31664 powershell.exe -File C:\po-watcher\watcher-launcher-singlelane.ps1
PID=33556 powershell.exe -File C:\po-watcher\ProjectOperations\scripts\...   (supervise-watcher)
PID=22712 node.exe        C:\po-watcher\ProjectOperations\scripts\pr-watcher\index.mjs
```

Full three-tier chain (launcher → supervisor → node) is up. **Not wedged, not crash-looping.**

Last `watcher-launch.log` entry is `2026-08-19T12:35:02Z` — a GitHub **HTTP 503** on
`gh pr list` ("will retry next tick"), i.e. a transient GitHub-side outage, not a watcher fault.
~1h40m of log silence follows, which is **expected**: the board is empty (0 open PRs) and
armed = 0, so there is nothing to log.

`Get-ScheduledTask` filtered on `pr-shepherd|night-qa|watcher-triage|feature-queue-watch|ProjectOp`
returned **empty**. Per the standing rule, **an empty result is CANNOT-VERIFY, not "disabled"** —
these four tasks are not registered in Windows Task Scheduler under those names. Do not conclude
they are off.

### F3 — 🔴 `sot/04-data-model.md` is **3 models / 9 FK edges** behind `main`

[MEASURED]

| | `sot/04` header | live `schema.prisma` on `origin/main` |
|---|---|---|
| Last updated | 2026-08-18 14:22 UTC | — |
| schema sha256 | `465e02ef3351` | **`42437d43af55`** |
| Models | 284 | **287** |
| Enums | 66 | 66 |
| FK edges | 466 | **475** |
| Domains | 23 | 23 |

Missing models — set-difference of `^model \w+` in schema vs the `#model-…` anchors in sot/04:

```
IN SCHEMA, NOT IN sot/04:  ["ClientShare","ContactShare","WorkerShare"]
IN sot/04, NOT IN SCHEMA:  []
```

**Provenance of the drift:** PR **#1230** `feat(tenancy): MT-4 SLICE 1 — share-list tables
(schema only, additive)`, merged 2026-08-19T05:57:04Z. Backing migration exists and is coherent:
`apps/api/prisma/migrations/20260819160000_mt4_slice1_share_tables/migration.sql`
(creates `client_shares`, `worker_shares`, `contact_shares`; documented rollback; purely additive).
This is the **D48 one-table-per-domain** share model.

Drift is **clean, one-directional, and fully deterministic** — nothing removed, three models added.
It is squarely inside the auto-fix allowlist. It is **not** staged this run only because of S7 (see
"Why no PR was staged").

**Resolving command** (for whoever lands it):

```
node scripts/data-model/build-relationship-map.mjs
# then re-merge the generated body into sot/04 BETWEEN the markers, exclusive:
#   sot/04-data-model.md:18    <!-- SOT04-GENERATED:BEGIN -->
#   sot/04-data-model.md:3568  <!-- SOT04-GENERATED:END -->
# everything from line 3571 (MERGED SOURCES) onward must be byte-identical.
```

**CRLF warning:** `sot/04-data-model.md` is **100 % CRLF** (CR 5073 / LF 5073) while the generator
writes LF. Normalise (`tr -d '\r'`) before any hash comparison, or the re-merge will report a
whole-file diff that is pure line-ending churn.

### F4 — 🔴 GATE BLINDNESS: **no CI job anywhere can detect `sot/04` drift**

[MEASURED — `.github/workflows/ci.yml:118-131`]

```yaml
data-model-drift:
  name: Data model — generator sanity (schema.prisma parses cleanly)
  run: node scripts/data-model/build-relationship-map.mjs --check
```

This is **not** a drift gate despite the job id. Confirmed at the source
(`build-relationship-map.mjs:17-21`):

> "The --check mode does **NOT** compare against a committed output file (the generated JSON/MD are
> gitignored — committing them churned every open PR). It re-runs the parser against schema.prisma
> and fails if the schema references any model/enum the generator cannot resolve."

And the artifacts are gitignored: `.gitignore:126` (`relationship-map.json`), `.gitignore:127`
(`relationship-map.md`).

So `--check` exited **0** locally *and* CI is green — **and both are blind**. This is not an
ENVIRONMENT DISAGREEMENT (local and CI agree); it is worse in one respect: there is **no instrument
at all**. `sot/04` drifted by 3 models with a fully green board. Nothing will ever catch this except
this station.

**Sharpens the standing memory note.** The prior record said the artifacts are gitignored "so CI can
never see sot/04 drift" — true, but incomplete. The job was *also deliberately rescoped* to a parse
sanity check. Restoring drift detection is a **code change**, out of allowlist → hand to a
development chat. A viable design: have the gate compare the *header counts* of the freshly
generated map against the header of `sot/04-data-model.md` (LF-normalised), which needs no committed
artifact and so cannot churn PRs.

### F5 — 🟠 `sot/02-roadmap-and-status.md` is 15 days stale and states a false live board

[MEASURED]

- `sot/02:3` — **"Last updated: 2026-08-04 (AEST)"** — 15 days old.
- `sot/02:61` — **"## 2. 🔧 In-PR — open right now (2)"**, listing **#895** and **#894**.
- Actual board: `list_pull_requests(state=open)` → **`[]` — ZERO open PRs.** Both #894 and #895 are long closed.
- `sot/02:181` still records the superseded **"nullable; null = shared master data"** multi-tenant
  mechanism from 2026-08-04. `grep -c "D48" sot/02` → **0**. D48 (one explicit share table per
  domain) is what actually shipped in #1230 — and the migration header cites D48 by name.
- The `error-envelope humane render **Done**` claim is still present (`grep -c` → 1) and still false.

This is **curated prose** → **REPORT ONLY**, never auto-edited. Two of the three corrections are
already written verbatim in the pending prompt (see below); the third (§2 In-PR table now empty) is
new as of today and is **not** covered by it.

### F6 — 🟠 `sot/03-progress-log.md` last narrative entry is **2026-04-16**

[MEASURED — `grep -n '^### ' sot/03-progress-log.md | tail`] → last heading
`### Update 2026-04-16 — Roadmap Refresh + Execution Queue Direct Actions`.

Roughly four months and several hundred merged PRs behind. Append-only curated prose →
**REPORT ONLY**. Unchanged from prior sweeps; restating so it does not quietly normalise.

---

## Why no reconcile PR was staged (S7)

**S7 — one-and-done.** `docs/pr-prompts/pr-sot-02-reconcile-2026-08-19-ready.md` is **tracked and
armed on `origin/main`** and its premise still **HOLDS**:

```
grep -c "error-envelope humane render \*\*Done\*\*" sot/02-roadmap-and-status.md   ->  1   (premise HOLDS)
grep -c "D48" sot/02-roadmap-and-status.md                                        ->  0   (done_when unmet)
```

The work has **not** run. Its `-ready.md` is deleted in the dev tree and an untracked
`-HOLD.md` sits beside it — verified **byte-identical modulo line endings** (4598 bytes both, content
equal after CRLF normalisation), so it is a deliberate **disarm**, not a corrupted copy.

A sot/ reconcile is therefore pending and unrun. **S7 forbids staging a second one.** Reported, not
fixed.

Secondary reason, recorded for honesty: S5 (scope cap) would also have been strained — the `sot/04`
re-merge and the `sot/02` prose corrections are two different documents, and the law is
**one `/sot/` doc, one chat, one PR**.

---

## Safeguard ledger

| ID | Check | Result |
|---|---|---|
| S1 | Never edit main / never push | ✅ Held — nothing pushed, nothing committed |
| S2 | Determinism (generator run twice, byte-identical modulo stamp) | ✅ **`deterministic: true`** |
| S3 | Section-scoped re-merge | n/a — no re-merge performed |
| S4 | No content loss | n/a — no `sot/` file touched |
| S5 | Scope cap | ✅ Held — see incident below |
| S6 | Post-fix validation | n/a — no fix |
| S7 | One-and-done | 🔴 **FIRED** — reconcile already pending → fixing stopped |

### S5 incident (self-caught, resolved)

Running `build-relationship-map.mjs` for the S2 determinism proof **rewrote the tracked file**
`docs/data-model/metadata-catalog.json` (the generator merges the catalog on every run — it is *not*
read-only). `git status` reported it as `M`.

`git checkout --` failed on a `.git/index.lock`, so the file was byte-verified directly instead:

```
HEAD blob bytes 671288 sha 833c45502bf19818
disk      bytes 671288 sha 833c45502bf19818
IDENTICAL: true
```

**The `M` was stat-only (mtime), not content.** No restore was needed and none was performed.
Recorded because `git status` alone would have made this look like an out-of-scope edit —
§7 again: the instrument lied in the *pessimistic* direction this time.

### Repair performed: stale `.git/index.lock`

A **0-byte** `C:\ProjectOperations2\.git\index.lock` (createdUtc `2026-08-19T14:14:26Z` — created
*during this run*, by this run's own interrupted `git status`) was blocking all git writes in the dev
tree. Removed on the Windows host after confirming length 0; `git status` then returned
`git_exit=0`, `dirty_lines=22`. Left unrepaired it would have wedged the watcher's next git
operation.

---

## Clean / no-drift

- ✅ **Catalog validity** — `docs/data-model/metadata-catalog.json` parses as valid JSON. (Was
  invalid for four consecutive sweeps historically; **clean now**.)
- ✅ **Generator sanity** — `build-relationship-map.mjs --check` exit **0**: 287 models, 66 enums,
  475 edges, all model/enum references resolve.
- ✅ **Model ↔ migration coherence** for the drift set — all three new models have a backing,
  documented, additive migration with a stated rollback.
- ✅ **Watcher chain** — alive, three tiers, no crash loop.
- ✅ **Board** — 0 open PRs; nothing stuck, nothing red, no CP-26 `do-not-merge` holds outstanding.

---

## Handover — what needs a human / another station

| # | Item | Owner | Action |
|---|---|---|---|
| 1 | **Queue divergence** (F1): 10 armed on `main`, 0 in the dev tree | supervisor / machine-minder | Commit the dev-tree sweep as a `docs/pr-prompts/**`-only PR; keep `pr-sot-02-reconcile-2026-08-19` at `-HOLD` |
| 2 | **`pr-sot-02-reconcile-2026-08-19`** is armed on `main`, disarmed on disk, premise HOLDS | Marco / supervisor | Decide: re-arm and let it run, or fold it into a wider sot/02 reconcile that also fixes the now-false §2 In-PR table (F5) |
| 3 | **`sot/04` +3 models** (F3) | next SoT Keeper run, once #2 clears | Deterministic re-merge between the `SOT04-GENERATED` markers; normalise CRLF before hashing |
| 4 | **CI cannot detect `sot/04` drift** (F4) | development chat | Code change — restore a real drift gate (header-count comparison needs no committed artifact) |
| 5 | **`sot/02` 15 days stale, §2 board false, D48 absent** (F5) | development chat (curated prose) | Not auto-fixable |
| 6 | **`sot/03` ~4 months behind** (F6) | development chat (curated prose) | Not auto-fixable |

**No Azure / Entra / SharePoint action was taken or contemplated.**

---

*Every line above is stamped MEASURED, INFERRED or CANNOT MEASURE. Claims are true at
`origin/main` = `171b5f80` as of 2026-08-19T14:17Z. A claim that outlives its SHA is a lead, not a
finding — re-verify before acting.*
