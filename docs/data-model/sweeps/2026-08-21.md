# SoT sweep — Station 05 (sot-keeper)

- **Run at:** `2026-08-20T14:20Z` (Brisbane `2026-08-21 00:20 +10`) — filed under the Brisbane run date.
- **True at SHA:** `origin/main = a561b703` (`fix(waste): include transport delta in rate-variance
  notification body (#1285)`). Dev tree `C:\ProjectOperations2` = `c211ad62`, **1 behind**; that one
  commit touches only `apps/api/.../scope-waste.service.ts` + a spec — **no `sot/`, no
  `schema.prisma`, no `docs/data-model/`** — so every measurement below is valid for `a561b703`.
- **Verdict:** `NO-OP (fix): reconcile already pending — S7.` Audits ran; **nothing staged**.
- Provenance tags per DOCTRINE 7.1: `[MEASURED]` / `[INFERRED]` / `[CANNOT MEASURE]`.

---

## 0. LEAD — automation health

🔴 **`00-supervisor` is DISABLED.** `[MEASURED]` `list_scheduled_tasks` →
`{"taskId":"00-supervisor","enabled":false,"lastRunAt":"2026-08-17T22:06:30.974Z"}`.
That is **~64 hours** with no supervisor cycle. The supervisor is, per `sot/README.md`, *"the ONLY
thing that starts board or machine work."* `sot/README.md` §3 says explicitly that a disabled
station means *nothing is merging or being tested automatically* and that a chat must **say so up
front**. This is that line.

**It is not, however, a stalled board.** `[MEASURED]` `is:pr is:merged merged:>=2026-08-19` →
`total_count: 81`; most recent merge `2026-08-20T09:23:08Z` (#1285). Work continued via interactive
chats. `[INFERRED]` the disable looks deliberate rather than accidental — but only Marco knows, so
**this station did not re-enable it.** Enabling it would start board work, which is precisely the
thing this station is not allowed to start.

| Station | Enabled | Last run `[MEASURED]` |
|---|---|---|
| `00-supervisor` (`5 */2 * * *`) | 🔴 **false** | 2026-08-17T22:06:30Z |
| `04-scanner` (`0 */4 * * *`) | ✅ true | 2026-08-20T14:09:41Z |
| `05-sot-keeper` (`10 0 * * *`) | ✅ true | 2026-08-20T14:10:47Z (this run) |
| `weekly-security-audit` | ✅ true | 2026-08-18T08:18:52Z |
| `03-machine-minder` | ⚪ **not present in the task list at all** | — |

`03-machine-minder` was recorded as created on 2026-08-20 (`34 */4 * * *`, device-bound).
`[MEASURED]` it does not appear in `list_scheduled_tasks`. Either it was never created on this
device or it is registered outside that surface. `[CANNOT MEASURE]` which, from here.

### Watcher daemon — ALIVE, but running stale code

`[MEASURED]` Matched by **command line**, never image name (21 `node.exe` are running; exactly one
is the watcher):

```
WATCHER PID=23584 started=2026-08-20T08:29:30Z
  cmd="C:\Program Files\nodejs\node.exe" --no-deprecation
       C:\po-watcher\ProjectOperations\scripts\pr-watcher\index.mjs
```

Positive control on the same matcher: matched 21/21 for a pattern known present, so the matcher can
produce a positive. Last dequeue `[MEASURED]` `docs/pr-prompts/processed/rev-1290-ready.md.log`,
mtime `2026-08-20 18:09 +10` = **08:09Z**, ~6h ago — consistent with an idle queue, not a wedge.

🟠 **The watcher clone is 4 commits behind `origin/main`.** `[MEASURED]` in
`C:\po-watcher\ProjectOperations`: `HEAD = fa061e02`, `git rev-list --count HEAD..FETCH_HEAD = 4`.
The watcher executes `index.mjs` **from the clone**, so it is running 4-commits-old pipeline code.
It is also dirty — 5 deleted `docs/pr-reviews/pr-*.md`. **Station 03's problem, not mine**; the fix
is fast-forward-with-watcher-stopped, then relaunch. A restart alone adopts nothing.

---

## 1. ENVIRONMENT DISAGREEMENT (Rule Zero) — **none**

| Check | Local (sandbox, CRLF tree) | Real CI on `a561b703` |
|---|---|---|
| relationship-map generator | `[MEASURED]` exit 0 — *"OK: generator ran cleanly against schema.prisma (292 models, 66 enums, 482 edges)"* | `[MEASURED]` `Data model — generator sanity (schema.prisma parses cleanly) :: completed :: success` |

All 11 check-runs on the `main` head are `success`, except `PR gates — diff checks` which is
`skipped` (expected on a push, not a PR). **No local-PASS / CI-FAIL asymmetry this run.**

⚠️ **But note what `--check` actually is.** `[MEASURED]` `build-relationship-map.mjs:18-21` and
`:540-560`: *"The --check mode does NOT compare against a committed output file (the generated
JSON/MD are gitignored)… The gate's job is instead to prove the generator still parses the current
schema."* Confirmed at `.gitignore:126-127`. **So the `data-model-drift` CI job is a parse-sanity
check and CANNOT detect `sot/04` drift.** The station brief's audit step 1 — *"Non-zero = the
committed map is stale"* — describes behaviour this script no longer has. Audit step 3 (header-count
comparison) is the **only** working `sot/04` instrument. Flagging so the brief gets corrected.

---

## 2. `sot/04-data-model.md` — DRIFTED (deterministic, allowlisted)

`[MEASURED]` sot/04 header vs a fresh generator run in an isolated `mktemp -d` tree (the real
`docs/data-model/` was **not** written to):

| | `sot/04` header | Live `schema.prisma` | Δ |
|---|---|---|---|
| Models | 284 | **292** | **+8** |
| Enums | 66 | 66 | 0 |
| FK edges | 466 | **482** | **+16** |
| Domains | 23 | 23 | 0 |
| schema sha256 | `465e02ef3351` | **`49b774e989af`** | changed |
| Last updated | 2026-08-18 14:22 UTC | — | 2 days stale |

`[MEASURED]` the 8 models present in the schema and absent from sot/04's model quick-jump:

```
AllocationWeightConfig, AllocatorDelegate, ClientShare, ContactShare,
EstimatorCapacity, TenderAllocationCandidate, TenderAllocationRejection, WorkerShare
```

Zero models in sot/04 are gone from the schema — this is **pure additive drift**, `[INFERRED]`
landed by #1274 (`EW-1 allocation + capacity schema`) and the multi-tenant share models.

**Resolving command** (for whoever lands it):
`node scripts/data-model/build-relationship-map.mjs`, then re-merge only the body between
`<!-- SOT04-GENERATED:BEGIN -->` and the `MERGED SOURCES` seam.

🚧 **Do NOT commit the regenerated `metadata-catalog.json` with it.** `[MEASURED]` that file is
**tracked** (`git ls-files` lists it; `git check-ignore` says NOT IGNORED), and regenerating shrinks
it **706,712 → 678,599 bytes (−28 KB)**. Safeguard **S4 (no content loss) ABORTS on that.** It has
0 `reviewed: true` entries so nothing hand-curated is at risk, but a 28 KB unexplained shrink must
be understood before it is committed, not ridden along in a doc PR.

---

## 3. `sot/02-roadmap-and-status.md` — DRIFTED (curated → report only)

`[MEASURED]` header: `Last updated: 2026-08-04` — **17 days stale**.

🔴 **§2 "🔧 In-PR — open right now (2)" lists #895 and #894. `[MEASURED]` there are ZERO open PRs
on the repo** (`list_pull_requests state=open` → `[]`). Both listed PRs are long since closed; main
is ~400 PRs past them. This is the exact 2026-07-13 failure the station brief names — a status
section confidently describing a board that no longer exists.

`[MEASURED]` §3 "Staged" still describes the queue as *"currently drained (0 armed)"* as of
2026-08-04. That happens to be **true again today by coincidence** (0 armed), which is worse than
being wrong — a stale line that reads correct will not get fixed.

**Not auto-edited.** Roadmap STATUS semantics are on the station's explicit NEVER-auto-fix list.

---

## 4. `sot/03-progress-log.md` — SEVERELY BEHIND (curated → report only)

`[MEASURED]`:
- `Last updated: 2026-07-07 09:26 AEST`
- **Highest PR number referenced anywhere in the file: `#495`.** Zero references to any `#1xxx` PR,
  in any format (`#1234`, `PR 1234`, `PR-1234` all return 0 hits).
- Latest date stamp in the body: `2026-04-16`. 11,198 lines, 416 sections, 306 distinct PR refs.
- `main` is now past **#1291**.

`sot/README.md` registers `03` as the *"append-only chronological history of what shipped (per-PR
ledger)"*. **That ledger stopped at #495 and has recorded nothing for ~6 weeks and ~790 PR numbers.**
This is the largest single SoT gap on the board. Reconstructing it is a bulk curated-prose job that
needs a human chat and a decision about granularity — it is not deterministic drift and this station
must not invent it.

---

## 5. 🔴 `sot/05` — THE `LL-` NAMESPACE HAS COLLIDED (7 identifiers, 2 meanings each)

`[MEASURED]` `sot/05` defines **LL-01 … LL-58, contiguous, no gaps.** It is the register of record.
`[MEASURED]` by **reading each line**, not counting grep hits (LL-52's own lesson):

| ID | `sot/05` says (2026-07-20) | Station artifacts / chat memory say (2026-08) |
|---|---|---|
| LL-47 | `Get-Board` renders EMPTY labels for every PR | "BREADCRUMB RULE — leave a notice in the shared tree" |
| LL-48 | `Set-PrBody` threw after a write that had SUCCEEDED (#721) | "I verified a proposition that could not fail" |
| LL-49 | A quarantined prompt stays COMMITTED as armed on main | "absent from `origin/main` ≠ orphaned" |
| LL-50 | The watcher's `queuePaused` flag is in-memory, no reset path | "a PR-body 'known limitation' is an unowned accepted regression" |
| LL-51 | Watcher ALIVE but frozen N commits behind main | "`lint-prompt.mjs` is NOT the arming authority" |
| LL-52 | `enable-automerge.ps1` ignores the `do-not-merge` LABEL | "a bare identifier grep is not a search" |
| LL-53 | A 15-file arming sweep armed an irreversible table-drop prompt | "the STANDING AUTHORITY heading is an IMPOSTER in 17/75 prompts" |

**Seven identifiers, fourteen lessons.** The August series was numbered without checking `sot/05`,
which already ran to LL-58. **The next genuinely free number is LL-59.**

This is the same defect class as the `D<n>` decision-namespace collision found on 2026-08-20, one
level up: a shared identifier space with no exclusivity marker and no intake gate. Anyone citing
"LL-51" today is ambiguous between two unrelated lessons, one of which is about a live watcher
failure mode.

**Needs a human decision** (see §8). Renumbering is destructive to every existing citation, so it is
squarely outside this station's allowlist.

Minor, same file: `[MEASURED]` **LL-57 is filed between LL-52 and LL-53** (line 1448, ahead of line
1454). Ordering only, no ambiguity.

---

## 6. Clean results

- **Catalog validity** `[MEASURED]` `docs/data-model/metadata-catalog.json` parses as valid JSON,
  706,712 bytes. Positive control: the same parser correctly rejects `{"a":`. ✅ *(This was invalid
  for four consecutive sweeps in an earlier era. It is fixed and has stayed fixed.)*
- **Model ↔ migration coherence** `[MEASURED]` 292 models → 292 tables; **0 tables lack a
  `CREATE TABLE` in some migration.** 297 tables were created across migration history; the 4
  extras (`user_ai_providers`, `user_ai_preferences`, `subcontractor_contacts`, `leads`) each have a
  matching `DROP TABLE` — legitimately retired. (A 5th, `IF`, is an artifact of my own regex against
  `CREATE TABLE IF NOT EXISTS`, not a finding.) Positive control confirmed the checker reports a
  planted missing table. ✅
- **Board** `[MEASURED]` 0 open PRs · 0 armed `*-ready.md` · 68 `*-HOLD.md` · 7 `needs-marco/` ·
  6 `failed/` · 0 `blocked/` · 10 `no-pr-opened/`. Fully idle, consistent with §0.

---

## 7. `sot/01` module registry — 35 of 81 API modules unregistered (report only)

`[MEASURED]` module directories under `apps/api/src/modules` whose name appears **nowhere** in
`sot/01-charter-and-architecture.md` (positive control on a planted fake name: OK):

```
access-requests, admin-imports, admin-settings, admin-users, agreed-records, ai-settings,
api-keys, bid-prioritisation, branding, cases, client-quotes, client-versions,
comms-approvals, company-profile, correspondence, estimate-export, expenses,
field-definitions, geocoding, global-lists, handover-templates, handovers, job-roles,
list-bindings, map-locations, notification-preferences, pilot-feedback, public-holidays,
schedule-of-rates, subcontractor-rates, surveys, tenants, tender-clarifications,
tender-clients, win-likelihood
```

`[INFERRED]` a substring match is a weak test — a module could be described under a different name.
Treat this as an upper bound on the gap, not a precise count. Audit step 7 is report-only by design.

---

## 8. 🔬 NEW INSTRUMENT LIE — `gh run list --branch main` is 14 days stale

This one nearly went into this report as a headline finding, which is why it is written down.

`[MEASURED]`, reproduced twice:

```
gh run list --branch main --limit 12
  -> newest row: "Push on main" conclusion=failure  createdAt=2026-08-06T15:41:05Z
  -> "CI" and "Tendering Browser Smoke" status=queued, conclusion=""  (since 2026-08-06)
```

Read at face value that says: **main CI has been failing for 14 days and two required checks have
been stuck queued since 2026-08-06.** It is false.

`[MEASURED]` positive control — the **same binary, no `--branch` filter**:

```
gh run list --limit 8
  -> headBranch=main  "Push on main"            success  2026-08-20T09:23:10Z
  -> headBranch=main  "CI"                      success  2026-08-20T09:23:10Z
  -> headBranch=main  "Deploy"                  success  2026-08-20T09:23:10Z
  -> headBranch=main  "Tendering Browser Smoke" success  2026-08-20T09:23:10Z
```

And the authoritative per-commit read on the actual head agrees: 11/11 check-runs on `a561b703`
completed, 10 `success` + 1 expected `skipped`.

**Rule: read CI state per-commit (`gh api .../commits/<sha>/check-runs`), or `gh run list` unfiltered
and filter client-side. Never trust `gh run list --branch <b>`.** `[CANNOT MEASURE]` the root cause
from here — a `gh` cache or a stale workflow-run index are both consistent with the evidence, and I
am not going to guess between them.

Two DOCTRINE §7 guards re-confirmed the hard way in the same session:
- `--jq` from PS 5.1 re-splits on spaces → `accepts 1 arg(s), received 5`. Use `--json` +
  `ConvertFrom-Json`.
- The Desktop Commander inline-command layer **eats `$`** (LL-43). `$ErrorActionPreference='Continue'`
  arrived as the command `Continue=Continue`. Write a `.ps1` and run it with `-File`.

---

## 9. What was staged: NOTHING — safeguard S7

`[MEASURED]` three doc-reconcile prompts from prior runs are staged and **still unlanded**:

| Prompt | Target | Premise re-run now |
|---|---|---|
| `pr-sot-02-reconcile-2026-08-19-HOLD.md` | `sot/02` | ✅ **TRUE** — work not done |
| `pr-sot-01-nav5-reconcile-2026-08-20-HOLD.md` | `sot/01` §9 | not re-run |
| `pr-sot-04-bp0a-job-canonical-reconcile-HOLD.md` | `sot/04` B-P0a section | not re-run |

`[MEASURED]` the `sot/02` premise, run in both directions:
`grep -q 'error-envelope humane render \*\*Done\*\*' sot/02-roadmap-and-status.md` → **exit 0, live**.
Negative control on an absent string → correctly false, so the grep can say "no".
Its second target fragment, `null = shared master data`, is also still present (1 hit).

**S7 is unambiguous:** *"if a reconcile PR prompt from a prior run is still unmerged, do NOT stage
another — report 'reconcile already pending' and stop fixing."* Three are pending. **Stopped.**

`NO-OP (fix): reconcile already pending — 3 unlanded sot-reconcile prompts. Audits complete.`

Note also: none of the three is **armed**, and `[MEASURED]` `.gitignore:75` swallows
`docs/pr-prompts/*-ready.md`, so arming is a `git mv` of the tracked `-HOLD.md`, never the creation
of a `-ready.md`. With `00-supervisor` disabled, nothing will arm them on its own.

---

## 10. Needs a human decision

1. **Re-enable `00-supervisor`?** Disabled ~64h. Only Marco knows whether that was deliberate.
   This station will not start board work.
2. **`LL-` namespace collision (§5).** Renumber the August series to LL-59+, or accept the
   collision and add an exclusivity marker? Every existing citation breaks either way. Same shape as
   the `D<n>` chain — likely wants the same slice treatment.
3. **`sot/03` reconstruction (§4).** ~790 PRs unlogged since #495. Needs a granularity decision
   (per-PR ledger vs. batched program entries) before anyone starts.
4. **Arm the three pending reconcile prompts?** All three targets are still genuinely drifted.
5. **The −28 KB `metadata-catalog.json` shrink (§2).** Understand it before any regen is committed.

## Method note

`docs/data-model/sweeps/` is **untracked** in the dev tree (`?? docs/data-model/sweeps/`), so these
sweep reports have never reached `main` and are visible only on this machine.
