# SoT sweep — 2026-08-25 14:11Z–14:27Z

**True at `b968e4f1`** (origin/main == dev tree). Station 05 — SoT Keeper. NOT BLIND.
Full narrative, dispositions and evidence:
`docs/pr-prompts/00-05-sot-keeper-2026-08-25-1411-sot04-remerged-check-is-not-a-drift-gate.md`.

## Headline

**NOT clean.** `sot/04-data-model.md`'s generated schema map was **7 days and 8 models stale**
(284→292 models, 466→482 FK edges). Re-merged this run via a doc-reconcile PR.

**And the instrument that was supposed to catch it never could.** `build-relationship-map.mjs
--check` is a *parse-sanity* gate, not a drift gate — the generated artifacts are gitignored, so it
has nothing to diff against. Proven by negative control: a garbage line prepended to
`relationship-map.md` still returned `OK`, exit 0. Audit step 1 of the station brief asserted the
opposite and has been corrected in the same PR.

## Audit results

| # | Check | Result |
|---|---|---|
| 1 | Schema parse sanity (`--check`) | OK exit 0 — **but see headline; this proves less than the brief claimed** |
| 2 | `metadata-catalog.json` valid JSON | **VALID** (`keys=4`). The 4-sweep unterminated-string defect is gone |
| 3 | sot/04 header vs fresh generator | 🔴 **DRIFT — 284/66/466 vs 292/66/482** → re-merged |
| 4 | Roadmap drift (sot/02 §2 In-PR) | 🔴 lists #894/#895 as open; **both MERGED 2026-08-04** (21 d stale) |
| 5 | Automation health | 🟢 watcher LIVE pid 29024, wrapper alive, `processed/` newest 12:33:31Z |
| 6 | Model ↔ migration ↔ code coherence | ⚪ **NOT RUN this sweep** — not reported as clean |
| 7 | sot/01 module registry drift | ⚪ **NOT RUN this sweep** — not reported as clean |

## Rule Zero — local vs CI

No **ENVIRONMENT DISAGREEMENT**. CI read **per-commit** (never `gh run list --branch`, §9.4):
`commits/b968e4f1/check-runs` → 12 checks, 11 success + 1 correctly-skipped (`PR gates — diff
checks`, a push build), **0 failure**. `Data model — generator sanity (schema.prisma parses
cleanly)` :: success. Local `--check` :: OK exit 0. Both green, and — this sweep's point — both
green is compatible with sot/04 being 8 models stale.

## The exact resolving commands

```
# the drift this PR fixes (re-runnable, deterministic)
node scripts/data-model/build-relationship-map.mjs
# then re-merge ONLY between the SOT04-GENERATED:BEGIN / :END markers, plus the three
# header stat lines above BEGIN. Everything from :END to EOF must be byte-identical.

# the real drift probe (audit 3) - NOT --check:
#   compare sot/04's "- Models: N | Enums: N | FK edges: N" against the same line in a
#   freshly generated docs/data-model/relationship-map.md
```

## Safeguards satisfied

```
S2 determinism   two consecutive generator runs, byte-identical
                 relationship-map.md   sha=a938ae94a43f31a8  bytes=163617
                 relationship-map.json sha=03681b6836ffb5cd  bytes=2030082
                 metadata-catalog.json sha=c78229ae95e1332d  bytes=678599
S3 section-scope curated tail sha256 6e0db192a89a8f3ce2aa4e77 identical before AND after read-back
S4 no loss       curated lines 1507 -> 1507; preamble lines changed = exactly 3
S5 scope cap     sot/ + docs/ only (CP-24 legal). Code, scripts, workflows untouched
S6 post-fix      --check -> OK exit 0
S7 one-and-done  zero open PRs touch sot/; no reconcile prompt armed
```

`sot/04-data-model.md` read back after writing: `bytes=278791 BOM=false U+FFFD=0
double_encode_sig=0 CRLF=5168 lone_LF=0` — no BOM, no mojibake, no mixed line endings.
`git diff --numstat` → `124 29 sot/04-data-model.md`.

## Handed on, not done here

- **Station 00 — P1:** arm `docs/pr-prompts/pr-sot-04-bp0a-job-canonical-reconcile-HOLD.md`. A filed
  Station-05 action item that was never picked up; sot/04 §B-P0a still says Project is the surviving
  spine while Marco confirmed **Job is canonical** on 2026-08-20. Twelve slices are blocked on it.
  It is judgement work and needs its own Station 05 run.
- **Station 00 — P3:** arm `docs/pr-prompts/pr-sot-02-reconcile-2026-08-19-HOLD.md` for the stale
  In-PR table (audit 4). Curated prose; never auto-fixed.
- **Next Station 05 run:** audits 6 and 7.
