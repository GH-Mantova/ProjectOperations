# SoT sweep — 2026-08-26 14:11Z–14:26Z

True at `origin/main@44b5f3af` / dev tree `main@7ad50697`. Station 05, doc version 1 = bootstrap 1.
Reached the Windows box (`start_process` OK) — **this is a sighted run, not a quiet blind one.**

Full findings and dispositions live in the tracked-path breadcrumb:
`docs/pr-prompts/00-05-sot-keeper-2026-08-26-1411-sot04-remerged-pr1341-roadmap-and-registry-drift.md`

## Headline

**Automation health: HEALTHY.** [MEASURED] Watcher node pid 29024 running, `PO Watcher Keepalive`
Ready, freshest `docs/pr-prompts/processed/` log 15 min old (`rev-1340-ready.md.log`, 13:56Z).
Armed prompts: 0. Nothing is stalled.

**Environment disagreement: NONE.** [MEASURED] Local `build-relationship-map.mjs --check` = OK exit 0;
CI job `Data model — generator sanity (schema.prisma parses cleanly)` on `44b5f3af` = **success**.

**NOT PASS — one deterministic drift, fixed; four report-only findings.**

## Audit results

| # | Check | Result |
|---|---|---|
| 1 | Schema parse sanity | OK — 292 models, 66 enums, 482 edges, exit 0 (parse gate, **not** a drift gate) |
| 2 | `metadata-catalog.json` validity | **VALID**, 678752 bytes — the historic unterminated-string defect is gone |
| 3 | sot/04 drift | 🔴 **DRIFTED** — fixed, see below |
| 4 | Roadmap drift | 🔴 `sot/02` §2 lists #894/#895 as "open right now"; both **MERGED** |
| 5 | Automation health | ✅ healthy — but the four task names in this station doc's step 5 do not exist |
| 6 | Model / migration / code coherence | ✅ 0 models without a backing `CREATE TABLE`; 0 real orphan tables |
| 7 | Module registry | 🟠 31 of 81 `apps/api/src/modules` absent from `sot/01` |

## The drift, and the exact resolving command

`sot/04-data-model.md`'s generated section was stale: `#1321` added a field to `Tender`, so
`- Table: \`tenders\` | Domain: Tendering | Fields: 70` should read `71`. Header stamps were also
stale (`2026-08-25 14:16 UTC` / schema `sha256 49b774e989af`, against a live `b26240cf69d9`).

🔴 **Model/enum/FK/domain counts MATCHED throughout (292 / 66 / 482 / 23).** The count comparison is
too coarse to catch a field-level change. **The schema sha is the probe that works.**

Resolved by: `node scripts/data-model/build-relationship-map.mjs`, then re-merging the artifact body
between the `<!-- SOT04-GENERATED:BEGIN -->` / `<!-- SOT04-GENERATED:END -->` markers and refreshing
the three header stamp lines.

Shipped as **PR #1341** — `docs(sot-04): re-merge generated schema map after #1321 (Tender 70 -> 71
fields)`, branch `docs/sot-04-reconcile-2026-08-26`, built in a disposable worktree off
`origin/main@44b5f3af`. **+3 / -3, one file.**

🔴 **#1341 MERGED at 14:19:04Z as `main@c63c5504` — Station 05 did not merge it and issued no merge
command.** Mechanism is almost certainly the watcher's `tests-docs` auto-merge policy; attribution is
not provable (all actors merge as `GH-Mantova`). **A `sot/` governance doc therefore reached `main`
without Marco reading the rendered diff.** 3 generator-derived lines, so ~nil risk this time — but the
policy question belongs to Station 00 and Marco. Read-back on `origin/main` confirms `Fields: 71` and
schema `sha256 b26240cf69d9`; `Data model — generator sanity` on the merge commit = **success**.

Safeguards S1–S7 all measured and passing — determinism (two identical generator runs), curated tail
sha256 `2d36fe00ab862b49` identical before/after, only 2 stamp lines changed above the BEGIN marker,
line count 5243 -> 5243, `sot/` only, post-fix `--check` exit 0, no prior reconcile pending.

🔧 `metadata-catalog.json` appears to shrink 706871 -> 678752 bytes on regeneration. **`git diff
--numstat` is EMPTY** — it is CRLF->LF, not content loss. It was not staged. Check `--numstat` before
believing that byte count.

## Report-only (handed on, not touched here)

- **`sot/02` §2** asserts two merged PRs are open, snapshot dated 2026-08-04. Curated status
  semantics — never auto-fixed. DISPATCHED to Station 00.
- **This station doc's audit step 5** names four scheduled tasks (`pr-shepherd`, `night-qa`,
  `watcher-triage`, `feature-queue-watch`) that **do not exist on the box**. A future run could read
  their absence as "disabled ⇒ nothing is merging" and lead with a false alarm. DISPATCHED to 00.
- **`docs/data-model/sweeps/2026-08-18/19/20/21.md` are UNTRACKED**; only `2026-08-25.md` is tracked.
  By this station's own report contract those four runs are unreported. No sweep exists at all for
  08-22/23/24. DISPATCHED to 00.
- **31 of 81 API modules** missing from `sot/01` §13. Needs a human pass (each entry carries status
  and known issues). DEFERRED.
- **Watcher clone** on `feat/orphaned-discharge-guard`, dirty=37, plus 4 orphaned worktrees. Not
  blocking — the watcher is processing. DISPATCHED to Station 03 via 00.
