# SoT sweep — 2026-08-27 14:11Z–14:20Z (Station 05)

**True at `origin/main` `01ad020e`; dev tree `main @ cb9fce55`. NOT blind — Desktop Commander reached
the box.** Full findings and dispositions: `docs/pr-prompts/00-05-sot-keeper-2026-08-27-1411-sot03-nine-replacement-chars-sot04-in-sync.md`

## Headline

**Automation is healthy and the data model is in sync — but `sot/03-progress-log.md` carries 9
committed U+FFFD replacement characters, and this station's own brief audits four scheduled tasks
that do not exist.**

No `ENVIRONMENT DISAGREEMENT` this run: local `--check` printed OK and the CI job
`Data model — generator sanity (schema.prisma parses cleanly)` is `success` on the `01ad020e`
check-runs, read per-commit.

## Audit results

| # | Check | Result |
|---|---|---|
| 1 | schema parse sanity (`--check`) | [MEASURED] OK — 292 models, 66 enums, 482 edges. Parse gate only; not a drift gate. |
| 2 | `metadata-catalog.json` validity | [MEASURED] **VALID JSON**, 678,752 bytes. The old unterminated-string defect is gone. |
| 3 | sot/04 drift | [MEASURED] **IN SYNC.** 292/66/482/23 matches; LF-normalised schema sha256 `b26240cf69d9` matches the header exactly. |
| 4 | roadmap drift | [MEASURED] **DRIFT** — sot/02 §2 "open right now" lists #894/#895, both merged 2026-08-04. Live open set is #1353/#1354. |
| 5 | automation health | [MEASURED] watcher pid 28328 alive, last processed 12:33Z, 0 armed. 🔴 `pr-shepherd` / `night-qa` / `watcher-triage` / `feature-queue-watch` **are not Windows tasks at all** — only `PO Watcher Keepalive` is. |
| 6 | model ↔ migration ↔ code | [MEASURED] **0 mismatches** both directions. (One apparent orphan table `IF` was my own regex matching a SQL comment — verified and discarded.) |
| 7 | module registry | [MEASURED] sot/01 §13 names 36 of 81 API modules; **45 unlisted**. Report-only. |
| — | sot/ encoding | [MEASURED] `03` = **9 × U+FFFD**; every other sot file clean; zero double-encode signatures. |

## Instrument notes worth keeping

- **Hash the schema LF-normalised, or you will report false drift.** Raw CRLF bytes hash to
  `d77585daef9d`; the generator hashes the normalised text (`build-relationship-map.mjs:524`) to
  `b26240cf69d9`. Same file, two answers.
- **`CREATE TABLE` regexes match SQL comments.** A migration comment reading *"guarded by CREATE
  TABLE IF NOT EXISTS"* produced a phantom table named `IF`. Read the file before believing an orphan.
- **`git log -- sot/03-progress-log.md` has only 2 commits**, both carrying the same corruption — the
  damage predates the file's current path and is not recoverable from its history.

## Fix run

**None.** The only thing in the auto-fix allowlist — sot/04's generated section — measured
byte-current, so there was nothing to reconcile. S7 also clean: no reconcile branch or PR is pending
(`git ls-remote --heads origin "*sot*reconcile*"` → empty). The generator was deliberately **not**
run in write mode: it rewrites tracked `metadata-catalog.json` in a git index shared with concurrent
chats, and no drift justified that risk.

## For Station 00

1. **Authorise the sot/03 character repair, or decline it** — 9 characters, inferred replacements,
   `sot/03` is on the never-auto-fix list so 05 will not act alone. RULE 1 options are in the breadcrumb.
2. **Fix audit step 5 of `docs/pipeline/stations/05-sot-keeper.md`** — a permanently-false probe.
   Replacement wording is in the breadcrumb; it is outside safeguard S5 for 05 to take itself.
3. **Discharge backlog item `settings-restructure-sot-nav-reconcile`** — its marker
   `docs/audits/settings-restructure-sot-reconcile.md` is already on `main`. It reads as blocked work
   owed to Station 05; it is not.
4. Both this file and the breadcrumb are **untracked**. Sweep them into a board PR or they are
   unreported.
