# SoT sweep — 2026-08-29

**Run:** Station 05 — SoT Keeper, 2026-08-29T14:12Z–15:30Z · **true at** `origin/main` **fb3cc64b**
Full findings and dispositions:
`docs/pr-prompts/00-05-sot-keeper-2026-08-29-1412-devtree-served-stale-instructions.md`

## Automation health — lead item

- **[MEASURED] The board is braked, not stalled.** `.credentials.json` `expiresAt` =
  2026-08-28T16:13:35Z, file mtime 16:13:26Z, unmoved; at 15:11:22Z that is **22 h 58 m expired**
  and nothing is refreshing it. Newest `failed/` (08-29 07:03Z) carries
  `401 OAuth access token has expired`. Newest `processed/` is **2026-08-28T16:13Z** — the same
  minute the token died. Only Marco can re-authenticate. **ESCALATED (already open, re-stamped).**
- **[MEASURED] Watcher alive**, resolved by command line not image name: 1 of 11 `node.exe` matches
  `pr-watcher[\\/]index\.mjs` → pid **26364**, running from the clone
  `C:\po-watcher\ProjectOperations`.
- **[MEASURED] Live schedule** (scheduled-tasks MCP, not `Scheduled\` folders): 00 every 2 h, 03
  daily, 04 every 4 h, 05 daily — all enabled. **`weekly-security-audit` DISABLED**, lastRunAt
  2026-08-18T08:18Z. 02 and 06 have no task, as designed.
- **[MEASURED] Board:** OPEN PRs 0 · armed 0 · `index.lock` absent both trees · git processes 0 ·
  `status-sweep.ps1` verdict **SAFE TO ACT** (14:15:54Z, re-measured before the one mutation).

## Environment disagreement (Rule Zero)

**None this run.** `node scripts/data-model/build-relationship-map.mjs --check` → `OK … 292 models,
66 enums, 482 edges`, exit 0; the matching CI job `Data model — generator sanity (schema.prisma
parses cleanly)` on `fb3cc64b` = **success**. Local and CI agree. Trunk: 12 check-runs success,
`PR gates — diff checks` skipped (pull_request-only, expected on a push).

## Audit results

| # | Check | Result |
|---|---|---|
| 1 | Schema parse sanity | **PASS** — 292 models / 66 enums / 482 edges, exit 0 |
| 2 | `metadata-catalog.json` valid JSON | **PASS** — 678 752 B, 4 top-level keys |
| 3 | sot/04 generated-header drift | **NO DRIFT** — `sot/04:16` reads 292 / 66 / 482 / 23 domains |
| 4 | Roadmap drift | **DRIFT** — `sot/02` §2 lists #894/#895 as open; both merged 2026-08-04, 0 open PRs today |
| 5 | Automation health | see above |
| 6 | Model↔migration↔code coherence | **[CANNOT MEASURE]** — not run this sweep |
| 7 | Module registry vs repo | **[CANNOT MEASURE]** — not run this sweep |

## sot-refs burn-down

- **The CI answer is 23.** Clean extract of `origin/main` (`git archive -o` + `tar -xf`, 2695 files):
  `sot-refs: 23 baselined exemptions remain … total=274 dangling=0 baselined=23`, exit 0. The dev
  tree prints **17** at the same SHA — do not quote it.
- **The floor is 8, not 0.** `git check-ignore -v` on every target: 8 entries reference
  gitignored-by-design artifacts, so the `/sot/` reference is correct and the entry can never be
  retired. Named with `.gitignore` line numbers in the baseline `_readme` by this PR.
- **The other 15 are real debt and none is mechanically fixable** — every target's basename is
  absent from `origin/main` entirely, so each needs a judgement call about a deleted document.
  **Zero entries deleted this run**, deliberately.

## Drift found, with the exact resolving command or edit

1. **Dev tree serves superseded station docs.** `git diff --numstat origin/main -- docs/pipeline/…`
   → `05-sot-keeper.md` 3/5, `STATION-CAPABILITIES.md` 3/10. The copies the bootstrap points at
   still carry the refuted "in the listing ⇒ blind" diagnostic. `station_doc_version` cannot catch
   this. Resolving move: read station docs via `git show origin/main:<path>`. **DISPATCHED to 00.**
2. **Uncommitted SoT vandalism in the shared dev tree** — three truncated code paths in `sot/03` and
   `sot/06`, three new entries appended to the baseline. **ACTIONED**: reverted to `origin/main`,
   `git diff --numstat -- sot docs/qa/sot-refs-baseline.json` now **EMPTY**. Backups at
   `C:\po-sup-fix-scripts\_sotk-backup-2026-08-29\`.
3. **Three documents, three sot-refs counts.** `CLAUDE.md:19` = 26, `ci.yml` comment = 28, measured
   = **23**. Non-functional comments only. Edits: `26 known-dangling` → `23 known-dangling`;
   `the 28 pre-existing dangling references` → `the 23 pre-existing dangling references`.
   **DISPATCHED to 00** (both files outside safeguard S5's scope cap).
4. **`docs/data-model/sweeps/` is a tracked path nothing ever commits** — 1 file on `main`, 7 on
   disk, not gitignored. **ACTIONED**: the six orphans ship in this PR.
5. **`sot/02` §2 roadmap table stale since 2026-08-04.** **DEFERRED** — the section, `CLAUDE.md` and
   the project instructions all already say `bring-up-to-speed.ps1` beats it.

## Not done, and why

No regeneration (audit 3 found zero drift; regenerating shrinks tracked `metadata-catalog.json`).
No `/sot/` content edit. No arm, no merge, no label. `docs/data-model/metadata-catalog.json` left
alone — its ` M` is a pure LF/CRLF stat artifact, `git diff --numstat` against `origin/main` is
empty. Audits 6 and 7 not run and reported as such rather than inferred.
