# SoT sweep — 2026-08-31

- **UTC:** 2026-08-31T14:11:52Z–14:40Z
- **True at SHA:** `origin/main` `6e105076` (dev tree `main @ 6e105076`, index clean)
- **Station:** 05 SoT-Keeper, doc version 1 / bootstrap 1 (agree)
- **Reachability:** SIGHTED — PowerShell on the Windows host on the first call. Not a blind run.
- **Full report:** `docs/pr-prompts/00-05-sot-keeper-2026-08-31-1411-sot04-remerged-and-trap2-outlived-its-cause.md`

## Headline

**Automation health GREEN.** Watcher `PID 32916` (command line matched on
`pr-watcher\index.mjs`, never by image name), auto-restart wrapper alive, newest `processed/`
write 13:45Z. Live schedule from the scheduled-tasks MCP: 00 / 03 / 04 / 05 enabled;
`weekly-security-audit` **disabled** since 2026-08-18; no `02` and no `06` task. [MEASURED]

**No ENVIRONMENT DISAGREEMENT.** Every local pass has a matching green CI conclusion: main trunk
`3 success / 0 not-success`; #1450 and #1443 each `13 pass / 0 fail / 0 pending`. [MEASURED]

## Audit results

| Step | Result |
|---|---|
| 1 — schema parse sanity | `--check` exit 0 — 292 models, 68 enums, 482 edges. (Not a drift gate; step 3 is.) [MEASURED] |
| 2 — catalog validity | `metadata-catalog.json` parses. Regenerated **byte-identical** to HEAD (679192 B) — did not shrink, not staged. [MEASURED] |
| 3 — **sot/04 drift** | **DRIFT** — header `Enums: 66` vs schema `68`, sha `b26240cf69d9` vs `221a543f55ce`, stale since 2026-08-26. **FIXED this run.** [MEASURED] |
| 4 — **roadmap drift** | **DRIFT** — `sot/02` §2 "In-PR — open right now" lists #894/#895, both merged 2026-08-04. Live: #1450/#1443. **ESCALATED** (curated; not auto-fixable). [MEASURED] |
| 5 — automation health | GREEN, see headline. [MEASURED] |
| 6 — model↔migration↔code | **CLEAN** — 292 models, all with `@@map`; 0 whose table is absent from all 232 `migration.sql`. Controls: `users`=present, `zzz_no_such_table`=absent. [MEASURED] |
| 7 — module registry | **DRIFT** — 32 of 81 `apps/api/src/modules` dirs named nowhere in `sot/01`. **DEFERRED** (curated). [MEASURED] |
| sot-refs burn-down | **14 → 13 baselined**, `dangling=0`, `exempt` 9 → 10. Ratchet vs `origin/main`: `OK`, self-test 4/4. [MEASURED] |

## Reconcile PR — what it changes

One doc-reconcile PR, opened from a disposable worktree off `origin/main`. CP-24 clear
(`sot/` + `docs/` only). Nothing armed, nothing merged.

- `sot/04-data-model.md` — generated section re-merged (+10/−8). S2 determinism PASS · S3 curated
  sha256(16) `4833c436f1c142f5` unchanged · S4 curated lines 1581→1581 · S6 `--check` exit 0.
- `sot/README.md` — the false claim that the repo *ships a committed* knowledge graph corrected
  (`graphify-out/` is gitignored at `.gitignore:134` and has **never** been tracked: 0 commits,
  positive control on `sot/README.md` = 2), plus an inline `sot-ref-allow` marker on its
  `GRAPH_REPORT.md` reference.
- `docs/qa/sot-refs-baseline.json` — entry 14 burned down; the two `sot/04` entries re-keyed
  4142→4144 / 4443→4445 after the re-merge shifted them; **TRAP 2 retired** (its cause, a
  grep-based ratchet, was replaced by `check-sot-baseline-ratchet.mjs` in #1407 and no longer exists).
- `docs/pipeline/stations/05-sot-keeper.md` — dropped the stale hard-coded "26 dangling references"
  (real figure was 14) and documented the line-number re-key trap. `lint-station.mjs` ADMIT, all 7 clean.

## The one trap worth carrying forward

`check-sot-refs.mjs` matches baseline entries on `line` **exactly**; `check-sot-baseline-ratchet.mjs`
deliberately **ignores** `line`. So any `sot/` edit above a baselined reference invalidates the
baseline while sailing through the ratchet — measured this run, a +2-line re-merge produced
`dangling=2, exit 1` with no warning. The structural cure (key the baseline on
`(sot_file, missing_path)`) is `scripts/`-side and would break CP-24 here; **dispatched to Station 06.**
