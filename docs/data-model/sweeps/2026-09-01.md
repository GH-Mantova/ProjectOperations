# SoT sweep — 2026-09-01

- **UTC:** 2026-09-01T14:11:54Z–14:45Z
- **True at SHA:** `origin/main` `3f021384` (dev tree `main @ 3f021384`, no tracked modifications)
- **Station:** 05 SoT-Keeper, doc version 1 / bootstrap 1 (agree)
- **Reachability:** SIGHTED — PowerShell on `LAPTOP-E6NHU4E4` on the first call. Not a blind run.
- **Full report:** `docs/pr-prompts/00-05-sot-keeper-2026-09-01-1411-sot04-remerged-and-the-encoding-gate-sees-3-of-7.md`
- **Reconcile PR:** **#1496**

## Headline

**Automation health GREEN.** Watcher node `PID 28400`, matched on the command line
(`pr-watcher[\\/]index\.mjs` via `Get-CimInstance Win32_Process`), **never by image name** —
**exactly one**. Wrapper chain `30600 -> 34332 -> 28400`: **node=1, wrappers=2**, the healthy shape.
`PO Watcher Keepalive` `State=Ready`, `LastTaskResult=0`, last run 14:15:01Z — one minute before it
was measured. Newest `docs/pr-prompts/processed/` write 06:49Z; heartbeat 343 min stale **with
`armed: 0`**, which is idle, not wedged. **The 90-cycle crash loop Station 00 reported at 12:13Z has
not recurred.** [MEASURED]

Live schedule, from the scheduled-tasks MCP (never from the `Scheduled\` folder listing): 00 every
2 h, 03 daily, 04 every 4 h, 05 daily — all enabled; `weekly-security-audit` **disabled** since
2026-08-18; **no 02 task and no 06 task.** [MEASURED]

**No ENVIRONMENT DISAGREEMENT.** The local `build-relationship-map.mjs --check` pass has a matching
green CI conclusion: `Data model — generator sanity (schema.prisma parses cleanly) :: success` on
the full 40-char SHA of `3f021384`. Trunk `4 success / 0 failed`. [MEASURED]

## Audit results

| # | Check | Result |
|---|---|---|
| 1 | Schema parse sanity | **OK** — 293 models, 68 enums, 488 edges, exit 0. CI agrees. |
| 2 | Catalog validity | **OK** — `metadata-catalog.json` parses. |
| 3 | sot/04 drift | **DRIFT → FIXED.** 292→293 models, 482→488 edges. Three schema merges (#1464, #1471, #1478) landed after 05's last re-merge. Re-merged in #1496. |
| 4 | Roadmap drift | **DRIFT, report-only.** `sot/02:61` "In-PR — open right now (2)" names #894/#895, both merged 2026-08-04. Real open board: #1494, #1483, #1477. |
| 5 | Automation health | **GREEN** — see headline. |
| 6 | Model ↔ migration coherence | **CLEAN** — 293 models, 0 without a backing `CREATE TABLE`, 0 orphan tables (235 migrations, 298 created, 38 dropped). |
| 7 | Registry | **DRIFT, report-only.** 21 of 81 `apps/api/src/modules/` directories are not named anywhere in `sot/01`. |
| — | sot-refs burn-down | `dangling=0 exempt=10 baselined=13 excluded=2`, exit 0. Baseline unchanged in count; two entries **re-keyed** because the re-merge moved their lines. |
| — | sot/ encoding | `01`=0 · `02`=0 · **`03`= 9 U+FFFD** · `04`=0 · `05`=0 · `06`=0 · `README`=0. Zero double-encodes anywhere in `sot/`. |

## Carried to other stations

1. **`check-sot-bytes.mjs` reads 3 of the 7 `sot/` files** — a hard-coded array at `:8-12` covering
   `README`, `01` and `05` only. It is green in CI *because* it never opens `sot/03`, the one file
   with damage. `scripts/` change → **DISPATCHED, 04 or 00.**
2. **`sot/03`'s 9 U+FFFD are 6 days old**, and the reason is **CP-24**: the RULE-1 complete fix is a
   `sot/` repair plus a `scripts/` assertion, and no PR may carry both. Two PRs, in order:
   widen the checker first, repair second. **DISPATCHED, 00.**
3. **411 double-encoded `â€` sequences across 28 files** (node reader, 2015-file positive control):
   `schema.prisma` 142 · `BACKLOG.yaml` 81 · `docs/plans/cluster-chaining-plan.md` 46 ·
   the six `.claude/agents/*.md` 110. All outside 05's allowlist. **DISPATCHED, 04 / 00.**

## Instrument note

The first version of this run's step-6 script required **double-quoted** table identifiers in
migration SQL and reported `model ScopeCard` as having no backing migration. It is created,
unquoted, at `20260516120000_scope_card_foundation/migration.sql:8`. The **negative control** is
what exposed it: `git grep -l 'CREATE TABLE "scope_waste_items"'` returned 0 for a table that
certainly exists. Regex widened, controls added to the script, re-run clean. Recorded because
DOCTRINE §7 counts a broken instrument's confident verdict as the more dangerous failure.

Separately, the run's *first* action nearly produced the same class of error: a PowerShell string
comparison of `git show origin/main:<path>` against `Get-Content -Raw` reported all three binding
documents as differing from `origin/main`. `git hash-object` proved them byte-identical. **Compare
blobs with git, never with a PowerShell string equality over decoded text.**
