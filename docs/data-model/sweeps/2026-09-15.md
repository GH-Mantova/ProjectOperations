# SoT sweep — 2026-09-15T22:22Z @ origin/main 64053600

Station 05, sighted run (Desktop Commander present). Covers the **missed 2026-09-15T14:10Z
occurrence** as well as today's — one sweep, stated as one. Full detail, with commands and
controls, is in the breadcrumb:
`docs/pr-prompts/00-05-sot-keeper-2026-09-15-2222-the-heartbeat-alarms-on-a-station-that-did-report-and-schema-prisma-is-an-unswept-encoding-layer.md`

## Headline

**`/sot/` itself is in sync.** The one drift found was sot/02's live PR snapshot, refreshed in this
PR. No `sot/04` re-merge was needed and no baseline entry remained to burn down.

## Audit results

| # | Check | Result |
|---|---|---|
| 1 | Schema parse sanity (`--check`) | **OK**, exit 0 — 296 models / 69 enums / 493 edges. A parse gate, not a drift gate. |
| 2 | `metadata-catalog.json` parses | **Valid JSON.** |
| 3 | sot/04 drift — header counts | **MATCH** (296 / 69 / 493 / 23). |
| 3 | sot/04 drift — **content** comparison | **IDENTICAL** — 166641 chars both sides, strings compared, never lengths. **No re-merge.** |
| 4 | Roadmap drift | **DRIFT — F1.** sot/02 named 5 open PRs; all 5 merged, two within 24 min of the snapshot. Refreshed. |
| 5 | Automation health | watcher RUNNING pid 20948 · restarter alive · armed 0 · trunk green. |
| 6 | Model ↔ migration ↔ code | **296 of 296** models backed by a migration. (A first pass reported 1 missing; that was my regex, not the repo.) |
| 7 | Registry | 28 of 81 API modules unnamed in sot/01. **Report only** — needs a spec judgement. |

## Rule Zero — local PASS vs real CI conclusion

**No environment disagreement.** `check-sot-refs.mjs` passes locally, passes in a clean worktree off
`origin/main`, and its CI job (`Pipeline — watcher + linter tests`) is **success** on `64053600`.
On `#1960`, where that job is red, the job log shows `sot-refs ratchet: OK - 0 -> 0 baselined
entries`; the red is the **hex ratchet** on `apps/web/src`, outside this station's lane.

## sot-refs burn-down — COMPLETE

`entries.length = 0`. `total=275  dangling=0  exempt=20  baselined=0  excluded=2`, exit 0, in the
dev tree **and** in a clean worktree off `origin/main` (TRAP 1's prescribed control), and agreed by
CI's own ratchet step. Nothing left to burn down.

## Findings and dispositions

- **F1** sot/02's In-PR snapshot was six days stale, and two of its five PRs merged 10 and 24
  minutes after it was written — **ACTIONED** (refreshed here) **+ ESCALATED** (should section 2
  hold a PR table at all? Options in the breadcrumb, complete-and-additive first).
- **F2** `Pipeline heartbeat` alarmed `SILENT … newest is station 00 at 06:40Z` while Station 04 had
  reported at 10:10Z into an **untracked** breadcrumb — 12 on disk for 2026-09-15, **8 tracked**.
  Verdict right, evidence wrong — **DISPATCHED → Station 00.**
- **F3** `apps/api/prisma/schema.prisma` has held **86** double-encoded sequences since `7d655aa5`
  (2026-08-12), comment-only, zero `U+FFFD`, invisible to every encoding sweep because the file is
  not in the layer map — **DISPATCHED → Station 00.** `sot/` measured clean (0 in 01/04/06).
- **F4** This station missed its 2026-09-15T14:10Z occurrence while `--freshness` read `ok` (the 2×
  rule) — **DISPATCHED → Station 00** as evidence for escalation #23.

## Changed

`sot/02-roadmap-and-status.md` section 2 snapshot only — 7 insertions / 7 deletions, byte delta
asserted (`179 = 179`), curated prose proved byte-identical either side of the replaced block, CRLF
preserved. **No roadmap STATUS semantics changed.** No generated artifact and no catalog shrink
enters this PR; the regeneration happened only inside the disposable worktree.
