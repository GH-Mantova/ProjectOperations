# SoT sweep — 2026-09-16

- **UTC:** 2026-09-16T14:11:49Z → 14:30Z
- **True at SHA:** `origin/main` `bdc5d05b`; dev tree `main @ bdc5d05b`
- **Station:** 05 SoT-Keeper, sighted run (Desktop Commander reached the host on the first call)
- **Verdict:** `status-sweep.ps1` §7 — `SAFE TO ACT`

## Headline

**PASS with one drift, fixed: `sot/04`'s generated section was stale and has been re-merged.**
No ENVIRONMENT DISAGREEMENT — every local check that passed has a green CI counterpart on
`bdc5d05b`. Automation is healthy, so this report does not lead with it.

## Results

| # | Check | Command | Result |
|---|---|---|---|
| 1 | Schema parse sanity | `node scripts/data-model/build-relationship-map.mjs --check` | ✅ exit 0 — `296 models, 70 enums, 493 edges`. **Not a drift gate** (2026-08-25 correction) |
| 2 | Catalog validity | `node -e "JSON.parse(...)"` on `metadata-catalog.json` | ✅ valid JSON, 695,182 bytes |
| 3 | sot/04 drift — header counts | header vs freshly generated map header | 🔴 **DRIFT** — Enums **69 → 70**; schema sha `a5ba7c95a076` → `a351d01ca823` |
| 3b | sot/04 drift — content comparison | marker-sliced string compare, normalised EOL | 🔴 **DRIFT** — first difference at generated-line 877, `scope_of_works_items Fields: 76 → 77` |
| 4 | Roadmap drift | `sot/02` §2 vs live board | 🔴 **STALE** — named 5 open PRs, all closed; live board is 1 (`#1986`) |
| 5 | Automation health | `Win32_Process` on `pr-watcher[\\/]index\.mjs`; live task list | ✅ watcher pid **30248** up since `2026-09-16T00:35:18Z`; `PO Watcher Keepalive` Ready, `LastTaskResult=0`, last run `2026-09-16T14:15Z` |
| 6 | Model ↔ migration coherence | 296 models vs 301 `CREATE TABLE` targets | ✅ **0** models without a backing migration (POS `users`=true, NEG minted needle=false) |
| 7 | sot-refs / registry | `node scripts/pipeline/check-sot-refs.mjs` | ✅ `total=275 dangling=0 exempt=20 baselined=0 excluded=2`; baseline `entries.length` = **0** |

`build-toc.mjs --check` was deliberately **not** run against `sot/` — no `sot/` file carries
TOC markers, so it reports drift unconditionally.

## Rule Zero — CI conclusions on `origin/main` `bdc5d05b`

`CI`, `Deploy`, `Tendering Browser Smoke`, `CodeQL` — **all `success`**.
`Pipeline heartbeat` — `failure`, event **`schedule`**, excluded from the trunk verdict by the
denylist; the sweep independently reported it as `NOT trunk CI on this commit`. **Trunk is green.**
The heartbeat red is real but is measuring untracked breadcrumbs, not a code regression — see the
breadcrumb's F4.

Open board: **#1986** only — `BLOCKED`, `do-not-merge`, 13 pass / 2 fail, both reds
`FAIL - CP-26 approval-receipt [LABEL_PRESENT]` read from column 3 of the job log. **Parked by
design; Marco's to release.**

## Fixes shipped (one doc-reconcile PR, `sot/` + `docs/` only — CP-24 clean)

1. `sot/04-data-model.md` — generated section re-merged between the
   `SOT04-GENERATED:BEGIN`/`END` markers. **+150 bytes, 14 insertions / 13 deletions.**
   S2 determinism ✅ · S3 curated sha256 `dad62580c6866207` unchanged ✅ · S4 curated lines
   1581 → 1581 ✅ · S6 `--check` re-run exit 0 ✅. Post-fix content comparison: **IDENTICAL**.
2. `sot/02-roadmap-and-status.md` — In-PR snapshot refreshed to the live board.
   **−544 bytes, 4 insertions / 8 deletions**, curated prose untouched.

`docs/data-model/relationship-map.{md,json}` are gitignored and are not in the PR. Tracked
`metadata-catalog.json` was rewritten by the regen and proved **content-identical** to `origin/main`
(`git diff --numstat` EMPTY); it did **not** shrink and is not in the PR.

## Carried out of this run

- **F1 → Station 00:** `scripts/pipeline/vm-git-guard.sh` installs onto `PATH` via `~/.bashrc` /
  `~/.profile`, which a non-interactive sandbox shell does not source — so the guard did not fire
  and this run left a 0-byte `.git/index.lock` (cleared and read back within the run). A `scripts/`
  fix, outside 05's lane, CP-24-blocked from this PR.
- **F4 → Station 00:** 10 of 20 recent breadcrumbs are untracked, including all four of today's
  Station 04 reports. This is what the red heartbeat is measuring.
- **F3 → Marco (already open):** whether `sot/02` §2 should hold a live PR table at all. Option (b)
  "refresh every run" has now failed on two consecutive runs.
- **F5:** the sot-refs burn-down list is empty; nothing to burn down.
