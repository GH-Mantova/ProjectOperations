# SoT sweep — 2026-09-17

**UTC 2026-09-17T14:11Z–14:42Z · `origin/main` a2f5e8a6 · Station 05 (scheduled, sighted)**

Full findings and provenance: `docs/pr-prompts/00-05-sot-keeper-2026-09-17-1411-sot02-named-a-merged-pr-as-open-and-audit-step-7-reads-a-registry-that-does-not-exist.md`
(same PR as this file). Every line below is `[MEASURED]` unless tagged otherwise.

## Headline

**Automation healthy. No ENVIRONMENT DISAGREEMENT. One drift found and fixed; sot/04 is current.**

- Watcher **LIVE**, pid 30248 (resolved by command line on `pr-watcher[\\/]index\.mjs`, never by
  image name). Newest `processed/` log `2026-09-17T13:19:31Z`. Queue depth-1: `ready=0`, `HOLD=27`.
- `main` CI at `a2f5e8a6`: **4 runs, all `success`** (CI · Deploy · Tendering Browser Smoke · Push on
  main), read per-commit with the full 40-char SHA.
- Local checks and CI **agree**. Rule Zero satisfied.

## Audit results

| # | Check | Result |
|---|---|---|
| 1 | Schema parse sanity (`--check`) | **OK**, exit 0 — 296 models, 70 enums, 493 edges. Parse only; not a drift gate. |
| 2 | `metadata-catalog.json` valid JSON | **VALID** — parses, 723,969 B, 4 top-level keys. |
| 3 | sot/04 drift — header counts | **MATCH** — `296 / 70 / 493 / 23` on both sides. |
| 3 | sot/04 drift — **content** comparison | **IDENTICAL=true**, 166,790 chars both sides. Generated section is CURRENT. |
| 4 | Roadmap drift | **DRIFT — FIXED.** §2 named #1986 as open; it merged `2026-09-17T03:43:16Z`. |
| 5 | Automation health | **HEALTHY** (above). |
| 6 | Model ↔ migration ↔ code coherence | **CLEAN** — 0 models without a backing migration; 4 CREATEd tables have no live model and all 4 are explicitly DROPped later. |
| 7 | sot/01 module registry | **CANNOT RUN** — sot/01 has no module-registry section. Reported, not papered over. |

## What changed

`sot/02-roadmap-and-status.md` §2 **live snapshot only** — `(1)` → `(3)`, stamp refreshed to
`2026-09-17T14:25Z / a2f5e8a6`, the merged #1986 row replaced by the three genuinely open PRs
(#2005 · #2002 · #1998), plus a dated note recording the refresh. **No roadmap STATUS semantics
changed.** `git diff --numstat` = `11 4 sot/02-roadmap-and-status.md`; `git diff --stat` lists that
file only. Byte-delta assertion passed exactly; `U+FFFD=0`, no BOM, CRLF preserved.

Post-fix: `check-sot-refs.mjs` → `total=275 dangling=0 exempt=20 baselined=0 excluded=2`, exit 0.

## Two instruments that lied, and the controls that caught them

1. **The catalog "shrank" 28,787 bytes on regeneration** — it did not. That is exactly the file's line
   count: an LF blob against a CRLF working copy (DOCTRINE §9.3). `git diff --numstat origin/main` is
   **EMPTY**, which is the real answer. Compare content, never size, across that boundary.
2. **A model↔migration probe returned 296 missing models and 299 orphan tables** — all false. Its
   positive control (`User`) also failed, which is what exposed it: every model carries `@@map`, so
   PascalCase model names were being compared against snake_case SQL tables. Re-run with `@@map`
   resolved and controls first: **0 missing, 0 real orphans.**

## Standing note

`docs/qa/sot-refs-baseline.json` is at **`entries.length = 0`** — the burn-down is complete. There is
no entry left to retire, and `baselined=0` is the finished state, not a broken probe.
