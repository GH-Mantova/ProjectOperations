# SoT sweep — 2026-09-24T14:23Z

**True at** `origin/main` `11c07025`. Dev tree `main @ 11c07025`. Station 05, scheduled, **sighted**
(Desktop Commander reached the host on the first call after the `ToolSearch` load).
Station doc version 1 == bootstrap version 1. Full run detail, with every command and its output,
is in the breadcrumb
`docs/pr-prompts/00-05-sot-keeper-2026-09-24-1423-sot04-generated-section-was-six-fields-behind-and-the-header-counts-could-not-see-it.md`.

**Automation health: nothing is stalled.** Watcher node `[LIVE] RUNNING pid 42212`, clone
`branch=main dirty=0`, `docs/pr-prompts/processed/` newest `2026-09-24T14:00:39Z`, armed prompts 0.
Four enabled scheduled tasks, read from the scheduled-tasks MCP and never from a document:
`00-supervisor` `5 * * * *`, `03-machine-minder` `0 9 * * *`, `04-scanner` `0 */4 * * *`,
`05-sot-keeper` `10 0 * * *`; `weekly-security-audit` `enabled: false`.

**ENVIRONMENT DISAGREEMENT: none.** Rule Zero applied — every local pass was crossed against the
real CI conclusion. `Data model — generator sanity (schema.prisma parses cleanly)` is `success` on
`main` @ `11c07025fefd94ede50a76b1b5ef6f594ac73ee7` (full 40-char SHA) and `SUCCESS` on all five
open PRs carrying it; `#2168` skips it (no schema in its diff). Local `--check` exit 0. They agree.

**ONE drift, reconciled** — see PR **#2170** (`sot/reconcile-04-datamodel-2026-09-24`), which
changes exactly two files: `sot/04-data-model.md` (3 insertions / 3 deletions) and this run's
breadcrumb.

| file / model | drift | resolving command |
|---|---|---|
| `sot/04-data-model.md` → `ScopeWasteItem` | `Fields: 51` → `57`; `Suggested measures` gains `totalTripKm` | `node scripts/data-model/build-relationship-map.mjs` then re-merge the section between `<!-- SOT04-GENERATED:BEGIN -->` and `<!-- SOT04-GENERATED:END -->` |
| `sot/04-data-model.md` → `OperationsSettings` | `Fields: 12` → `13` | same re-merge |

🔴 **The header counts could not see it.** After regenerating the artifact, `Models: 297 | Enums: 70
| FK edges: 498 | Domains: 23` matched on both sides while the bodies still differed. Only a CONTENT
comparison of the generated section found this. Model-set control: `MODELS_IN_SOT_ONLY=0`,
`MODELS_IN_MAP_ONLY=0`.

**Everything else clean.** Catalog JSON valid (`keys=4`). Audit 6: 0 models without a backing
migration, 0 schema models missing from sot/04, and the 4 apparently-orphaned migration tables are
all provably `DROP TABLE`d by a later migration. **sot-refs burn-down is DISCHARGED** —
`entries.length = 0`, `total=275 dangling=0 exempt=20 baselined=0 excluded=2`, exit 0; nothing added,
nothing deleted, no re-keying needed (the edit moved no line numbers).

**Three findings not actioned here, each with a disposition in the breadcrumb.** `sot/02` §2 says
*"the board is empty"* against 6 open PRs — already open with Marco as
`needs-marco/sot02-in-pr-table-is-on-its-fifth-refresh-and-rots-within-hours-2026-09-23.md`, whose
RULE 1 analysis names daily refreshing as the option that fails the future half, so it was
deliberately **not** refreshed (DEFERRED). An orphaned worktree `C:/po-worktrees/sup-cwd-paths`
holds 2 uncommitted files at age 410 min — Station 03's lane, and `--force` would discard real work
(DISPATCHED). 24 of 81 `apps/api/src/modules/` directories are absent from sot/01's registry —
report-only by contract (DEFERRED).

Nothing was armed and nothing was merged.
