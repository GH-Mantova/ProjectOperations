---
pr: 1614
approved_by: GH-Mantova
approved_at: 2026-09-05T02:00:04Z
---

Marco removed the do-not-merge label at 02:00:04Z, in one pass across #1614,
#1615, #1619 and #1621, and told this lane in chat: "i removed the label from all
prs with do not merge label / run whatever other checks you need on them, keep
driving the board to green and merge". This receipt records that release. The
text was written by the Station 00 cloud lane on his instruction; the approval is
his, and it is independently checkable in the label timeline.

Why it was escalated: opened by a second lane with no entry in `.arming-log.txt`,
touching non-test `apps/web/src/**`, so Station 00's supervisor classified it
RULE 2 and stopped it. That classification was correct by its own rules. It was
opened directly because Marco had handed the board to this lane for the weekend
to drive the estimating, calculation and CRM clusters to merge.

What it does: CRM sidebar count badges and in-sidebar tab rows, six tab counts
across the three CRM shells, and an Inbox triage row that MOUNTS the existing
archive modal and the existing `deleteEntry` rather than rebuilding either. No
API route, service, DTO or schema field changed. Zero colour literals added.

Not verified before approval, and stated in the PR body: no running app and no
seeded database in the authoring lane, so the rendered badge figures, the six
rendered tab counts, and archiving a lead end-to-end are unexercised. Three gaps
needing their own API slice are named in the body rather than worked around --
`GET /tenders` gated on `tenders.view`, the server-side `limit=100` cap in
`listTasks`, and oldest-first sorting being within-page only.
