---
pr: 1615
approved_by: GH-Mantova
approved_at: 2026-09-05T02:00:34Z
---

Marco removed the do-not-merge label at 02:00:34Z, in one pass across #1614,
#1615, #1619 and #1621, and told this lane in chat to run whatever checks were
needed and drive the board to green and merge. This receipt records that release.
The text was written by the Station 00 cloud lane on his instruction; the
approval is his, and it is independently checkable in the label timeline.

Why it was escalated: opened by a second lane with no entry in `.arming-log.txt`,
touching non-test `apps/web/src/**`, so Station 00's supervisor classified it
RULE 2 and stopped it. Correct by its own rules. The watcher's own pre-merge
review on this PR returned VERDICT: MERGE.

What it does: the Comms hub Threads tab becomes a 1fr/400px grid with a to-do
rail, to-dos become tickable through one shared `buildToggleTaskBody`, an overdue
chip counts rows already in state with no new request, and thread rows thicken.
The composer reuses the existing `AnchorPicker` selection and
`buildCreateTaskBody` unchanged. No API route, service, DTO or schema field
changed. Zero colour literals added -- the change removes five hex-bearing lines
and adds none.

Approved knowing two things are NOT built, both named in the PR body with the
select that justifies each: the mock-up's last-message preview and message count
are not rendered, because `listThreads` returns rows through `threadInclude()` =
`createdBy` only. Both need an API read slice. Nothing in this PR fakes them.

Also not verified: no running app, so creating a to-do, ticking one, and the
1fr/400px layout are asserted as pure functions and values, not as pixels.
