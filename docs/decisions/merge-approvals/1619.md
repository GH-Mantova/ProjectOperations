---
pr: 1619
approved_by: GH-Mantova
approved_at: 2026-09-05T02:00:57Z
---

Marco removed the do-not-merge label at 02:00:57Z, in one pass across #1614,
#1615, #1619 and #1621, and told this lane in chat to run whatever checks were
needed and drive the board to green and merge. This receipt records that release.
The text was written by the Station 00 cloud lane on his instruction; the
approval is his, and it is independently checkable in the label timeline.

Why it was escalated: the prompt carries `escalates: true`, and Station 00's
supervisor separately classified it RULE 2 -- opened by a second lane, no entry
in `.arming-log.txt`, five non-test files including
`packages/config/src/charge-step-semantics.ts` and
`apps/api/src/modules/rates/rate-step-evaluator.ts`. Both were the right call.
The watcher's own pre-merge review returned VERDICT: MERGE.

This is the one on this board that changes money, so the escalation earned its
keep. The admin editor's live preview and the server's evaluator were two
implementations of the same arithmetic and disagreed on nine measured inputs.
The worst is silent on BOTH sides: `Elevation is "inverted"` never matched a cell
holding `"Inverted"`, so the core-hole shape with Depth 50, Rate 30, Elevation
"inverted", Holes 2 has been pricing 300 where it should price 600. That is a
rate that has been quietly wrong in production, not merely inconsistent between
two screens.

Three more where the editor printed a confident number the server would have
refused: divide-by-zero previewed 100, multiply-by-text previewed 10, and a zero
round interval rendered "= NaN". All three now return a named issue and a null
total, so an unresolvable step can no longer show a plausible figure beside its
own error line.

Approved knowing this is a real behaviour change, not only a de-duplication:
multiplying by a TEXT column holding "150" previewed 1500 and is now an error.
A TEXT column is not an arithmetic column even when its text parses as a number.

Approved knowing one file is outside the prompt's `scope:` -- `apps/web/vite.config.ts`
gains a four-line subpath alias mirroring the one already there for
`forms-rule-definition`. Without it the web suite cannot resolve the new shared
module at all, and the two cannot land in either order without a red suite in
between. Nothing else in that file changed.

Not verified: no running app, so the editor was not clicked through; the
rendering change is covered by unit tests plus the pre-existing
`runningTotal === null` guard.
