---
pr: 1621
approved_by: GH-Mantova
approved_at: 2026-09-05T02:01:33Z
---

Marco removed the do-not-merge label at 02:01:33Z, in one pass across #1614,
#1615, #1619 and #1621, and told this lane in chat to run whatever checks were
needed and drive the board to green and merge. This receipt records that release.
The text was written by the Station 00 cloud lane on his instruction; the
approval is his, and it is independently checkable in the label timeline.

Note on authorship, because it matters for how much weight this receipt carries:
this PR was NOT written by the lane writing this receipt. It is Station 00's own
fix, opened by the supervisor lane, which labelled it itself on the correct
grounds that it is `scripts/` and therefore Marco's. The description below is
taken from that lane's own F2 finding and its PR body, not from an independent
review of the diff by the approving lane.

What it fixes, in its author's words: `status-sweep.ps1:177` read
`$isLive = ($dirtyCount -gt 0) -or (age < 30)`. Dirtiness pinned LIVE with no
expiry, because the recency test was reachable only for a clean tree. One
untracked file from an aborted run made section 7 emit board-wide
"CAUTION ... prefer to wait and re-run" on every sweep, forever. `C:/po-vg` had
been doing exactly that for 15.2 hours, which is a control that had stopped
controlling anything.

The fix re-aims dirtiness from blocking the board to warning before a prune, and
lets recency decide liveness. Its author verified it with both controls in one
run -- a 16-hour-silent dirty worktree now reads `orphaned` plus a new
"HOLDS UNCOMMITTED WORK ... PRESERVE OR COMMIT BEFORE PRUNING" line, while a
worktree created minutes earlier still reads `LIVE STATION WORKTREE`. The
positive control is the part that matters: without it the change would be
indistinguishable from switching liveness detection off.

Approved on that evidence. The approving lane did not re-run the sweep itself and
has no independent measurement of either control.
