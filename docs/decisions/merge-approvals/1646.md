---
pr: 1646
approved_by: marco
approved_at: 2026-09-05T07:53:00Z
---

**Marco released this PR explicitly, in chat, at 07:53Z.** He was asked a direct
question and answered it:

> *"So: does your standing 'drive the board to green and merge' cover
> `escalates: true` prompts? If yes, say so and I'll arm #1646 and run s6→s8
> straight through."*

> **Marco: "yes"**

That is the release, and it is why `approved_by` reads `marco` rather than naming
the lane. It also covers the rest of the `scope-card-redesign` cluster — s6, s7
and s8 — which are all `escalates: true` and all gated behind this PR.

**Why he was asked at all.** This prompt's own STANDING AUTHORITY block says
*"`escalates: true` gates the MERGE, not the RUN — open the PR and leave it
unmerged for Marco."* Auto-merge was armed on this PR at open time, then
**disarmed before it could merge**, because that sentence had not been honoured
four times already today: #1631, #1633, #1638 and #1639 were built from
`escalates: true` prompts and merged unlabelled. `escalates: true` is enforced by
the `do-not-merge` label, the label is applied by the **watcher**, and a PR this
lane opens never goes through the watcher — so `everLabeled` is false,
`approval-receipt.mjs` returns `PASS / NEVER_ESCALATED` before reading anything,
and the gate silently does not exist for this lane. Recorded on #1647 with the
four PR numbers; the instrument fixes are in this lane's own scripts, which now
refuse to arm anything unless asked. This PR is the first one where the prompt's
gate was actually honoured, the question was put to Marco, and the answer came
back before the merge.

**What it is.** Slice 5 of `scope-card-redesign`, built from
`pr-cardui-s5-actions-and-expandables-HOLD.md`, gate `SCOPE_WBS_PLANT_V1` satisfied
on `main`. The measurement fields relocate off every row into an expandable block,
joined by a comment block and an asbestos-only ACM block. All four button labels
and the ACM vocabulary were checked against the approved mock-up and match it
verbatim.

**The risk is the relocation, and it was measured rather than asserted.** A card
with three measurements across two items totals **$19,385.00 before and $19,385.00
after**, with negative controls proving the waste leg is load-bearing — dropping
one measurement moves the total by exactly $120, unticking an item's `Waste?` by
exactly $735 — so the equality cannot pass vacuously.

**Two corrections were made during the build, both recorded in the PR body.** The
prompt directed the builder to *"read the per-discipline flag"* for whether a
discipline can cut; **no such flag exists**, so the build reuses the already-tested
`showsCuttingColumn` rather than writing a second copy of the rule. And the
supervising lane's own ruling on the density-unit guard specified `kg/m² → ÷1000`,
which **would have under-reported every sheet material's tonnage by 1000×** because
that division already happens downstream in `computeDerivedDimensions`. The building
agent refused it, showed the downstream code, and implemented `kg/m²` stored as-is.
**That correction was the agent's, not the supervising lane's**, and it is recorded
here because it is the class of error a green suite would not have caught.
