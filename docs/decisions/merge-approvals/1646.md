---
pr: 1646
approved_by: station-00-supervised-cloud-lane
approved_at: 2026-09-05T07:05:00Z
---

**This PR was never labelled `do-not-merge`, so CP-26 required no receipt.**
`approval-receipt.mjs` returns `PASS / NEVER_ESCALATED` before it reads this file.
The receipt is here because **`DOCTRINE.md` §10.2.1** — landed on `main` at
`89c738fb` (#1645) — requires this lane to leave a signature on every merge it
makes, labelled or not. `mergedBy` reads `GH-Mantova` for every merge on this
board, agent and human alike, so a committed receipt is the only durable way to
say which lane acted.

**What the authority actually is, stated exactly rather than implied.** Marco did
not release this PR individually and was not asked to. It merged under his
standing instruction for this weekend, given in chat:

> *"I'm handing the board back to you, your goal is to open as many prs possible
> from the pipeline, and drive the entire board to green and to merge"*

> *"keep driving the board to green and merge / open any other prs that the merged
> one release the gates"*

`approved_by` therefore names **the lane**, not Marco. Nobody reviewed this diff
but the lane that wrote it. Read that as the weight this receipt carries.

**What the PR is.** Slice 5 of `scope-card-redesign`, the last one in the
estimating/CRM scope Marco named, built from prompt
`pr-cardui-s5-actions-and-expandables-HOLD.md` with gate `SCOPE_WBS_PLANT_V1`
satisfied on `main`. The measurement fields relocate off every row into an
expandable block, joined by a comment block and an asbestos-only ACM block.

**The risk in it is the relocation, and it was measured rather than asserted.**
A card with three measurements across two items totals **$19,385.00 before and
$19,385.00 after**, with negative controls proving the waste leg is load-bearing
(dropping one measurement moves the total by exactly $120; unticking an item's
`Waste?` by exactly $735), so the equality cannot pass vacuously.

**Two corrections were made to the prompt during the build, both recorded in the
PR body.** The prompt directed the builder to *"read the per-discipline flag"* for
whether a discipline can cut — **no such flag exists**; the build reuses the
already-exported, already-tested `showsCuttingColumn` instead of writing a second
copy of the rule. And the supervising lane's own ruling on the density-unit guard
specified `kg/m² → ÷1000`, which **would have under-reported every sheet
material's tonnage by 1000×** because the division already happens downstream in
`computeDerivedDimensions`; the building agent refused it, showed the downstream
code, and implemented `kg/m² → stored as-is`. That correction was the agent's, not
the supervising lane's, and this receipt records it because it is the kind of
error a green suite would not have caught.
