---
pr: 1699
approved_by: marco
approved_at: 2026-09-06T09:50:08Z
---

**The release.** The `do-not-merge` label was applied to this PR at
2026-09-06T09:13:09Z and removed at 2026-09-06T09:50:08Z. Both events show as
`GH-Mantova`, which is exactly the ambiguity this receipt exists to close: the
watcher applies the label and Marco releases it, and both authenticate as the
same account, so the events API alone cannot tell an approval from an agent
clearing its own gate.

Marco was asked directly whether the release was his, and answered:

> **Marco: "label removed"**

That confirms the actor. It is a two-word answer, so the scope below is recorded
narrowly rather than read generously.

**What this approves.** Merging #1699 as built: `unit: "day"` on the three
unit-less seeded VALUE columns (`plant` / `fuel` / `enclosure`, all per-day
rates, matching `labour`'s existing treatment), the guarded data migration that
sets the same value on a live database, and the `VALUE_COLUMNS_HAVE_UNITS`
canonical assertion. The migration runs against production on merge and was
exercised against a real Postgres first: clean replay of all 242 migrations, a
re-run is a no-op, a whitespace-only unit is repaired, a cuid-id row under the
same name is still updated, and renaming all three targets fails loudly.

**What this does NOT approve, deliberately.** ⚠️ `other-rates` / `Rate` is broken
in production the same way — every column add and edit on the Other rates table
is refused today — and this PR knowingly leaves it. Unlike the three, it is not
a copy-paste omission: its 28 rows price in "per visit", "p/hr", "p/day",
"each", "p/hr/man", "per 6mm bar diameter", so there is no correct single
per-column unit and choosing one is a product decision. Marco was told this in
the same message and has not answered it. **This receipt is not that decision.**
It wants its own slice — either Marco picks a unit for that column, or the
schema is changed to allow a unit-less VALUE column where the unit genuinely
varies per row.

**Why the PR needed a receipt at all.** It was opened by the supervised cloud
lane (DOCTRINE §10.2.1) with auto-merge deliberately unarmed, because the prompt
carries `escalates: true` and writes to production. The label was then applied
and released, which arms CP-26. Without this file the required check reports
`RELEASED_NO_RECEIPT` and the PR stays blocked on a fact that is no longer true.

Separately: the stale-run half of that failure mode is fixed by #1721, which
adds `labeled` and `unlabeled` to `ci.yml`'s trigger types so releasing a label
can actually re-run CI. Before it, the verdict could not refresh at all.
