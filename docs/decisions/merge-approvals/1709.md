---
pr: 1709
approved_by: marco
approved_at: 2026-09-06T20:01:45Z
---

⚠️ **FILED RETROSPECTIVELY on 2026-09-07.** The merge happened first; this record was written the
next morning once Marco confirmed the actor.

**The merge.** `feat(tender-lifecycle): TenderClient.bidStatus - the schema and API foundation`
merged `2026-09-06T20:01:45Z`. `deploy.yml` ran `prisma migrate deploy` against **production** two
seconds later — `completed/success` at `20:01:47Z`. The column is live.

**Marco merged it himself, directly.** Asked on 2026-09-07 whether the release was his, he answered:

> **Marco: "Yes, that was me"**

That is what this file exists to record, because nothing else could. The PR was built from an
`escalates: true` prompt and the cloud lane deliberately left it **unarmed** for exactly this
reason; it then merged with **no `auto_merge_enabled` event** — a direct click — and the timeline
actor reads `GH-Mantova`, which is Marco *and* every agent. Until he answered, the repo could not
distinguish "Marco approved a production migration" from "an agent cleared its own escalation".

**No gate demanded this receipt, and that is the finding.** The PR carried **zero label events, ever**,
so `approval-receipt.mjs` returned `PASS / NEVER_ESCALATED` before looking for a receipt, and
`Approval receipt (CP-26)` was green on a production migration nobody had signed. CP-26 is armed by
**labelling**, not by the diff. The label is applied by the watcher; the watcher never saw this PR.

Station 00 recorded the same class on 2026-09-06 in breadcrumb 2115 —
*"CP-26 is green on an unlabelled production migration and only hand-classification is holding it"* —
and against `#1730` in breadcrumb 1930 (F7). This is the third live instance in one evening and it is
the same root cause as the escalation dated 2026-09-05,
`needs-marco/cp26-passes-vacuously-on-an-unlabelled-destructive-migration-2026-09-05.md`.

**The fix is upstream of the label.** CP-26 must be armed by the **diff** — a `migrations/` path or
an `apps/api` path demanding a receipt regardless of label history — because labelling can never
reach a lane the watcher does not run.
