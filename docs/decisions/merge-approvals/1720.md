---
pr: 1720
approved_by: marco
approved_at: 2026-09-07T00:00:00Z
---

⚠️ **FILED RETROSPECTIVELY on 2026-09-07, not at merge time.** That is the defect this file
also records. See "Why this is late" below.

**The merge.** `docs(pipeline): PREFLIGHT installs the device-bridge git guard, in the gated block` merged `2026-09-06T12:49:23Z`. It was opened AND auto-merge-armed by the
**supervised cloud lane** (DOCTRINE §10.2.1), from a prompt carrying `escalates: false`.

**The authority.** Marco's standing instruction, chat, 2026-09-05, quoted in §10.2.1:

> *"I'm handing the board back to you, your goal is to open as many prs possible from the pipeline,
> and drive the entire board to green and to merge"*

and re-confirmed 2026-09-06: *"keep driving board"*.

This is a **standing** authorisation, not a per-PR approval. Marco did not see this PR before it
merged. That distinction is the point of writing it down: a reader should be able to tell a merge
the lane made under standing authority from one Marco actually looked at, and until today the repo
could not.

**Ruled 2026-09-07.** Asked directly whether the lane may merge `escalates: false` PRs that touch
`apps/api` and `apps/web`, or whether §10.1's tests-docs boundary is the real line, Marco chose:
**the lane merges, but writes a receipt first.** This file is the back-fill of that rule over the
merges that preceded the ruling.

**Why this is late — and it is the lane's own rule it broke.** §10.2.1 has said since 2026-09-05
that this lane *"must leave a `docs/decisions/merge-approvals/<N>.md` receipt naming itself as the
author of every merge it makes."* **The lane wrote that sentence and then made 17 merges, of which
one carried a receipt.** Nothing caught it, because §10.2.1's claimed enforcement is false: it
asserts that `Approval receipt (CP-26)` means *"a merge by this lane that leaves no signature cannot
reach main"*. CP-26 is armed by **labelling**, not by the diff — `approval-receipt.mjs` returns
`PASS / NEVER_ESCALATED` whenever `everLabeled` is false, before it looks for a receipt. A lane PR is
never labelled, because the label is applied by the watcher and the watcher never sees it. So the
gate the exception rests on has never been able to fire for the lane it was written to bound.

Station 00 found the same hole independently on 2026-09-06 (breadcrumb 1930, finding F7) against
`#1730`: *"a green CP-26 on this PR is a statement about a release that never happened, not about
the merge."*
