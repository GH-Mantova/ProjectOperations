---
pr: 1740
approved_by: marco
approved_at: 2026-09-07T02:05:00Z
---

**Marco reviewed this PR and released it himself.** He applied the `do-not-merge` label at
2026-09-06T23:03:01Z and removed it at 2026-09-06T23:53:21Z, and confirmed in chat on 2026-09-07
that the removal was his: *"1740 it was me removing the label"*, followed by *"drive all open prs
(except for 1746) to green and merge"*.

That distinction is the whole reason this file exists. **This is NOT a standing-authority merge.**
Most receipts in this directory record a merge the supervised cloud lane made under Marco's blanket
instruction to drive the board, on a PR he never looked at. This one records the opposite: a human
read an `escalates: true` change and released it. Removing the label is the release; this file is
the artefact of it, and CP-26 `RELEASED_NO_RECEIPT` is what required it to be written down instead
of left as a label event nobody would find later.

**What was approved.** `deps(api): puppeteer 23.11.1 -> 25.9.0, removes extract-zip
(GHSA-jmr9-qjv8-65gv, Dependabot alert #88)`. The advisory has no patched version — `extract-zip@2.0.1`
is the final published release — so the vulnerability is closed by **removal**, not suppression:
puppeteer 25.9.0 resolves `@puppeteer/browsers@3.2.1`, which dropped `extract-zip` for
`yargs` + `modern-tar`. No `pnpm.overrides`, no vendoring. `grep -c "extract-zip" pnpm-lock.yaml`
returns 0.

**Two defects were found and fixed on the branch after the label came off**, both consequences of
puppeteer 25 being ESM-only: the CLI entry point moved (`lib/esm/puppeteer/node/cli.js` ->
`lib/puppeteer/node/cli.js`, repointed in `deploy.yml` and `ensure-puppeteer-chrome.mjs`), and Jest
could not parse the ESM graph, fixed by `test(pdf-rendering): let ts-jest transpile puppeteer@25's
ESM tree` from the station-00-armed `fix-1740-jest-cannot-parse-puppeteer-25-esm` prompt.

**One risk is open and is NOT closed by this merge**, filed at
`docs/pr-prompts/needs-marco/app-service-node-version-is-pinned-nowhere-and-puppeteer-25-needs-2212-2026-09-07.md`:
puppeteer@25.9.0 declares `engines: node >=22.12.0`, and the App Service runtime Node version is
pinned nowhere in this repo — no `linuxFxVersion`, no `WEBSITE_NODE_DEFAULT_VERSION`. On Node 20 or
22.0–22.11, PDF rendering breaks in production. **No agent may check Azure (DOCTRINE §5.1,
absolute), so this can only be verified by Marco.** The PDF smoke test in the PR body was run on
Marco's laptop and cannot rule it out, because the mechanism is Node-version-gated.

The merge itself — arming auto-merge once every check is green — was performed by the supervised
cloud lane on Marco's explicit instruction above.
