---
pr: 1774
approved_by: marco
approved_at: 2026-09-07T11:42:40Z
---

**Marco released this PR himself.** The supervised cloud lane applied `do-not-merge` by hand at
2026-09-07T08:57:35Z, because the prompt carries `escalates: true` and a PR this lane opens never
passes through the watcher, so nothing else would have gated it. Marco removed the label at
**2026-09-07T11:42:40Z**, and confirmed the intent in chat immediately afterwards: he named
**#1767 and #1775** as the two he would receipt himself, and instructed the lane to *"drive the
other prs to merge"*. This is one of those others.

That distinction is the whole reason this file exists. **This is NOT a standing-authority merge.**
Most receipts in this directory record a merge the lane made under a blanket instruction, on a PR
Marco never looked at. This one records the opposite: a human read an `escalates: true` change and
released it. Removing the label is the release; this file is the artefact of it, and CP-26
`RELEASED_NO_RECEIPT` is what required it to be written down rather than left as a label event
nobody would find later.

**What was approved.** `ci(web): ratchet hard-coded colour literals so apps/web/src can only get
cleaner`. It adds a gate and wires it into `ci.yml`, so the things worth having weighed are:

- **It is not a required status check** — ruleset `15532058` requires exactly `CodeQL`, `API`,
  `Web` and `tendering-e2e`. **But it is de-facto blocking**: `scripts/pr-watcher/index.mjs:1920-1928`
  aborts a merge on the first check whose conclusion is `FAILURE`/`CANCELLED`/`TIMED_OUT`, by name,
  required or not. A red here still burns a slice.
- **It exits 0 against pristine `origin/main`** in 0.08 s — `tree has 4353 literals in 263 files
  (baseline allows 4353 in 263)` — so it does not reject the tree it ships into.
- **It migrates nothing.** Zero application files were touched, per the prompt's explicit
  prohibition. It only stops the count rising.

**The finding this PR surfaced is larger than the gate**, and it is not fixed here: **590 of the
4,353 literals (13.6%) are fallbacks in `var(--token, #hex)` where the token is defined nowhere in
any CSS under `apps/web/src`** — so the fallback is what renders, in both themes. `--border` alone
accounts for 174 uses across 50+ files with twelve different hex fallbacks, against a real token
named `--border-default`. Those are live theme defects, not style debt, and burning them down is
the follow-up this gate exists to protect.

The merge itself — arming auto-merge once every check is green — was performed by the supervised
cloud lane on Marco's explicit instruction above.
