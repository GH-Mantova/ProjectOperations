---
pr: 1810
approved_by: marco
approved_at: 2026-09-08T05:15:31Z
---

Approved after review in chat on 2026-09-08. This is a documentation reconcile
and nothing else: SECTION 9 of sot/01 described a sidebar that stopped existing
when NAV-1 landed on 2026-08-14, and this rewrites it to the sidebar that
actually ships.

It was checked against code rather than against the PR body. NAV_GROUPS in
apps/web/src/components/ShellLayout.tsx on origin/main declares the group
labelled "Tendering", a top-level CRM group sitting between Tendering and
Projects, and the comment recording that custom dashboards render inline and
are not part of NAV_GROUPS — which is what the new SECTION 9 says.

Two judgement calls in the PR were put to me before approval and I accepted
both. Directory is removed from group 2 because it has no entry in NAV_GROUPS;
the page and the /directory route still exist and are still reached by
redirects, so the removal is correct as a description of the sidebar, and
whether Directory should regain a sidebar entry is a separate product question
that this reconcile does not decide. And ShellLayout.tsx line 351 sets
to: "operations/assets-equipment" with no leading slash — the only one of the
23 nav targets that does — which is recorded verbatim rather than quietly
corrected, because SECTION 9 must describe what ships. That is a real
navigation defect and it is being carried forward as its own item rather than
fixed inside a doc PR.

No code, schema, migration or configuration change. Receipt written by the
supervised Station 00 cloud lane on Marco's instruction, under his ruling of
2026-09-07 (PR #1736). The approval is his; the authorship of this file is the
lane's. He removed the do-not-merge label himself at 2026-09-08T05:15:31Z.
