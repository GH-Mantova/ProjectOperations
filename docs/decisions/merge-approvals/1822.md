---
pr: 1822
approved_by: marco
approved_at: 2026-09-08T20:12:20Z
---

Released by removing the do-not-merge label at 2026-09-08T20:12:20Z. The watcher's
pre-merge review returned MERGE and its recommendation was exactly this step:
"Marco removes the do-not-merge label to release the merge. The work is correct,
scoped, tested, and CI-green." Every check on the PR is SUCCESS except the CP-26
pair, which is one cause — this receipt's absence.

⚠️ Attribution, stated plainly rather than assumed: the label event reads actor
GH-Mantova, which is how every actor on this board authenticates, so the removal is
not independently attributable from the timeline alone. It is recorded as Marco's on
the strength of his stated intent in chat — he chose the "open them all, park them
all" model for escalating prompts and said he would strip labels himself — and of the
same pass merging #1820 at 20:02:55Z and #1821 at 20:17:43Z. If it was not his hand,
this receipt is wrong and the merge should be reverted.

The load-bearing safety property was verified independently rather than taken from
the review. `ensureCopyFolderPath` in admin-imports.module.ts:158-167 calls
ensureFolder with module "tendering-legacy-copy" and deliberately omits
linkedEntityType and linkedEntityId, with the reason in a comment: a row carrying
them could win the `linkedEntityType: "Tender"` destination lookup and poison it.
admin-imports.module.spec.ts asserts their ABSENCE — `expect(callArg).not
.toHaveProperty("linkedEntityType")` and the same for linkedEntityId — which is the
right shape of assertion, not a presence check that would pass the day someone sets
them to null.

Recursion is capped at MAX_COPY_DEPTH = 10. Skips are counted in
`skippedDepthCapped` and threaded into the execute() summary log line, so truncation
is visible in the run output; the review described it as "silently truncated", which
understates what the code does.

🔴 THE OPERATIONAL CAVEAT, carried here because it is the one thing this PR does not
protect against. The review records: "No code-level 2026-year guard — the '2026
tenders only' rule is enforced at operational level (job configuration), not in
code." This PR enables recursive folder creation against production SharePoint. The
year restriction therefore lives entirely in how the job is invoked. Before the first
production run, confirm the job configuration scopes it to 2026 tenders, and watch the
`skippedDepthCapped` count in the output.

Receipt written by the supervised Station 00 cloud lane on Marco's standing
instruction to drive armable PRs to green and merge, under his ruling of 2026-09-07
(PR #1736). The approval is his; the authorship of this file is the lane's.
