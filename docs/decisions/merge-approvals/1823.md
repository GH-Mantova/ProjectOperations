---
pr: 1823
approved_by: marco
approved_at: 2026-09-09T22:40:06Z
---

Released by removing the do-not-merge label at 2026-09-09T22:40:06Z, read from the
issue timeline (`unlabeled`, `label=do-not-merge`, actor GH-Mantova), not from a
timestamp I chose. Marco then confirmed it in chat in his own words: "1823 label
removed". Attribution here is direct rather than inferred — which is worth saying
plainly, because #1822's receipt had to record the opposite: the label actor reads
GH-Mantova for every actor on this board, so on that PR the removal was attributed to
Marco on the strength of his stated intent rather than the timeline. Here the chat
message and the timeline agree.

⚠️ THE WATCHER'S VERDICT ON THIS PR IS "REJECT AND REDO", NOT "MERGE". That is not a
stale-verdict problem and it is not being waved past. The verdict was correct. It
named a real defect, the defect was fixed, and the verdict has not been re-run against
the fix. Recording it here so nobody later reads a REJECT verdict beside a merged PR
and assumes the gate was bypassed.

What the verdict caught, confirmed independently rather than taken on trust:
`.github/workflows/deploy.yml` runs `pnpm --filter @project-ops/api exec prisma migrate
deploy` and contains NO seed step anywhere. The PR's previous head, e43a5f3b, modified
`apps/api/prisma/seed.ts` (+32 lines) and added no migration. So on deploy the
permission code would have shipped — the registry re-syncs on API startup — while the
role grant would not. Admin keeps visibility through admin-all-permissions and, in the
PR's own words from the seed comment it added, "every estimating manager silently drops
to self-view". CP-23 was right to refuse it.

One correction to the verdict, in the PR's favour, also recorded: it marked build, lint
and both test suites UNVERIFIED ("CI cancelled"). On the head it was judging, `API —
lint, test, compliance smoke`, `Web — lint, vitest, build` and `tendering-e2e` were all
SUCCESS. The verdict's substance stood; its evidence table did not. That is the second
instance of a verdict outliving the commit it describes, and it is filed as its own
escalation in PR #1825 — verdicts carry no head SHA, so nothing in the pipeline can
tell substance from expired evidence.

THE FIX MARCO CHOSE was option (d), which the verdict did not offer. The verdict gave
(a) add a migration, (b) drop the code, (c) ship code-only and accept the regression.
The fourth option was to stop depending on an undeliverable grant at all:

    export const TEAM_VISIBILITY_CODES = ["reporting.team", "tenders.allocate"] as const;

`tenders.allocate` already exists in production and is already held by the
manager-shaped roles this PR means — "Allocate tenders to estimators; view and manage
the estimator capacity board". Keying team visibility off a list that includes it makes
the first deploy correct with no data migration at all. `seed.ts` is now byte-identical
to origin/main and nothing under `prisma/` is touched, so CP-23 reports PASS with no
trigger rather than a suppressed one.

Additive by construction: holding either code widens visibility, holding neither is
identical to today.

🔴 THE OPERATIONAL CAVEAT, carried here because it is the one thing this receipt cannot
verify. Keying off `tenders.allocate` means every role holding that code now sees the
team-wide rollup in the estimating and win-rate reports, including individual
colleagues' numbers. That is the intended effect and it is what "nobody loses access on
deploy" requires. But [CANNOT MEASURE] from here — I did not query production, and
this lane must not — WHICH roles actually hold `tenders.allocate`, or who is in them.
If that set is wider than "estimating managers", this change shows more people more
numbers than intended. Worth one look at the role list in Admin before anyone relies on
the reports, and it is cheap to narrow afterwards: remove one entry from
TEAM_VISIBILITY_CODES and land the follow-up migration instead.

Verified before the merge, with controls rather than assertions:
  - `node scripts/pr-gates/pr-gates.mjs` with PR_NUMBER=1823, run locally against
    5fa45ce8: PASS - CP-23 seed-without-migration [no seed files changed]. Every other
    gate PASS or SKIP; the only FAIL was CP-26, which is this receipt's absence.
  - `jest --testPathPattern report-self-filter`: 8/8, including a new
    [delivery-mechanism guard] test that fails if anyone narrows the helper back to
    `reporting.team` alone without first landing a real migration.
  - `eslint` on all four changed API files: exit 0.
  - POS/NEG controls on the seed check: the seed diff was empty WHILE the resolver diff
    was non-empty, so the diff instrument was live; `grep reporting.team` under
    `prisma/` returned 0 WHILE a control string returned 12 hits, so the grep was
    reading the file.
  - A TS2353 error in the first draft of the new test (an `email` key on a `Pick<>`
    literal) was caught by that local jest run and fixed before the push. It never
    reached CI.

Also corrected in the fix, because the fix made it false: the comment beside
`reporting.team` in permission-registry.ts claimed the seed grants the code to
allocate-holding roles. No role is granted it by this PR at all, and the comment now
says so and says why.

FOLLOW-UP NOT IN THIS PR: a separate idempotent migration granting `reporting.team` to
every role holding `tenders.allocate`. When it lands, nothing here needs to change and
no user's visibility moves — that is the point of keying off a list. The originating
prompt has been amended and the class of defect filed, both in PR #1824.

Receipt written by the supervised Station 00 cloud lane. The approval is Marco's,
stated in chat and matching the timeline; the authorship of this file is the lane's.
