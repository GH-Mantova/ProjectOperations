---
pr: 1832
approved_by: marco
approved_at: 2026-09-11T00:23:00Z
---

Released by Marco in chat on 2026-09-11 at about 00:23Z, in a Cowork session he was directing
(DOCTRINE section 10.2.1, supervised interactive lane). His words, selecting this PR together
with #1845, #1850, #1852, #1823 and #1865: release them "as long as you can complete any and
all the checks/tests that the PR may have on its body". The receipt is written by the lane;
the approval is his.

The PR's test plan names the device-bridge Linux VM. That transport is DOWN on this box today
(the harness reports a 2026-09-08 Windows update broke the Cowork mount; `device_bash` returns
`no Plan9 drive shares mounted`, exit 1, before any command runs) - so [CANNOT MEASURE] on the
VM itself. The three items were therefore run on a Linux host with the same shape - bash 5.2,
`/usr/bin/git` 2.43 - against a fixture HOME carrying `mnt/ProjectOperations2/`, truth known by
construction, with the script at this PR's head (30aa5c62) and origin/main's script as the
control:

  CONTROL  origin/main script, cwd = the mount  -> exit 1 "FAIL: guard blocked a call that
           targets nothing mounted"  (the defect reproduces)
  T1       PR script, cwd = $HOME                -> exit 0, 3 lines, ONE copy of
           "vm-git-guard installed" (no recursion)
  T2       PR script, cwd = the mount            -> exit 0 (was 1), 3 lines, ONE copy
  T3       from $HOME/mnt: `command git --version` after install -> REFUSED, exit 99
  NEG      from /tmp with the shim on PATH: `git --version` -> exit 0, "git version 2.43.0"
  IDEMP    second install from $HOME -> exit 0; ~/.bashrc export line count 1 before and after

Lane classification: watcher verdict `{"ok":false,"marco":true,"reason":"outside tests/ or
docs/: scripts/pipeline/vm-git-guard.sh"}` in processed/pr-vmgitguard-selftest-and-recursion-
ready.md.log - RULE 2 bound until this release. Merge path: Assert-SmokedOrEscalate then
Merge-Pr, never by hand.

Receipt written by the supervised Station 00 interactive lane (Cowork, Desktop Commander
transport). The approval is Marco's, stated in chat; the authorship of this file is the lane's.
