---
pr: 1852
approved_by: marco
approved_at: 2026-09-11T00:23:00Z
---

Released by Marco in chat on 2026-09-11 at about 00:23Z, in a Cowork session he was directing
(DOCTRINE section 10.2.1, supervised interactive lane), together with #1832, #1845, #1850,
#1823 and #1865, on the condition that every check on the PR body is completed first.

Verification, run on the box with this PR's `status-sweep.ps1` (head cae63e90) executed
in-process under a `gh` shim that answers only `gh run list --commit` with fixture JSON and
forwards every other call to the real gh.exe. The six fixture rows from the PR body were
replayed through the PR's actual code, and origin/main's script was run on fixture 1 as the
control that must print the defect:

  F1 four green trunk runs + six Dependabot failures  -> expect (trunk green), excluded 6/6
  F2 CI (push) failed                                  -> expect TRUNK IS RED
  F3 CodeQL (event dynamic) failed                     -> expect TRUNK IS RED  (the allowlist trap)
  F4 only a scheduled heartbeat failed                 -> expect (trunk green)
  F5 only non-trunk runs                               -> expect [CANNOT MEASURE]
  F6 one push check still running                      -> expect not yet green
  CONTROL origin/main script on F1                     -> expect TRUNK IS RED

Exact readings are quoted in the lane's breadcrumb for this run. The live board's own
instance was also measured: on e6e11370 the sweep printed "4 success / 2 failed  <-- TRUNK IS
RED" while `gh run list --commit <full sha>` showed the only failures were two
`Dependabot Updates` runs and Deploy, CodeQL, CI and Tendering Browser Smoke all success.

Lane classification: NO watcher log for this PR, no arm in its window; second lane,
hand-classified Marco's under DOCTRINE 10.1 step 2 (`scripts/pipeline/status-sweep.ps1` is
outside every NESTED_TEST_PATHS form). Merge path: Assert-SmokedOrEscalate then Merge-Pr.

Receipt written by the supervised Station 00 interactive lane. The approval is Marco's, stated
in chat; the authorship of this file is the lane's.
