---
pr: 1874
approved_by: marco
approved_at: 2026-09-13T10:58:00Z
---

Released under Marco's authorization given in chat on 2026-09-13 at about 10:05Z, in the Cowork session he was
directing (DOCTRINE section 10.2.1, supervised interactive lane): "you have my permission to review all open prs,
identify if they are safe to merge or not, drive them to green and merge / you may remove the label yourself and
enter the receipt where required / make sure to follow the guidelines (name yourself and reasons why label was
removed)". He had said just before that he would not remove a label himself without a verdict saying it is safe.

What the PR is: feat(auth): SLICE 17 slice 1  company.manage permission + auto-grant migration (SLICE 17 slice 1: registers company.manage (isHighRisk), grants it by idempotent additive migration to every role holding platform.admin, swaps the guard on admin/company/* and admin/branding/* endpoints, six permission-matrix rows).

Why it is safe to merge (the lane's review, measured on the current head 
4a7021b3
):
- Watcher review VERDICT: REJECT-AND-REDO blamed the migration for missing the Admin role. Measured wrong by the lane on 09-11: seed-reference.ts grants Admin every permissions row (platform.admin included) so the INNER JOIN matches it; five of the six new matrix rows PASSED for admin on that very run; the single red row was PATCH /admin/company/profile whose handler calls assertSuperUser after the permission guard (the matrix admin is not a super-user). The fix lane landed 957a778d: that one spec row no longer asserts admin passes; the migration, registry and controllers are untouched.`n- Migration read by the lane: INSERT permission ON CONFLICT DO NOTHING + INSERT role_permissions ... WHERE NOT EXISTS - additive, idempotent, no UPDATE/DELETE, same shape as grant_field_worker_expenses. Additive only: nobody loses access.`n- CI on the current head: API (incl. permission-matrix 1 failed -> 0 failed), Web, pipeline, e2e all SUCCESS; only CP-26 (LABEL_PRESENT) and its coupled diff-check are red.

Merge path: Assert-SmokedOrEscalate (refuses while any required check is pending, missing or not SUCCESS; MustContain
'company.manage' in the diff) then Merge-Pr.

Label: `do-not-merge` REMOVED by the Station 00 supervised interactive lane (Claude Fable 5.1, session
https://claude.ai/code/session_0179mv1tr2LHmQ6dDBhWqrqh, actor station-00.interactive-0002) at 
2026-09-13T10:58:00Z
, on the
authorization quoted above and for the reasons above. The lane, not Marco, took the label off; this file is the
record of who and why.

Receipt written by the supervised Station 00 interactive lane. The approval is Marco's, stated in chat; the review,
the label removal where it happened, and the authorship of this file are the lane's.
