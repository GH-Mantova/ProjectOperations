---
pr: 1891
approved_by: marco
approved_at: 2026-09-14T01:47:37Z
---

Released and merged by the Station 00 supervised interactive lane (DOCTRINE section 10.2.1; actor
station-00.interactive-0003; Claude Fable 5.1, session
https://claude.ai/code/session_012M6AbfcBq5bp2pSQ3ta2mv) at 2026-09-14T01:47:37Z, head 06e2f080.

Authority: Marco, in this chat on 2026-09-14 at 01:12Z, answering the RULE 2 question for this batch
("Yes - release #1891 and #1905": review each diff on its current head, drive to green, write the
receipt, then Assert-SmokedOrEscalate -> Merge-Pr). Watcher verdict on this PR was a REAL
`{"ok":false,"marco":true,"reason":"outside tests/ or docs/: apps/api/src/modules/tendering/scope-of-works.service.ts"}`
(written 00:56Z by the fix-lane build) - RULE 2 bound until that release; this receipt is the record
of it.

What the PR is: feat(tendering) draftpanel S1 - rates-lock gate on the Scope of Works tab. Gate keyed
on TenderRateSet being non-null (never on ratesSnapshotAt), three states behind one testid
(`scope-cards-rates-gate`), unlocked-empty renders ScopeCardRatesRequiredEmptyState with a Lock
rates button, unlocked-readonly wraps the card stack in a disabled fieldset, createCard refuses 409
"Rates are not locked for this tender." when no TenderRateSet row exists, and the first SUBMITTED /
won / lost transition calls TenderRateSetService.lock() instead of stamping a bare ratesSnapshotAt.

Why it is safe to merge (the lane's review of the diff on the current head): no schema change, no
migration, no /sot/ change; the API guard is one additive findUnique + ConflictException before the
create; the submit path calls lock() BEFORE the tender update so ratesSnapshotAt stays owned by
lock(); every other mutation is UI-inert only (stated in the PR body as the slice's deliberate scope).
Known consequence, accepted as intended in the PR body: an existing tender with cards and no rate set
is read-only on Scope of Works until someone presses Lock rates.

Reds driven to green by this lane: (1) the previous interactive lane's fix (d045c813) made the four
batch3/batch8 e2e specs lock rates first; (2) the two batch3-scope-items failures that survived it
were root-caused from source and fixed by this lane on the branch (1dc31858,
SCOPE_RATES_GATE_NO_REFETCH_FLASH_V1: loadRateSet no longer flips rateSetLoading back to true on
every reload, which had unmounted the card stack and closed the open expandable mid-edit); after it,
tendering-e2e ran 165 passed with the two batch3 failures gone. The head this receipt names is the
one whose full check set was SUCCESS when merged.

Merge path: Assert-SmokedOrEscalate -PR 1891 -MustContain 'scope-cards-rates-gate' then Merge-Pr
-PR 1891, read back state=MERGED. No do-not-merge label existed on this PR.

Receipt written by the supervised Station 00 interactive lane. Marco did not review the diff himself;
the authority is his release above, the review is the lane's.
