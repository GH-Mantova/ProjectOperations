---
pr: 1896
approved_by: marco
approved_at: 2026-09-13T13:58:00Z
---

Released under Marco's authorization given in chat on 2026-09-13 at about 10:05Z, in the Cowork session he was
directing (DOCTRINE section 10.2.1, supervised interactive lane): "you have my permission to review all open prs,
identify if they are safe to merge or not, drive them to green and merge / you may remove the label yourself and
enter the receipt where required / make sure to follow the guidelines (name yourself and reasons why label was
removed)". He had said just before that he would not remove a label himself without a verdict saying it is safe.

What the PR is: feat(schedule-of-rates): S6a - guarded push-back API (preview + push, hub row id preserved) (Rate Hub S6a: guarded push-back API (preview + push, hub row id preserved) - SorPushBackService, SorPushBackController, rates.push-back permission (isHighRisk), 7 spec groups).

Why it is safe to merge (the lane's review, measured on the current head 
0979127a
):
- No watcher verdict exists for this PR, so the lane reviewed the diff itself on the current head: every endpoint sits behind JwtAuthGuard + PermissionsGuard with RequirePermissions(rates.push-back) (registered in permission-registry.ts as isHighRisk) or rates.manage; the confirmed push demands the expected figures and answers 409 on any mismatch; an EXPIRED period is Forbidden before any write; an internal push updates the SAME RateRow id inside one $transaction (never create, never isActive), which is what keeps every locked tender TenderRateEntry key valid; a vendor push follows the schema supersede rule in one $transaction; every push writes an audit row. No schema change, no web change, no /sot/ change.`n- Two fix-lane runs by this lane: the futureLocks filter named the relation tenderRateSet (Prisma knows it as rateSet), and the internal push issued the array-form $transaction on the outer client so the row update ran outside it; both corrected, the spec now drives the interactive transaction client. API job green on the current head.`n- Labelled do-not-merge at open (escalates true: it writes master hub rates). Reviewed by the lane; safe to merge; label removed and receipt entered under the 10:05Z authorization.

Merge path: Assert-SmokedOrEscalate (refuses while any required check is pending, missing or not SUCCESS; MustContain
'SorPushBackService' in the diff) then Merge-Pr.

Label: `do-not-merge` REMOVED by the Station 00 supervised interactive lane (Claude Fable 5.1, session
https://claude.ai/code/session_0179mv1tr2LHmQ6dDBhWqrqh, actor station-00.interactive-0002) at 
2026-09-13T13:58:00Z
, on the
authorization quoted above and for the reasons above. The lane, not Marco, took the label off; this file is the
record of who and why.

Receipt written by the supervised Station 00 interactive lane. The approval is Marco's, stated in chat; the review,
the label removal where it happened, and the authorship of this file are the lane's.
