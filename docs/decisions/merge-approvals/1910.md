---
pr: 1910
approved_by: marco
approved_at: 2026-09-14T02:52:35Z
---

Released and merged by the Station 00 supervised interactive lane (DOCTRINE section 10.2.1; actor
station-00.interactive-0003; Claude Fable 5.1, session
https://claude.ai/code/session_012M6AbfcBq5bp2pSQ3ta2mv) at 2026-09-14T02:52:35Z, head 8302e2a8.

Authority: Marco, in this chat on 2026-09-14 ~02:02Z: "keep opening/arming prs that are either slice-0
or have their gates open / drive them to green and merge" - read with his 09-07 ruling (section 10.2.1:
the lane merges, but writes a receipt first). This lane armed the prompt (02:13:14Z,
pr-draftpanel-s2-finish-this-draft, gate released by #1891); the watcher built and opened the PR and
recorded the policy routing `{"ok":false,"marco":true}` (apps/web outside tests|docs) - RULE 2 bound
until the release above; this receipt is the record of it.

What the PR is: feat(tendering) draftpanel S2 - Finish-this-draft panel + wizard re-entry from the
tender itself. `deriveDraftCompleteness()` (pure, 5 checkable of 7 steps), `DraftProgressPanel`
(step list, hatched meter, Resume wizard / Discard draft), mounted on `TenderDetailPage` while
`status === "DRAFT"`, `NewTenderWizard` mounted for re-entry with a `resume` reducer action, and the
`ratesLocked` rehydration fix (reads `GET /tenders/:id/rate-set`). Six files, all inside the prompt's
scope; web-only; no API, schema, migration or /sot/ change. `design_ref` mock-up named in the prompt.

Why it is safe to merge: review-lane verdict MERGE (rev-1910, 02:40Z, scope compliance 6/6, self-verification
claims checked); this lane read the PR body and the review, and repaired the one red itself -
`DraftProgressPanel.tsx` carried 12 hex literals (hex ratchet, run 34799543424) -> tokens by role
(cbddf52d, DRAFTPANEL_S2_HEX_TO_TOKENS_V1; the undefined `--surface-border` replaced with
`--border-default`). The head named above had every check SUCCESS when merged. Visual smoke
(visual-smoke.mjs screens) was NOT run by this lane; the acceptance rests on CI, the review lane and
the unit tests in the PR.

Merge path: Assert-SmokedOrEscalate -PR 1910 -MustContain 'deriveDraftCompleteness' then Merge-Pr
-PR 1910, read back state=MERGED. No do-not-merge label existed on this PR.

Receipt written by the supervised Station 00 interactive lane. Marco did not review the diff himself;
the authority is his standing release above, the review is the lane's and the review lane's.
