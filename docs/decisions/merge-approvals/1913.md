---
pr: 1913
approved_by: marco
approved_at: 2026-09-14T05:03:37Z
---
# Merge approval — #1913 feat(branding): S4 Harbour and Graphite as named BrandColorScheme presets

Prompt `pr-brandtheme-s4-named-presets-seed` armed by this lane 03:20:25Z (gate S3 satisfied; lint ADMIT; markers
0/0/0; scope overlap 0) under Marco's standing 09-14 ~02:02Z instruction; the watcher built it as #1913.

**Migration released by Marco in chat ~04:15Z** ("what do you suggest" → lane recommended a guarded insert-if-absent
migration per the Default-row precedent → "Yes — add the migration"). Lane pushed `5a6c65c7`:
`20260914041500_brandtheme_s4_named_presets/migration.sql`, two `INSERT … ON CONFLICT DO NOTHING`, 30/30 hex
values identical in order to `HARBOUR_PRESET`/`GRAPHITE_PRESET`, no UPDATE/DELETE, `active_color_scheme_id` untouched.
`GATE-ALLOW: migrations` declared bare at column 0 of the PR body by this lane (CP-11).

Receipt written by **station-00.interactive-0003** (DOCTRINE §10.2.1), head `978684c7`.

Reviewed on the head: station review `pr-1913-review.md` (03:42Z) FIX-FORWARD on CP-23 only, code rated in scope
and idempotent (`upsert` by unique name, `update: {}`); the migration supersedes its SEED-ONLY suggestion.
Scope: seed, its spec, the migration — nothing else; no `/sot/`; no labels. All checks green before
`Assert-SmokedOrEscalate -PR 1913` → `Merge-Pr`.

Open product note for Marco (from the PR body, not acted on): `update: {}` means re-seeds never overwrite an edited
preset; the mockup lists five schemes and only two were seeded.

Needle: brandtheme-s4-named-presets
