---
pr: 1920
approved_by: marco
approved_at: 2026-09-14T09:44:38Z
---
# Merge approval — #1920 feat(platform): EA-2a — seed Estimating Analytics UserDashboard preset

`escalates: true` prompt `pr-ea-s2a-dashboard-preset-seed`, armed by this lane 06:03:57Z after Marco's chat
release ("Arm it, PR holds for you", ~05:30Z). The watcher built it as #1920 and applied `do-not-merge` (CP-26).

**Marco approved the merge in chat ~08:55Z 2026-09-14** ("Approve — I'll remove the label"): Marco removes
`do-not-merge` himself; this lane writes this receipt and runs `Assert-SmokedOrEscalate -PR 1920` → `Merge-Pr`
once every check is green.

Receipt written by **station-00.interactive-0003** (DOCTRINE §10.2.1), head `8babcff0`.

What this lane changed on the branch, each released by Marco in chat:
- `375c603a` EA2A_SEED_ROLE_RELATION_V1 — `prisma.user.findMany` used a relation `permissions` that does not
  exist; `Role.rolePermissions` (schema.prisma:405) does. CI `pnpm seed` had crashed on it.
- `986b7c4b` EA2A_PRESET_MIGRATION_V1 — `20260914063000_ea2a_estimating_analytics_preset/migration.sql`:
  INSERT … SELECT over users holding `reporting.view`, WHERE NOT EXISTS, ON CONFLICT DO NOTHING; config JSON
  checked identical to the seed's ten widgets in order (CP-23 required it; migration released ~06:30Z).
  `GATE-ALLOW: migrations` declared bare at column 0 of the PR body (CP-11 → ALLOWED).

Reviewed on the head: scope is `seed.ts`, its spec, the migration — nothing else; `active`/`is_default` flags
untouched; no `/sot/`. `Tendering Browser Smoke` (migrate + seed + e2e) SUCCESS on `986b7c4b`.

Needle: ea-2a-estimating-analytics
