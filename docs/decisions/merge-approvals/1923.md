---
pr: 1923
approved_by: marco
approved_at: 2026-09-15T01:01:19Z
---

---
pr: 1923
approved_by: marco
approved_at: 2026-09-15T01:01:19Z
---
# Merge approval — #1923 fix(rates): S0 — rename clash answers 409 not 500; delete-column refusal message

Prompt `pr-ratescol-s0-column-api-hygiene` armed by the scheduled Station 00 (`station-00.0808`, 08:24:38Z
09-14); watcher built it as #1923. Merged by the supervised interactive lane under Marco's 09-15 ~00:55Z
instruction: "keep driving the board, opening prs, running all checks/tests marked in their body, driving them to
green, and merging them".

Receipt written by **station-00.interactive-0003** (DOCTRINE §10.2.1), head `2b33a810`.

Checked on the head by this lane (the watcher's review job for #1923 failed to file a verdict, so this is the review):
- Diff read in full: two files, exactly the prompt's scope (`rate-tables.service.ts`, its spec). `updateColumn`
  now wraps `rateColumn.update` in the same `isUniqueViolation` → `ConflictException` shape `createColumn` uses;
  `deleteColumn` keeps the row-count guard and the 409, changes only the message. No route/DTO/schema/seed change.
- Body checks re-run against the head: `grep "Pick another name"` PASS, `grep "Remove the rows first"` PASS.
  `pnpm build` / `pnpm lint` / `@project-ops/api test` claims: executed by CI on the head (`API — lint, test,
  compliance smoke` SUCCESS; `Web — lint, logic tests, vitest, build` SUCCESS); the two new spec cases are in the diff.
- 15/15 checks green before `Assert-SmokedOrEscalate -PR 1923` → `Merge-Pr`.

Needle: ratescol-s0
