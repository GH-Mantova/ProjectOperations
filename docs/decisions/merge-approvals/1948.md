---
pr: 1948
approved_by: marco
approved_at: 2026-09-15T02:03:00Z
---
# Merge approval — #1948 feat(forms): fv2-import S2 — review the extraction before creating the template

Prompt `pr-fv2-import-s2-review-route` armed by this lane 01:13:02Z 09-15 (lint PROMOTE, gate released by #1918;
escalates:false; scope overlap 0). The first fire failed 3× — the writer obeyed the prompt's own prose
`## STATUS — HOLD. Marco arms this.` block and refused — which auto-paused the queue. This lane re-fired it with that
block reworded to ARMED (arming-log `REFIRED` row 01:20:51Z) and restarted the watcher per 03's procedure
(`ensure-watcher.ps1`, detached, verified). The watcher built it as #1948.

Receipt written by **station-00.interactive-0003** (DOCTRINE §10.2.1), head `5e40ab26`.

Checked on the head:
- Diff: exactly the eight scope files (three-endpoint split `preview-import` / `:jobId` / `:jobId/create`, in-memory
  30-min store, provenance, review page + route, modal navigates to the review). No schema, no migration.
- `done_when` greps re-run on the head: `preview-import` in the controller PASS (9), `forms/import/:jobId` in
  `App.tsx` PASS (1). `pnpm build` / api tests (24) / web tests claims: executed by CI on the head.
- Watcher review `pr-1948-review.md`: FIX-FORWARD on ONE ground — hex ratchet `NEW FormImportReviewPage.tsx 0 -> 16`
  (the seventh PR on this pattern, second since `HEX_RATCHET_IN_DONE_V1`). Cured on branch `5e40ab26`
  (FV2_IMPORT_S2_HEX_TO_TOKENS_V1, 16 lines, local ratchet OK). Its `needs-marco/pr-1948-review-fix.md` is
  discharged by this merge.
- All checks green before `Assert-SmokedOrEscalate -PR 1948` → `Merge-Pr`.

Needle: fv2-import-s2
