---
pr: 1964
approved_by: marco
approved_at: 2026-09-15T06:42:13Z
---

# Merge approval — #1964 fix(pipeline): the drive-root escapee scan, and PR-Master becomes binding

Opened by Station 00's supervised interactive lane in answer to Marco's 09-15 ~05:40Z question ("check if the
watcher/agents/stations are monitoring C:\PR-Master") and his ruling at ~06:00Z ("yes" to the fixes) and
~06:38Z ("PR-Master is the root folder for all pr worktrees … this is where station 06 should be saving all
its drafted prs"). Merged under his standing 09-15 ~00:55Z instruction to drive the board.

Receipt written by **station-00.interactive-0003** (DOCTRINE §10.2.1), head `9e1a1840`.

Checked on the head:
- `scripts/pipeline/status-sweep.ps1` — the only code change. Proven by A/B control on one box one minute
  apart: the old scan called an unregistered drive-root tree "none found"; the new scan reports it. PS 5.1
  parser 0 errors; control tree torn down after the run.
- `docs/pipeline/PR-MASTER.md` and `docs/pipeline/stations/06-pr-master.md` — the ruling, quoted, at the step
  06 reads. `lint-station` ADMIT all 8; `check-breadcrumb` CLEAN.
- Every edit carries a byte-delta assert (`delta == expected`, `U+FFFD == 0`). No `/sot/`, no labels, no
  migration, no `apps/**`.
- All checks green before `Assert-SmokedOrEscalate -PR 1964` → `Merge-Pr`.

This lane's own violations were cured before claiming the rule: `C:\po-fix1891` quarantined (moved, 3713
files intact) and the reusable scripts retargeted — this PR's worktree was `C:\PR-Master\worktrees\collect-0640`.

Needle: prmaster-root-binding
