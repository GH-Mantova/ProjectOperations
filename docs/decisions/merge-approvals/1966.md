---
pr: 1966
approved_by: marco
approved_at: 2026-09-15T08:32:17Z
---

# PR 1966 — approval receipt

**Merged by:** Station 00, supervised interactive lane `station-00.interactive-0003` (DOCTRINE
§10.2.1), under Marco's instruction of 2026-09-15: *"we need to fix this issue. I want all drafted
prompts to sit under the same folder or at least root folder. The system was creating several
folders under C:\ due to worktree issues."*

**What it is:** one new script, `scripts/pipeline/new-worktree.ps1` (`PO_WORKTREE_HELPER_V1`), plus
the two doc sections that point every station at it. Callers pass a slug; the helper computes the
path under `C:\PR-Master\worktrees\`. No code path, no migration, no seed, no `/sot/`.

**Grounds for merge at `d2f629df`:**

- Self-test on the box, PowerShell 5.1: **13 PASS / 0 FAIL**, parser 0 errors. It refuses a slug
  that is a path (exit 2), a drive-root root (exit 3), an occupied non-worktree directory (exit 4,
  fixture file verified still present), and an existing worktree without `-Force` (exit 4); it
  creates, registers, hands back only the path, and tears down clean. Drive-root worktrees after
  the run: 0.
- `node scripts/pipeline/lint-station.mjs` → `ADMIT: all 8 docs clean`, exit 0.
- CI green on the head; no `do-not-merge` label.
- **Proven in use before merge:** PR #1968's worktree was created by this helper
  (`-Slug rcpt-tidy`), which put it under the correct root without being told where.
- `Assert-SmokedOrEscalate -PR 1966` then `Merge-Pr -PR 1966`. No hand merge.

**Raised for Marco, not decided here:** `docs/pipeline/stations/02-board-driver.md:391` names a
third worktree root, `C:\ProjectOperations-Reference\worktrees\po-{n}-smoke`, carrying explicit
SAFETY language that it is Marco's own reference material. Left untouched pending his answer.

Receipt written 2026-09-15T08:32:17Z.
