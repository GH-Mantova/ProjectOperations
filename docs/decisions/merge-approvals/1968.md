---
pr: 1968
approved_by: marco
approved_at: 2026-09-15T08:46:28Z
---

# PR 1968 — approval receipt

**Merged by:** Station 00, supervised interactive lane `station-00.interactive-0003` (DOCTRINE
§10.2.1). Marco approved this work in chat, 2026-09-15, when this lane reported the doubled front
matter in merged receipts and offered to tidy it.

**What it is:** `scripts/pipeline/check-receipts.mjs` (`RECEIPT_SHAPE_GUARD_V1`) + its 7 tests +
one CI step, and 41 receipt files repaired. Docs and tooling only; no gate logic changed.

**Grounds for merge at `c9abb401`:**

- The measurement that justifies it: **26 committed receipts (1736..1799) carry a UTF-8 BOM and
  fail CP-26's own front-matter regex as committed.** `approval-receipt.mjs`'s parser anchors at
  string start with no `/m` flag, so a BOM yields `RECEIPT_MALFORMED_FRONT_MATTER`. All 26 parse
  after the BOM is stripped. The defect was invisible because CP-26 reads one receipt, for the PR
  being merged, only while `do-not-merge` is on.
- The repair refused to guess: a second front-matter block was removed **only** when byte-identical
  to the first. `LEFT ALONE because the two blocks differ: 0` — all 15 were exact duplicates, so no
  information left any merge-approval record.
- Guard before: `41 problem(s) across 120 receipt(s)`, exit 1. After: `120 receipt(s) parse under
  CP-26's own front-matter regex. OK`, exit 0.
- `node --test scripts/pipeline/__tests__/check-receipts.test.mjs` → 7 pass, 0 fail. The BOM case
  and the doubled case are both pinned, so a test suite that would have passed on 09-14 with 26
  unreadable receipts committed cannot pass now.
- CI green on the head, `checks=15 fail=0 pending=0`, no `do-not-merge` label.
- `Assert-SmokedOrEscalate -PR 1968` then `Merge-Pr -PR 1968`. No hand merge.

**Recorded in the PR, not fixed here:** the lane's own `sup-0003-receipt.ps1` had been stamping
every receipt commit with a hard-coded *"Marco release in chat 2026-09-14T01:12Z"* — an authority
Marco gave for a different PR. Receipt bodies are correct; the commit messages are wrong and are
immutable history. The script now takes the authority as an argument.

Receipt written 2026-09-15T08:46:28Z.
