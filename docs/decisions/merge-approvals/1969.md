---
pr: 1969
approved_by: marco
approved_at: 2026-09-15T11:44:18Z
---

# PR 1969 — approval receipt

**Merged by:** Station 00, supervised interactive lane `station-00.interactive-0003` (DOCTRINE
§10.2.1), under Marco's standing instruction to drive the board to green and merge.

**What it is:** Dependabot's patch bump of `sharp` 0.35.3 → 0.35.4 in the `npm_and_yarn` group.
Two files: `apps/api/package.json` (+1/-1) and `pnpm-lock.yaml` (+141/-128). No first-party code.

**Grounds for merge at `47f1ca3b`:**

- **It was never failing.** The PR read `BLOCKED` with what looked like 10 failed jobs. The rollup's
  conclusions were `CANCELLED x10, SUCCESS x12, NEUTRAL x1` — **zero FAILURE**. Four concurrent CI
  runs for the same sha were cancelled by the concurrency group when Dependabot force-pushed, and
  the surviving run (`34953494441`) succeeded, as did Tendering Browser Smoke (`34953493205`). The
  cancelled duplicates stayed in the rollup and held the PR blocked.
- Cured the way this lane cured the same class of staleness on #1920: `gh pr update-branch`, which
  produced one fresh run. After it: `MERGEABLE CLEAN, fail=0 cancelled=0 pending=0`.
- CP-13 (dependencies) passed on the diff; no `do-not-merge` label; `sharp` is a transitive image
  dependency of the API, and a patch bump inside the same minor carries no API change.
- `Assert-SmokedOrEscalate -PR 1969` then `Merge-Pr -PR 1969`. No hand merge.

**Method note, recorded because it cost a wrong claim:** this lane's own poll script counted any
conclusion outside `SUCCESS/NEUTRAL/SKIPPED` as a failure, so it reported `fail=10` on a PR with no
failures, and that wrong number reached Marco in chat. `CANCELLED` is a superseded concurrent run,
not a failure. The script now counts only `FAILURE / TIMED_OUT / ACTION_REQUIRED / STARTUP_FAILURE`
and reports cancellations in their own column.

Receipt written 2026-09-15T11:44:18Z.
