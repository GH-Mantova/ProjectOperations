---
pr: 1970
approved_by: marco
approved_at: 2026-09-15T11:18:24Z
---

# PR 1970 — approval receipt

**Merged by:** Station 00, supervised interactive lane `station-00.interactive-0003` (DOCTRINE
§10.2.1), under Marco's instruction of 2026-09-15: *"All prs are being saved now on the pr master
folder... Make sure they are captured, staged, armed, driven to green, all checks/tests within their
bodies are conducted, and then merged."*

**What it is:** one file, `docs/pr-prompts/pr-scopecards-s5-charge-steps-price-cutting-HOLD.md`,
drafted by Station 06 in `C:\PR-Master\drafts\`. Docs-only, +263/-0. It lands as `-HOLD`: merging
stages the prompt, it does not dispatch it.

**Grounds for merge at `4f3d8e48`:**

- The PR's file is **byte-identical to the draft in `C:\PR-Master\drafts\`** — 16,955 bytes both
  sides, compared as git blobs (an earlier mismatch was this lane's own PowerShell redirect writing
  UTF-16, not a difference in the file).
- `node scripts/pipeline/lint-prompt.mjs` on the PR head: `REJECT [GATE_NOT_RELEASED]` —
  `requires_on_main: ClientQuotesPanel.tsx :: QUOTE_PUSH_PANEL_V1` is not on `main`. **That is the
  correct state, not a defect**: S5 declares S4b's marker and S4b has not been built. The file is
  parked, exactly like s2a/s2b/s3/s4a/s4b already in the queue.
- `node scripts/pipeline/check-receipts.mjs` on this head: `122 receipt(s) parse under CP-26's own
  front-matter regex. OK` — the guard merged in #1968 is green on the tree this PR lands on.
- CI: `MERGEABLE CLEAN, checks=15 fail=0 pending=0`. No `do-not-merge` label.
- `Assert-SmokedOrEscalate -PR 1970` then `Merge-Pr -PR 1970`. No hand merge.

**Not done here — and it cannot be:** S5 is NOT armed. `escalates: true`, and `arm-prompt.ps1`
refuses any prompt whose gate lint does not say `GATE_RELEASED`. The chain ahead of it is
S1 (#1967, open) → S2a → S2b → S3 → S4a → S4b → S5. It arms one at a time when its predecessor's
marker reaches `main`.

**Materialisation:** after this merge the file is copied onto `C:\ProjectOperations2\docs\pr-prompts\`.
A committed prompt that is not materialised never runs — the watcher globs the dev-tree filesystem,
not `main`, and nothing watches `C:\PR-Master\drafts\`.

Receipt written 2026-09-15T11:18:24Z.
