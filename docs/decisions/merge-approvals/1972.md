---
pr: 1972
approved_by: marco
approved_at: 2026-09-15T23:05:00Z
---

# Merge approval — #1972 feat(tendering): DraftPanel S3 — carry-over strip and Unfinished-drafts picker

**Written AFTER the merge, and that is a defect this receipt records rather than hides.**
Station 00's supervised interactive lane (`station-00.interactive-0003`, DOCTRINE §10.2.1) merged
#1972 at `beafbfdc` without first writing a receipt. Every other merge this lane made on 2026-09-15
— #1963, #1965, #1966, #1968, #1969, #1970, #1973 — had its receipt pushed onto the PR branch
before `Merge-Pr`. This one did not, because the merge ran from a batched
`sup-0003-mergeif.ps1` poll-and-merge script that checks labels and check conclusions but has no
receipt step, so the lane's own rule was enforced by habit at the call site rather than by the
tool. The tool is the thing to fix; a rule that only holds when someone remembers it is the same
class of defect as the escapee scan that could not see a drive-root worktree.

CP-26 did not and would not have caught this: it runs only while `do-not-merge` is on, and #1972
carried no label (`escalates: false`).

## Authority

Marco's standing instruction, 2026-09-15: *"Make sure they are captured, staged, armed, driven to
green, all checks/tests within their bodies are conducted, and then merged."* `pr-draftpanel-s3-
carry-over-and-picker` carries `escalates: false`, so it is this lane's to merge, not Marco's.

## What it was

Prompt armed 2026-09-15T12:06:12Z, picked up by the watcher 2 seconds later, opened #1972.
Twelve files: the carry-over migration and schema, `tendering.service.ts` / `.controller.ts`,
`DraftCarryOverStrip.tsx`, `TenderDetailPage.tsx`, `TenderingPage.tsx`,
`newTenderWizard.helpers.ts`, two API/web test files, and the regenerated data-model catalogue.

## Grounds for the merge

- **One real red, cured by this lane.** The hex ratchet:
  `NEW apps/web/src/pages/tendering/DraftCarryOverStrip.tsx 0 -> 4`. All four literals were the
  fallback inside `var(--status-warning, #F59E0B)`, which the ratchet counts by design.
  `--status-warning` is defined at `tokens.css:24` as exactly that value, and the house pattern
  elsewhere — `JobSorRegisterPage.tsx:63` and `:286`, the same
  `color-mix(in srgb, var(--status-warning) N%, transparent)` shape — carries no fallback. Dropping
  the fallback renders identically and follows the existing convention. Pushed as `347eee55`;
  no logic changed, no test touched.
- Local verification before that push: hex ratchet `OK — 4226 literals in 260 files (baseline
  allows 4353 in 263), no file grew, no new file carries one`; and the PR body's own web tests,
  `pnpm --filter @project-ops/web test -- draft-carry-over unfinished-drafts-picker`, 2 files /
  20 tests passing.
- Final state before merge: `MERGEABLE CLEAN, fail=0 cancelled=0 pending=0`, no label.
- `Assert-SmokedOrEscalate -PR 1972` returned 0, then `Merge-Pr -PR 1972`. No hand merge.

## Disposition

**ACTIONED** — this receipt. **ESCALATED to this lane's own tooling**: `sup-0003-mergeif.ps1` must
refuse to merge when `docs/decisions/merge-approvals/<N>.md` is absent from the PR's head, so the
receipt rule is enforced by the instrument and not by memory. Reported to Marco in chat rather than
quietly corrected.
