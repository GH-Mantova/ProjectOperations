---
pr: 1973
approved_by: marco
approved_at: 2026-09-15T22:40:39Z
---

# PR 1973 — approval receipt

**Merged by:** Station 00, supervised interactive lane `station-00.interactive-0003` (DOCTRINE
§10.2.1). Marco approved this explicitly in chat, 2026-09-15, after asking where staged PRs go: the
lane proposed moving a draft to `_staged\` once its PR merges, and he replied "approved".

**What it is:** two documentation files. `PR-MASTER.md` gains the drafts lifecycle and states what
does not belong in `drafts\`; `06-pr-master.md` gains Phase 6 step **1b** between staging and
materialising, where 06 will actually read it. No code, no schema, no `/sot/`.

**Grounds for merge at `e1fd77e0`:**

- The measurement that earned the rule: `C:\PR-Master\drafts\` held **nine prompt drafts of which
  zero were unstaged** — five already in the queue byte-identical, two consumed into #1967 and
  #1970, two already retired to `superseded/`.
- It is not hypothetical. This lane **began re-staging scopecards S5 from that folder while Station
  06's own staging PR (#1970) was already open**, three minutes apart, on the same branch name. The
  duplicate was caught only because the push was refused as non-fast-forward — nothing in the
  layout would have stopped it.
- Executed on the box, not merely documented: 9 drafts moved to `C:\PR-Master\drafts\_staged\`,
  each gated on its slug actually existing under the queue first, so a draft with no queue file
  would have been reported and left alone rather than moved on a guess.
  `drafts 0 | _staged 9 | TOTAL PRESERVED 9 (was 9)` — nothing deleted.
- The ten stray PR/receipt bodies this lane had been writing into `drafts\` were moved to
  `C:\PR-Master\scripts\station-00\pr-bodies\` (10 in, 10 out, nothing deleted). This PR's own body
  is written there, which is the rule applied to itself.
- `node scripts/pipeline/lint-station.mjs` → `ADMIT: all 8 docs clean`, exit 0.
- CI: `MERGEABLE CLEAN, fail=0 cancelled=0 pending=0`. No `do-not-merge` label.
- `Assert-SmokedOrEscalate -PR 1973` then `Merge-Pr -PR 1973`. No hand merge.

Receipt written 2026-09-15T22:40:39Z.
