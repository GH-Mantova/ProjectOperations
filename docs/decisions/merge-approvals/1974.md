---
pr: 1974
approved_by: marco
approved_at: 2026-09-15T23:07:02Z
---

# PR 1974 — approval receipt

**Merged by:** Station 00, supervised interactive lane `station-00.interactive-0003` (DOCTRINE
§10.2.1), under Marco's standing instruction to capture, stage, drive green and merge the PR-Master
drafts.

**What it is:** Station 06's correction to the scopecards **S5** prompt merged in #1970. One file,
`docs/pr-prompts/pr-scopecards-s5-charge-steps-price-cutting-HOLD.md`, +110/-55. Docs only.

**Grounds for merge at `96b9124c`:**

- **Nothing was built from the old text.** S5 is a gated HOLD — `lint-prompt` returns
  `GATE_NOT_RELEASED` against S4b's `QUOTE_PUSH_PANEL_V1`, which is not on `main` — so the prompt
  has never dispatched and the correction costs only the edit. Correcting it now is strictly
  cheaper than correcting the code S6 would have had to tear out later.
- 06's grounds, which hold up: the Discipline Cards mock-up defines all four cutting rate tables in
  its `CUT_TABLES` block and states the rule — *"a rate table is a LOOKUP … no collapsing, no
  allowlists, no sanitising"* — with the old loadings becoming **priced rows** (Demosaw and Ringsaw
  wall cuts) or **conditional steps** (the +25% methods). The merged draft had invented a
  `Wall uplift` INFO column that appears nowhere in the mock-up.
- All three corrections are price-neutral by 06's own statement, and the file stays `escalates: true`
  with `gate_allow: migrations` — when S5 eventually builds, Marco merges it.
- CI green on the head, no `do-not-merge` label.
- `Assert-SmokedOrEscalate -PR 1974` then `Merge-Pr -PR 1974`. No hand merge.

**Still not armed, and still cannot be.** `arm-prompt.ps1` refuses any prompt whose gate lint is not
`GATE_RELEASED`. The chain ahead of S5 is S1 (#1967) → S2a → S2b → S3 → S4a → S4b.

Receipt written 2026-09-15T23:07:02Z.
