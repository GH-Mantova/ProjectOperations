---
pr: 1975
approved_by: marco
approved_at: 2026-09-16T03:40:53Z
---

# PR 1975 — approval receipt

**Merged by:** Station 00, supervised interactive lane `station-00.interactive-0003` (DOCTRINE
§10.2.1), under Marco's instruction of 2026-09-16: *"you drive the board. fix any issues with any
open pr."*

**What it is:** Station 05's SoT doc-reconcile carrying the 2026-09-15 sweep. Three files:
`sot/02-roadmap-and-status.md` (§2 live snapshot, 7 insertions / 7 deletions),
`docs/data-model/sweeps/2026-09-15.md`, and the run's breadcrumb.

**A disclosure, because it matters more than the merge.** The PR body says *"SoT governance doc —
**Marco reviews the rendered diff**"*, and this lane flagged it to him three times over two days
rather than merging it. He then said to drive the board. So it is merged on that instruction, and
**this is a one-command revert** if he wanted his own eyes on the rendered diff first. The lane did
not edit `sot/` — that is 05's lane, and CP-24 is what proves it.

**Grounds for merge at `6ae22ada`:**

- **CP-24 satisfied by construction, verified rather than taken on trust.** Every changed path
  listed and checked: all three are under `sot/` or `docs/`. No `scripts/`, no `apps/`.
- The `sot/02` diff was read in full, not just its diffstat. It replaces the §2 *"In-PR — open right
  now"* table and its reconcile-time header; **the curated "Why this table moved on 2026-09-06" note
  below it is untouched**, and no roadmap STATUS semantics change.
- What it replaces was badly out of date: the old snapshot read `2026-09-09T23:03Z` and named
  #1823–#1827. The new one reads `2026-09-15T22:35Z`. Strictly fresher.
- CI green on the head, `MERGEABLE CLEAN`, no `do-not-merge` label.
- `Assert-SmokedOrEscalate -PR 1975` then `Merge-Pr -PR 1975`. No hand merge.

## Finding for Station 05 — the snapshot was stale before it merged

The table this PR writes lists five PRs as open. At merge time **four of the five rows are already
wrong**: #1967, #1971 and #1972 are MERGED, and #1960 was CLOSED an hour ago as the duplicate of
#1959. Only #1973 is described correctly, and it is merged too.

This is not a defect in the PR — §2 declares itself a point-in-time read and the note directly
beneath it says `bring-up-to-speed.ps1`'s `[LIVE]` lines beat the table the moment it drifts. But a
table that is wrong on four of five rows the day it lands is a table nobody can trust, and the
previous snapshot sat stale from 09-09 to 09-15 for the same reason. **ESCALATED to 05**: either
§2 stops enumerating individual PRs and points at the live command instead, or the reconcile has to
run close enough to the merge that the snapshot survives contact with the board. Enumerating a
fast-moving list in a document that reconciles weekly cannot be made correct by trying harder.

Receipt written 2026-09-16T03:40:53Z.
