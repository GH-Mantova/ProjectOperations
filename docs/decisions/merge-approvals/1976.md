---
pr: 1976
approved_by: marco
approved_at: 2026-09-15T23:09:50Z
---

# PR 1976 — approval receipt

**Merged by:** Station 00, supervised interactive lane `station-00.interactive-0003` (DOCTRINE
§10.2.1), under Marco's standing instruction of 2026-09-15 to drive the board to green and merge.

**What it is:** one file, `docs/decisions/merge-approvals/1972.md` — the receipt this lane failed to
write before merging #1972. Docs only.

**Grounds for merge at `788a4a9a`:**

- The omission is real and is recorded rather than quietly corrected: #1972 was merged with no
  receipt because the batched poll-and-merge helper checked labels, check conclusions and
  mergeability but had no receipt step. CP-26 could not have caught it — that gate runs only while
  `do-not-merge` is on, and #1972 carried no label.
- The 1972 receipt states all of this in its own text, including the disposition, so the record is
  honest at the place someone will look for it.
- **The instrument is fixed, not just the record.** `sup-0003-mergeif.ps1` now refuses to merge when
  `docs/decisions/merge-approvals/<N>.md` is absent from the PR head and prints the exact
  `sup-0003-receipt.ps1` invocation needed. This receipt exists because that gate demanded it —
  the first merge it has governed.
- `node scripts/pipeline/check-receipts.mjs` on this tree: `126 receipt(s) parse under CP-26's own
  front-matter regex. OK`, exit 0. The guard merged in #1968 runs on this PR too.
- CI green on the head, no `do-not-merge` label.
- `Assert-SmokedOrEscalate -PR 1976` then `Merge-Pr -PR 1976`. No hand merge.

Receipt written 2026-09-15T23:09:50Z.
