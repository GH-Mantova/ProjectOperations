---
pr: 1977
approved_by: marco
approved_at: 2026-09-16T00:35:57Z
---

# PR 1977 — approval receipt

**Merged by:** Station 00, supervised interactive lane `station-00.interactive-0003` (DOCTRINE
§10.2.1). Marco approved this in chat, 2026-09-16, when this lane reported the census and proposed
arming `ratescol-s3` and folding the three retirements into one docs PR: *"go for it"*.

**What it is:** three consumed prompts moved from the queue root to `docs/pr-prompts/superseded/`.
Docs only.

**Grounds for merge at `9eeb21a4`:**

- **All three moves are R100 renames** — git reports 100% similarity, so no prompt content changed
  and nothing was deleted. Verified after the move that each file exists under `superseded/`.
- Each one's PR is MERGED, checked against the API rather than assumed: `draftpanel-s3` → #1972,
  `crmvis-s1` → #1956, `ratescol-s2` → #1958.
- The last two additionally lint **STALE** — `lint-prompt.mjs` reports the premise no longer holds
  and *"the work is ALREADY DONE. Binned before spawning an agent… 34 historical runs were burned on
  exactly this."* A stale prompt at the queue root remains a candidate every time the board is asked
  what can be armed; retiring it removes the candidate instead of relying on the last-moment refusal.
- Root HOLD count 39 → 36, matching the census taken at 00:29Z before the change.
- CI green on the head, no `do-not-merge` label.
- `Assert-SmokedOrEscalate -PR 1977` then `Merge-Pr -PR 1977`. No hand merge, and the receipt gate
  added after #1972 required this file to exist before the merge could run.

**Raised, not acted on:** `pr-ratescol-s3-add-in-grid` (armed 2026-09-16T00:34:22Z) shares four
scope files with #1960, which has been `CONFLICTING/DIRTY` for a day as the duplicate build of
#1959. Closing #1960 is Marco's call; this lane has not touched it.

Receipt written 2026-09-16T00:35:57Z.
