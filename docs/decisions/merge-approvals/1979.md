---
pr: 1979
approved_by: marco
approved_at: 2026-09-16T04:09:34Z
---

# PR 1979 — approval receipt

**Merged by:** Station 00, supervised interactive lane `station-00.interactive-0003` (DOCTRINE
§10.2.1). **Marco released it in chat, 2026-09-16: "label removed"** — `escalates: true`, so the
removal of `do-not-merge` is the release, and he removed it himself. This lane has never touched
that label.

**What it is:** Scope Cards / Rates **S3** — add columns and rows in the grid, guided first step,
shrunk Fields card. Five files: the new `EmptyTableFirstStep.tsx`, `FilterableRateGrid.tsx`,
`RatesListsAdminPage.tsx`, `ratesListsHelpers.ts` and its test. Prompt
`pr-ratescol-s3-add-in-grid`, armed by this lane 2026-09-16T00:34:23Z.

**Grounds for merge at `f28a7e41`:**

- **Station review verdict: MERGE**, with one blocking objection — *"blocks on one hex-ratchet
  violation: EmptyTableFirstStep uses a hard-coded `#d1d5db` border fallback"*. The review confirmed
  everything else the prompt required: the guided panel, the `structureAdding` prop, `handleAddRow`,
  the `pricingFieldRows` helper with tests, the Fields card shrunk and relocated, `RatesTab`
  untouched, CI green.
- **That objection is cleared** (`cffdec6b`). `var(--border-default, #d1d5db)` → `var(--border-default)`.
  Dropped rather than baselined for two reasons, the second the more important: `--border-default`
  is defined at `tokens.css:18` with dark overrides at `:76` and `:95` and `DensityControl.tsx:71`
  already uses it bare; and **the fallback was the wrong colour** — the token is `#E5E7EB`, the
  fallback said `#d1d5db`, so had the token ever failed to resolve this one dashed border would have
  rendered a different grey from every other border on the page.
- Verified locally before that push: hex ratchet `OK — 4226 literals in 260 files (baseline allows
  4353 in 263), no file grew, no new file carries one`; and the PR body's own web suite,
  `pnpm --filter @project-ops/web test -- ratesListsHelpers` → 1 file, **137 tests passing**.
- After the label came off, the stale check runs from the labelled state re-ran and cleared — the
  same staleness that held #1920 and #1971 up. Final state green, no label.
- `Assert-SmokedOrEscalate -PR 1979` then `Merge-Pr -PR 1979`. No hand merge, and the receipt gate
  added after #1972 required this file on the head before the merge could run.

**Recorded, and not closed by this merge:** #1979 had a twin. One arming-log entry at 00:34:23Z
produced **two** PRs seven minutes apart on different branches over the identical five files —
#1978 (`+997 −94`, verdict **REJECT-AND-REDO**) and this one (`+843 −275`). #1978 was closed
2026-09-16T03:39Z with its branch left intact. This is the **second** such pair in two days
(#1959/#1960 was the first, also closed). Two occurrences from single arms is a pattern, and it is
**ESCALATED to Station 03** to measure from the watcher's own run tracking before a third appears.

Receipt written 2026-09-16T04:09:34Z.
