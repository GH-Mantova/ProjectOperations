---
pr: 1980
approved_by: marco
approved_at: 2026-09-16T05:47:50Z
---

# PR 1980 — approval receipt

**Merged by:** Station 00, supervised interactive lane `station-00.interactive-0003` (DOCTRINE
§10.2.1). **Marco released it in chat, 2026-09-16: "i removed the labels from 1980 & 1981"** —
`escalates: true`, so removing `do-not-merge` is the release and he removed it himself.

**What it is:** Scope Cards **S2a** — every estimating line carries one `QuoteDestination`
(PRICE / PROVISIONAL / OPTION / INTERNAL), replacing three legacy switches. 23 files: two
migrations (additive DDL, then an UPDATE-only idempotent backfill), the schema, the redesign
service, DTOs, controllers, the estimate export and Excel builder, and six spec files. Prompt
`pr-scopecards-s2a-quote-destination-api`, armed by this lane 2026-09-16T03:28:19Z.

**Grounds for merge at `8a42e0e5`:**

- **The prompt's own `done_when` gates were failing, and this lane fixed them rather than waiving
  them.** `grep -c 'Rule A'` and `grep -c 'isProvisional'` are both required to read 0 and read
  **3 and 1**. Every occurrence was in a comment *describing* the removal — lines 28, 938–939, 984
  — so the code was already clean, and the writer flagged it honestly instead of forcing an edit.
  Reworded in `f849b5a1`: *"Rule A"* → *"covered-item zeroing"*, *"isProvisional"* → *"its boolean
  flag"*. No fact was deleted. The edit script asserted no non-comment line changed before writing.
- **All six `done_when` gates measured green afterwards:** `SCOPE_QUOTE_DESTINATION_V1` = 10,
  `enum QuoteDestination` = 1, `optionsTotal` = 4, `Rule A` = 0, `isProvisional` = 0, and
  `build-relationship-map.mjs --check` → OK, 296 models / 70 enums / 493 edges, exit 0.
- **Two suites were failing to COMPILE, with zero failed assertions**, and both are fixed in
  `5d489baa`: `quote-html.builder.spec.ts` (three TS errors from the new required
  `quoteDestination` and four new bucket fields) and `quote-destination-backfill.spec.ts`
  (`ScopeCardUncheckedCreateInput` requires `name`, unset on two `scopeCard.create` calls).
  The first was fixed the way `make-summary.ts` asks for in its own comment — *"Override a bucket
  through this function, never with a literal"* — which also makes that spec immune to the next
  field the type gains. It had broken this same way three times (#1443, #1471, S2a).
- Verified locally before pushing: `npx tsc --noEmit` on `@project-ops/api` went **3 errors → 0**,
  and `quote-html.builder` passes 22 tests. The backfill spec needs a database, so CI ran that one.
- Station review verdict **MERGE**. No production behaviour was changed by either of this lane's
  commits — comments and test files only.
- `Assert-SmokedOrEscalate -PR 1980` then `Merge-Pr -PR 1980`. No hand merge; the receipt gate
  required this file on the head first.

**The writer's second escalation, assessed and accepted:** `waste-section.test.tsx` was changed
though `.tsx` is outside S2a's scope — **+5 −1, one file**. It was grep-locking the exact pre-S2a
inline arithmetic, so leaving it would have failed the suite. Moving a test that asserts the old
shape is part of the refactor, not scope creep.

**Finding raised to 06, not blocking:** `test -f docs/data-model/relationship-map.json` appears in
this `done_when` and in **ten** queued prompts. That file is gitignored (`.gitignore:135`), so the
gate depends on whether the generator has been run in that tree rather than on the work.
`build-relationship-map.mjs --check` alone proves the same thing and is what CI runs.

Receipt written 2026-09-16T05:47:50Z.
