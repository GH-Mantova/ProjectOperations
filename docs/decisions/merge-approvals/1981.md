---
pr: 1981
approved_by: marco
approved_at: 2026-09-16T05:48:07Z
---

# PR 1981 — approval receipt

**Merged by:** Station 00, supervised interactive lane `station-00.interactive-0003` (DOCTRINE
§10.2.1). **Marco released it in chat, 2026-09-16: "i removed the labels from 1980 & 1981"** —
`escalates: true`, so removing `do-not-merge` is the release and he removed it himself.

**What it is:** CRM visual parity **S3** — Account 360 rebuilt on the s7 kit, activity feed left,
facts rail right (`CRM_PARITY_ACCOUNT360_V1`). Three files:

| file | diff |
| --- | --- |
| `apps/web/src/pages/crm/AccountDetailPage.tsx` | +584 −641 |
| `apps/web/src/pages/crm/__tests__/crmvis-s3-account-360.test.ts` | +159 −0 |
| `apps/web/src/pages/crm/crm.css` | +42 −0 |

Prompt `pr-crmvis-s3-account-360`, armed by this lane 2026-09-16T04:39:33Z, third in the `crmvis`
chain behind S1 (#1956) and S2 (#1971). It builds against Marco's own mock-up
(`design_ref` artifact `3372e3ff`), which is what makes it a visual-parity slice rather than a
feature.

**Grounds for merge at `a47e008d`:**

- **Station review verdict: MERGE**, written to `docs/pr-reviews/pr-1981-review.md` and mirrored to
  the PR at 04:56:19Z. No blocking findings — unlike its two immediate predecessors, this one
  needed no hex-ratchet cure from this lane.
- The page **shrinks**: −641 lines removed against +584 added on `AccountDetailPage.tsx`. A parity
  rebuild that adds less than it deletes is doing what it claims — replacing bespoke markup with
  the shared kit rather than layering over it.
- New spec `crmvis-s3-account-360.test.ts` pins the things that regress silently: hex-free source,
  `pickNextAction`, `formatRelativeAge`, and the header labels. The existing S1 and S5 pin suites
  were left untouched, so the older parity assertions still hold.
- CI green on the head after the label came off; the stale check runs from the labelled state
  re-ran and cleared, which is the same staleness that held #1920, #1971 and #1979 up.
- `Assert-SmokedOrEscalate -PR 1981` then `Merge-Pr -PR 1981`. No hand merge; the receipt gate
  required this file on the head first.

**What it unblocks:** `CRM_PARITY_ACCOUNT360_V1` on `main` releases `crmvis-s4-register`, which is
staged and parked in the queue. S5 → S8 sit behind that, each gated on the one before.

Receipt written 2026-09-16T05:48:07Z.
