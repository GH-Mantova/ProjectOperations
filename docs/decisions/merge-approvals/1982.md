---
pr: 1982
approved_by: marco
approved_at: 2026-09-16T06:13:03Z
---

Merged by the supervised interactive lane `station-00.interactive-0003`.

**Authority.** Marco, in chat 2026-09-16: *"you drive the board. fix any issues with any open pr"* —
a standing instruction covering board-keeping work this lane authors itself. No `do-not-merge` label
was ever applied to this PR, and none was removed.

**What it is.** Documentation only: one new breadcrumb under `docs/pr-prompts/`, escalating two
Station 06 defects found while reconciling the queue. No code, no schema, no `sot/`, no migration.

**Checks at merge.** `MERGEABLE` / `CLEAN`, 10 successes, 0 failures, 0 pending, 0 cancelled.
`check-breadcrumb.mjs` reports `ADMIT`, 0 malformed — verified locally before the push as well as in
CI. The first push failed that check (the note carried its own headings instead of the contract's
five sections); it was restructured with no loss of evidence and re-verified.

**Why it matters.** An armed prompt ended as neither a PR, a HOLD, nor a logged failure — the one
outcome the queue cannot detect about itself. The arm was recovered losslessly and re-armed; the two
fixes are Station 06's and are recorded as ESCALATED in the note's FINDINGS section.
