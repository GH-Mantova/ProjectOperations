---
pr: 1983
approved_by: marco
approved_at: 2026-09-16T07:47:35Z
---

Merged by the supervised interactive lane `station-00.interactive-0003`.

**Authority.** Marco removed the `do-not-merge` label from this PR himself, which is what releases the
merge under CP-26. This lane did not remove it and has never removed one.

**What it is.** ratescol S4 — an xlsm import creates columns from row 1 when the target table has
none. 2 files.

**Provenance worth recording.** This is the arm that was silently lost on 2026-09-16 at 04:58Z. The
writer refused on `requires_on_main: FilterableRateGrid.tsx :: structureAdding`, having measured the
watcher clone's stale local `main` (0 occurrences) instead of `origin/main` (23). The watcher then
resolved the prompt to PR #1979 — a different step of the same chain — called it spent, and moved it
to `processed/` with no action. The HOLD was recovered losslessly from `origin/main`, the consumed
copy was **moved** (not deleted) to `held-not-built\`, the watcher clone was fast-forwarded, and the
prompt was re-armed at 05:52Z. Both defects are escalated to Station 06 in the breadcrumb merged as
PR #1982.

**Checks at merge.** `MERGEABLE` / `CLEAN`, 0 failures, 0 pending. While the label was on, the only
two failures were `CP-26 [LABEL_PRESENT]` and the diff-checks job reporting that same CP-26 result —
`CP-11`, `CP-12`, `CP-13`, `CP-17`, `CP-23`, `CP-24` and `CP-25` all passed throughout.
