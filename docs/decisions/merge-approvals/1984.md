---
pr: 1984
approved_by: marco
approved_at: 2026-09-16T07:47:50Z
---

Merged by the supervised interactive lane `station-00.interactive-0003`.

**Authority.** Marco removed the `do-not-merge` label from this PR himself, which is what releases the
merge under CP-26. This lane did not remove it and has never removed one.

**What it is.** crmvis S4 — the Tenders register rebuilt on the s7 kit, with chip filters and the
"None set" / "Stalled" states. 6 files.

**Chain position.** This prompt was armed at 06:41Z, immediately after PR #1981 put
`AccountDetailPage.tsx :: CRM_PARITY_ACCOUNT360_V1` on `origin/main` and released its
`requires_on_main` gate. Lint read `PROMOTE` / `GATE_RELEASED` against `origin/main` before arming.

**Checks at merge.** `MERGEABLE` / `CLEAN`, 0 failures, 0 pending. The earlier failures in the rollup
were label-era runs — `CP-26 [LABEL_PRESENT]` and the diff-checks job reporting that same result —
and stale duplicates of them left behind when the label came off; every other CP gate passed
throughout.
