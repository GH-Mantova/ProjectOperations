---
pr: 1985
approved_by: marco
approved_at: 2026-09-16T09:36:59Z
---

Merged by the supervised interactive lane `station-00.interactive-0003`.

**Authority.** Marco, in chat 2026-09-16, approved the spec for this slice before it was staged
("the chips look alright") and settled the one open question in it ("Estimator is the owner"),
under his standing instruction to drive the board. No `do-not-merge` label was applied to this
PR and none was removed.

**What it is.** One new prompt file under `docs/pr-prompts/`. No code, no schema, no `sot/`, no
migration. Merging it only makes the prompt discoverable by the watcher; the work happens when
it is armed, and the PR that produces will carry `escalates: true` for Marco to judge.

**Provenance worth recording.** The spec was written and agreed before staging, per Marco's
standing rule that a spec comes first. Writing it changed the scope twice, both times by
measurement rather than assumption:

- I had reported three missing filter chips. Recon found only two. The third, Owner, was
  already built as Estimator behind a comment that deliberately argued they were different
  things. That was a business question, not a build task, so it went to Marco — who overruled
  the comment. The prompt now requires that comment be rewritten rather than silently dropped.
- I had implied the chips might need API work. `valueMin`/`valueMax` turned out to be already
  present in `FiltersForQuery` and in `tender-query.dto.ts` with no control attached, and
  Logged by can filter client-side over rows `fetchAllPages` already holds. The prompt
  therefore forbids adding an API parameter, a DTO field or a migration.

**Checks at merge.** `MERGEABLE` / `CLEAN`, 10 successes, 0 failures, 0 pending, 0 cancelled.
Lint on the draft before staging: `ADMIT (size 4)`.
