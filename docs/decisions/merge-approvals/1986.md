---
pr: 1986
approved_by: marco
approved_at: 2026-09-17T01:02:35Z
---

Merged by the supervised interactive lane `station-00.interactive-0003`.

**Authority.** The `do-not-merge` label was applied by the watcher at 2026-09-16T10:06:27Z
(`escalates: true`) and removed by Marco at 19:01:06Z. Under his standing decision of 2026-09-16 -
*"label off means merge"* - that removal is the release. This lane did not remove it and has never
removed one.

**What it is.** The residual slice of the crmvis S4 register work, from a spec Marco approved before
it was staged:

- The two Next action cell defects found in the post-merge visual review of PR #1984 - the chip
  rendering inline ahead of the text in the populated branch instead of stacked under it, and the
  `Stalled` badge carrying `display: block`, which overrode `.s7-badge`'s `inline-flex` and stretched
  it across the whole cell. The second was visible only rendered; it read as correct in the source.
- The **Value** chip, built on `valueMin` / `valueMax`, which already existed in `FiltersForQuery`
  and in `tender-query.dto.ts` with no control attached. No API work.
- The **Logged by** chip, filtered client-side over rows `fetchAllPages` already holds, the way
  `Mine only` does. No API parameter was added. Its known limit - the option list can only name
  people in the loaded rows - was written into the prompt rather than left to be discovered.
- **Owner**: a relabel of the existing Estimator chip. The file carried a comment arguing the two
  were different things; Marco settled it in chat on 2026-09-16 - *"Estimator is the owner"* - and
  the prompt required that comment be rewritten to record the decision rather than silently dropped.
  The field stays `estimatorId`; no other screen was touched.

**Pinned throughout.** No API parameter, no DTO change, no migration. `classifyNextAction` and the
overdue classification the sidebar Tenders badge shares were out of `scope` and untouched, which is
also why the chip wording stays `Overdue` / `Due soon` rather than the artboard's "3 days over".

**Checks at merge.** `MERGEABLE` / `CLEAN`, 0 failures, 0 pending. The earlier failures in the
rollup were label-era runs - `CP-26 [LABEL_PRESENT]` and the diff-checks job reporting that same
result - plus stale duplicates left behind when the label came off; every other CP gate passed
throughout.
