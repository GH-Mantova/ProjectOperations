---
pr: 1988
approved_by: marco
approved_at: 2026-09-17T00:53:04Z
---

Merged by the supervised interactive lane `station-00.interactive-0003`.

**Authority.** Marco, in chat 2026-09-16, chose to split the twice-truncated S2b slice, approved the
split spec before anything was staged, and said go. No `do-not-merge` label was applied to this PR
and none was removed.

**What it is.** Three new prompt files and one rename. No code, no schema, no `sot/`, no migration.

**The property worth recording.** Only S2b-c sets `SCOPE_QUOTE_DESTINATION_UI_V1`. Every prompt in
the `scopecards` chain gates on a marker string rather than on `cluster_order`, and S3 waits for
exactly that marker in `ScopeQuantitiesTable.tsx`. Had S2b-a or S2b-b set it, S3 would have armed
against a half-built screen. Because it is set last, **S3's prompt needed no edit**, and neither did
anything from S4 onward.

Lint at staging, which is the chain proving itself:

```
S2b-a   ADMIT (size 5)
S2b-b   REJECT [GATE_NOT_RELEASED] - parked on SCOPE_QD_UI_ITEMS_V1
S2b-c   REJECT [GATE_NOT_RELEASED] - parked on SCOPE_QD_UI_SECTIONS_V1
```

Those two REJECTs are the intended resting state.

**Why three and not the two the escalation proposed.** Counted against the original prompt's own
`scope` list, "control + items" against "everything else" is 6 files against 14 - and 14 is only two
fewer than the 20 that truncated twice. Three land at 6 / 6 / 10, and six of the last ten are tests
or pure arithmetic.

**The original is retired, not destroyed.** `pr-scopecards-s2b-quote-destination-ui-HOLD.md` moved
to `docs/pr-prompts/superseded/` - moved, never deleted, per Marco's standing rule.

**Checks at merge.** `MERGEABLE` / `CLEAN`, 10 successes, 0 failures, 0 pending, 0 cancelled.
