---
pr: 1989
approved_by: marco
approved_at: 2026-09-17T03:44:05Z
---

Merged by the supervised interactive lane `station-00.interactive-0003`.

**Authority.** Marco, in chat 2026-09-16: *"you drive the board. fix any issues with any open pr"*,
and again on 2026-09-17 when asked directly whether to take this PR. No `do-not-merge` label was
applied to this PR and none was removed.

**What it is.** One new prompt file — `pr-scopecards-s6-one-cutting-surface-HOLD.md`. No code, no
schema, no `sot/`, no migration. Merging it only makes the prompt discoverable by the watcher; the
work happens when it is armed, behind its own gate.

**Relationship to the S2b split merged as PR #1988.** None that blocks either. S6 sits further down
the `scopecards` chain and gates on its own predecessor's marker, so it is parked until that lands.
It does not gate on `SCOPE_QUOTE_DESTINATION_UI_V1` and is unaffected by the three-way split of S2b.

**Checks at merge.** `MERGEABLE` / `CLEAN`, 10 successes, 0 failures, 0 pending, 0 cancelled.
