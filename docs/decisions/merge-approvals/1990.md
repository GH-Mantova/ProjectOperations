---
pr: 1990
approved_by: marco
approved_at: 2026-09-17T05:21:33Z
---

Merged by the supervised interactive lane `station-00.interactive-0003`.

**Authority.** Marco, in chat 2026-09-17: *"keep driving the board"*, following his approval of the
S2b split spec on 2026-09-16. `escalates: false` on this slice was a decision in that spec, which he
read and did not object to. No `do-not-merge` label was applied and none was removed.

**What it is.** The first of the three slices the oversized S2b was split into (PR #1988): the
`QuoteDestinationSelect` control, and the destination column on the WBS item rows.

**The split worked, measured.** The 20-file S2b truncated two `01-code-writer` runs before commit —
24 min / 144 tool uses, then 18 min / 139. This six-file slice ran **04:02:55Z → 04:32:47Z, one
turn, PR opened**. Same runtime, same standing-authority language; the only variable changed was
size.

**Verified by hand before merging, beyond CI:**

| check | result |
|---|---|
| files changed | exactly the six in `scope` |
| `SCOPE_QD_UI_ITEMS_V1` set | yes |
| `SCOPE_QUOTE_DESTINATION_UI_V1` **not** set | confirmed absent — S3's gate stays shut until S2b-c |
| `CoveredGroupCells` / `COVERED_ITEM_TOTAL` deleted | both 0 occurrences |
| drawer renamed *Rejected proposals* | present |
| `optionLetter=` passed anywhere | 0 — the prop is omitted, not stubbed with `undefined` |
| *quoted on SUB* chip | present |
| other slices' files touched | none of the eight |

That `optionLetter` row is the one worth recording: passing a literal `undefined` is what the first
truncated run left behind, and all three split prompts name it as a bug rather than prior art. This
slice omitted the prop instead, which is what was asked.

The removal of the covered-item placeholder cells is recorded in a comment at the site rather than
silently deleted.

**Checks at merge.** `MERGEABLE` / `CLEAN`, 15 successes, 0 failures, 0 pending, 0 cancelled.

**On visual acceptance.** This slice renders a deliberately half-built screen — the other three
sections have no destination column until S2b-b, and no card or discipline figure moves until
S2b-c. A side-by-side against the mock-up is worth doing **once, at S2b-c**, when the screen is
whole. S2b-c carries `escalates: true` for exactly that reason.
