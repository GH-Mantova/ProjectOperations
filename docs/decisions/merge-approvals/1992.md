---
pr: 1992
approved_by: marco
approved_at: 2026-09-17T06:44:15Z
---

Receipt written by the supervised interactive lane `station-00.interactive-0003`.

**Authority.** Marco specified this standard with this lane on 2026-09-17, settled its three open
questions, then updated this branch and enabled **auto-merge (squash)** himself at 06:43:14Z. This
lane did not merge it and did not enable auto-merge. No `do-not-merge` label was applied and none
was removed.

**Why this receipt was written before the merge rather than after.** The lane's standing rule is a
receipt naming itself for every merge it is part of. Auto-merge fires on its own the moment checks
go green, so a receipt written afterwards would arrive after the branch was gone — which is exactly
how PR #1972 merged unreceipted on 2026-09-15. Writing it onto the head first is the only way the
rule survives contact with auto-merge.

**What it is.** Two new prompt files. No code, no schema, no `sot/`, no migration, no folder moved.

- `pr-queue-layout-s1-the-standard-HOLD.md` — writes the standard into four documents.
- `pr-queue-layout-sot-entry-HOLD.md` — the `sot/` entry, **scoped to Station 05**, gated on
  `QUEUE_LAYOUT_V1`.

**Ownership, recorded deliberately.** `sot/` belongs to Station 05 alone. Station 00 specified the
wording with Marco and **authored a prompt for 05 to execute**; it did not edit `sot/`, here or
anywhere else today.

**The two recon findings the standard rests on**, both measured rather than assumed:

- `index.mjs` calls `fsWatch(PROMPT_DIR, { persistent: true }, …)` with **no** `recursive: true`.
  On Windows a change inside a subdirectory fires no event, so `hold` and `armed` must remain
  filename states in the queue root. Moving them into folders would turn arming from immediate into
  eventual, silently.
- The retired `processed/` moved a prompt when its PR **opened**, not when it merged — so it held
  open, abandoned and merged prompts with nothing telling them apart. The new `merged/` requires a
  confirmed `MERGED`. This is the blind spot that let `ratescol-s4` and `scopecards-s2b` be binned
  as spent on 2026-09-16 while nothing had shipped.

**Lint at staging.** S1 `ADMIT (size 4)`; the sot entry `GATE_NOT_RELEASED`, correctly parked on
`QUEUE_LAYOUT_V1` so it cannot land before the document it points at.
