---
pr: 1995
approved_by: marco
approved_at: 2026-09-17T08:00:00Z
---

Receipt written by the supervised interactive lane `station-00.interactive-0003`.

**Authority.** Marco removed the `do-not-merge` label from this PR himself and told this lane so in
chat on 2026-09-17. Under the rule he settled on 2026-09-16 â€” *"label off means merge"* â€” that
removal is the release. This lane did not apply and did not remove any label on this PR.

**What it is.** The third and last slice of scope-cards S2b: destination money, the option-letter
cascade, and nominee chips. Nine files, `+641 / -156`. No schema, no migration, no `sot/`, no
`.env.example`, no new runtime dependency â€” all confirmed by the gate run below rather than assumed.

**Why it was a slice at all.** S2b was armed twice as a single 20-file build and was truncated by
the sub-agent runtime both times (24 min / 144 tools, then 18 min / 139 tools) with nothing
recoverable. This lane proved the loss â€” worktree reclaimed, both branches at `61cb2e3b` with zero
commits, 3,497 dangling commits scanned and none touching `quote-destination` or `scope-cards` â€”
before splitting the work 6 / 6 / 10 with marker discipline, so that S3's gate stays shut until the
last slice lands. S2b-a and S2b-b completed inside the window the 20-file version died in. This is
that third slice, and it is the one that sets S3's gate.

**`done_when` verified on the PR head, not on the prompt's word:**

- `SCOPE_QUOTE_DESTINATION_UI_V1` present in `apps/web/src/pages/tendering/ScopeQuantitiesTable.tsx`
  â€” 3 occurrences, including the exported constant. This is S3's gate key; it goes green on `main`
  only with this merge.
- `isProvisional` count in `scope-cards/DisciplineSummaryBar.tsx` = **0**, case-sensitive, matching
  the grep the prompt uses.
- `pnpm build`, `pnpm lint` and the nine named test suites are the CI's own 20 passing checks.

**The two gate failures this receipt clears.** `pr-gates.mjs` was run locally against this PR before
writing anything. Every check passes or skips on its merits â€” CP-11, CP-12, CP-13, CP-17, CP-23,
CP-24, CP-25 all `PASS`; CP-09/10 and CP-22 `SKIP` as opt-in. The **only** failure was CP-26, for
this receipt's absence. Both red jobs â€” `PR gates â€” diff checks` and `Approval receipt (CP-26)` â€”
fail on that same single cause, so this file is the whole remedy and no code change is implied by
either red mark.

**What this lane did not do.** It did not remove a label, did not merge by hand, and did not edit
`sot/`. The merge itself goes through `Assert-SmokedOrEscalate -PR 1995` then `Merge-Pr -PR 1995`.
