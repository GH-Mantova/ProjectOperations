---
pr: 1997
approved_by: station-00.interactive-0003
approved_at: 2026-09-17T08:45:00Z
---

Receipt written by the supervised interactive lane `station-00.interactive-0003`.

**What it is.** One new breadcrumb file under `docs/pr-prompts/`, authored by the **scheduled**
Station 00 run as an addendum to its 08:09Z run. No code, no schema, no `sot/`, no migration.
Green on all 15 checks with no `do-not-merge` label at any point, so CP-26 does not apply to it; this
receipt exists because this lane leaves one for every merge it performs, not because a gate demanded
it.

**Why this lane merged another lane's PR.** Marco's standing instruction to this lane on 2026-09-17
is to keep driving the board and merge what is green. The PR had been `CLEAN` with zero pending
checks and no auto-merge set for roughly ten minutes, i.e. its authoring run had finished without
landing it. Leaving a finished, green board PR open is how breadcrumbs go stranded â€” which is the
exact failure that PR #1993 and #1996 were opened to clean up.

**The addendum's findings were read before merging, not merged unread.** It concerns this lane
directly and it is accurate on every point this lane can check:

- Its commit-identity reading of `#1995` is correct: `43d95f23` (08:26:58Z) is this lane's
  merge-approval receipt and `ee8a288e` (08:27:09Z) is this lane's branch update.
- **A2** defers `#1995` to this lane rather than acting on it. Correct, and the deference is
  reciprocated here: that PR was released by Marco in chat, and this lane is driving it.
- **A4** is right that `station-00.interactive-0003 <marco@initialservices.net>` matches no row in
  `DOCTRINE.md` Â§10.2.1, and right that attribution by email alone would misread it as a watcher
  build. The row it drafts is sound. It is left for a run with a free lane, as that addendum says.

**A3 is actioned, not deferred.** The addendum measured that `status-sweep.ps1` was listing this
lane's live worktree `C:/PR-Master/worktrees/sup-0003-stage-queue-layout-standard` under *"orphaned
worktree â€” investigate/prune"*, and warned that a `--force` prune against it would tear the tree out
from under a live lane. That worktree's work landed in `#1992`; it was finished, not abandoned. This
lane therefore **retired it itself**, using a content proof rather than an is-ancestor test (the repo
squash-merges, so branch SHAs never become ancestors of `main`): both contributed paths confirmed
present on `origin/main`, then `git worktree remove`. `pr1823` cleared the same proof (6/6) and was
removed with it.

Two worktrees remain and both were deliberately kept: `po-vg` is dirty, carrying the untracked
`scripts/pipeline/check-pipeline-heartbeat.mjs` that the scheduled run measured, and
`sup-0838-addendum` is this PR's own live tree. **The collision A3 warned about no longer has a
subject**, and Station 03's prune dispatch is correspondingly narrowed.
