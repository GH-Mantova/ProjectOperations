---
pr: 1998
approved_by: marco
approved_at: 2026-09-17T09:50:00Z
---

Receipt written by the supervised interactive lane `station-00.interactive-0003`.

**Authority.** Marco removed the `do-not-merge` label from this PR himself and told this lane so in
chat on 2026-09-17. Under the rule he settled on 2026-09-16 â€” *"label off means merge"* â€” that
removal is the release, and on this PR it is also the visual sign-off the reviewer asked for. This
lane did not apply and did not remove any label here.

**What it is.** CRM S5 â€” the Follow-ups tab rebuilt to the artboard. Three files, `+485 / -159`:
`TendersRegisterPage.tsx`, `crm.css`, and one new test file. No schema, no migration, no `sot/`, no
`.env.example`, no new runtime dependency.

**`done_when` verified on the PR head, case-sensitively:**

- `CRM_PARITY_FOLLOWUPS_V1` present in `TendersRegisterPage.tsx` â€” 9 occurrences, including the
  exported constant `= "crmvis-s5"`.
- Hex literals in `TendersRegisterPage.tsx`: **0**. In `crm.css`: **0**. Design tokens only, which is
  what the prompt's two negated greps demand.

**The four defects its `premise_means` named, checked against the built head rather than the PR
description:**

- The Follow-ups tab now maps the `tender` column to **"What"** while the column model keeps
  "Tender" for the Register â€” the artboard's name, applied per tab rather than globally.
- **KPI colour stripes exist in both files** â€” a `stripeVariant` prop typed
  `danger | warning | neutral | primary`, and a real `crm-kpi--stripe-*` rule in `crm.css` giving the
  3px left border with matching value ink.
- The two labelled toggle rows are merged into one `SHOW` row with filled-when-on pills.
- The five `<label>` elements found in the file are **inside the "Log interaction" modal**, not the
  filter row â€” that block was opened and read rather than inferred from a match count. The artboard's
  "no visible field labels on filters" still holds.

**Artboard parity, measured rather than asserted.** The reviewer conditioned its MERGE verdict on a
visual check against `Claude Design\proposed\crm-visual-parity\FollowUps.dc.html`. That file's visible
strings were extracted and tested against the built page: **every** nav item, tab, KPI name, toggle,
column header and empty state is present â€” `Follow-ups`, `Show`, `Overdue`, `Due soon`,
`No next action`, `On track`, `Submitted tenders`, `Opportunities`, `Leads`, `Won & lost`,
`Mine only`, `Due this week`, `Never logged`, `Value at risk`, `What`, `Type`, `Client`, `Value`,
`Last interaction`, `Logged by`, `Next action`, `None set`, `Stalled`, `Export CSV`,
`Save this view`. The only artboard strings absent are its sample data (tender names, client names,
dollar figures) and its annotation prose. **This is a string-level parity proof, not a rendered
comparison** â€” stated plainly because the distinction is what went wrong on #1984, and because the
rendered check is Marco's, which he has now made.

**CI's own gate output is the authority here, not a local run.** `pr-gates.mjs` resolves its file list
from `git diff $(merge-base origin/main HEAD) HEAD`, so running it from a dev tree parked on `main`
evaluates an empty diff and reports vacuous passes. Before the label came off, CI's run of it showed
every diff gate passing or skipping on its merits with **CP-26 as the sole failure**, for the label.
With the label gone, this receipt is what clears it.

**Untouched, per the prompt's Do NOTs:** no `sot/`, no schema, no quote code, and the Register tab's
own column naming is unchanged.
