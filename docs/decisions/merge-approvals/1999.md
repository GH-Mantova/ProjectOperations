---
pr: 1999
approved_by: marco
approved_at: 2026-09-17T09:25:00Z
---

Receipt written by the supervised interactive lane `station-00.interactive-0003`.

**Authority.** Marco commissioned this standard with this lane on 2026-09-17 and settled its three
open questions himself: drafts come into the repo, exceptions nest under `exceptions/<reason>/`, and
the historical queue is migrated rather than line-drawn. His standing instruction to this lane is to
keep driving the board and merge what is green. This prompt is `escalates: false`, it carries no
`do-not-merge` label, and no label was applied or removed by anyone.

**What it is.** Four documents, `+187 / -13`. `QUEUE-LAYOUT.md` is new; `DOCTRINE.md`, `PR-MASTER.md`
and a new `docs/pr-prompts/README.md` point at it. **No prompt file is moved and no folder is
created** â€” writing the standard and migrating to it are deliberately different slices.

**`done_when` verified on the PR head:** `QUEUE_LAYOUT_V1` present in `QUEUE-LAYOUT.md`;
`QUEUE-LAYOUT.md` referenced 3Ã— in `DOCTRINE.md` and 1Ã— in `PR-MASTER.md`; `lint-station.mjs` exits 0
locally (`ADMIT: all 8 docs clean`). Exactly the four scoped files, confirmed against the PR's own
file list.

**The mechanical claim was re-measured, not trusted.** The document rests on `hold` and `armed`
staying filenames rather than folders, and cites `scripts/pr-watcher/index.mjs:3545`. Read back in
both trees just now, that line is:

```
const watcher = fsWatch(PROMPT_DIR, { persistent: true }, (event, name) => {
```

No `recursive: true`, and the line number is exact in the dev tree and the watcher clone alike. The
reasoning holds: making `armed` a folder would silently demote arming from event-driven to
eventual â€” at best five minutes late, at worst missed.

**One correction, recorded here rather than swallowed.** The *"merged is not processed"* section says
of 2026-09-16 that `ratescol-s4` and `scopecards-s2b` were filed as processed when their PRs opened
and that **"Both PRs were later confirmed merged"**. That is not what happened, and the softened
version weakens the very case the section is making.

What this lane measured at the time: both prompts were matched to a **predecessor's** PR by slug
prefix and binned as *spent* â€” no PR of their own was ever opened, and **the work was never done**.
S2b's loss was then proved rather than assumed: the worktree was reclaimed, both branches sat at
`61cb2e3b` with zero commits, and 3,497 dangling commits were scanned with none from that day
touching `quote-destination` or `scope-cards`. The work had to be rebuilt from scratch, split 6/6/10,
and only finished today with PR #1995.

The section's conclusion is right and its evidence is stronger than it claims: `processed/` did not
merely fail to distinguish three outcomes, it silently absorbed two arms that produced nothing. This
is merged as written because the standard itself is correct, the sentence is one clause inside a
document that is not yet enforced, and **S2 already has to touch these files** to introduce the
shared constant. The correction belongs in that slice, and it is written out here so the next run
does not have to rediscover it.

**Related, and left alone deliberately.** The promptâ†’PR mis-resolution behind that incident is open
as an escalation to Station 06 in PR #1982 (F2, spent-detection). This receipt does not re-file it.
