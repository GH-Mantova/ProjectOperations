---
pr: 2001
approved_by: station-00.interactive-0003
approved_at: 2026-09-17T09:35:00Z
---

Receipt written by the supervised interactive lane `station-00.interactive-0003`.

**What it is.** Two files from the scheduled Station 00 run: three appended rows in
`docs/pr-prompts/.arming-log.txt` and one breadcrumb. Insertions only on the log â€” a `3 0` numstat.
Green on all 10 checks, no `do-not-merge` label at any point, no auto-merge set. Merged by this lane
under Marco's standing instruction to keep driving the board and merge what is green, after its
authoring run had finished without landing it.

**This PR is a correction to THIS lane, and it is accepted as one.** Its F1 is right, and the rows it
publishes are this lane's own:

```
08:48:33Z  ARMED  pr-crmvis-s5-followups              escalates=true   actor=station-00.interactive-0003
09:12:13Z  ARMED  pr-queue-layout-s1-the-standard     escalates=false  actor=station-00.interactive-0003
09:30:54Z  ARMED  pr-transport-capacity-column-order  escalates=true   actor=station-00.interactive-0003
```

`arm-prompt.ps1` appends to `.arming-log.txt`, which is **tracked**, and then deliberately releases
its staged rename from the index (`ARM_INDEX_RELEASED`) â€” so the row is written to disk and nothing
commits it. `DOCTRINE.md` Â§9.5 already names this and is explicit about whose job it is: *any run
that arms something must commit the arming log in its board PR.* This lane armed three times in
43 minutes and published none of them. For roughly forty minutes `origin/main` would have answered
any clone, CI job or cloud-fired station that the last arm was **06:44:53Z** â€” a well-formed answer,
two arms and two and a half hours stale, which Â§9.5 calls the more dangerous shape precisely because
it answers.

**Adopted going forward:** this lane commits its arming-log row in its own board PR rather than
leaving it for a scheduled run to find. The defect that nothing commits the file by design is
unchanged, and this PR does not claim otherwise â€” it is one more gap closed by hand.

**Why the rest of it is sound, checked rather than waved through.** The publish was gated on a
strict-superset assertion comparing Buffers, with a byte-equality read-back after writing, and the
script exits non-zero and writes nothing if either fails. That is the correct direction for an
append-only file: Â§9.5's recorded hazard is a `git show HEAD:<path>` pipe *removing* a row while every
read-back still passes, and this operation cannot remove one.

**Its F2 is also correct and deliberately left open.** Two consumed `-HOLD.md` prompts â€”
`pr-crmvis-s5-followups` and `pr-queue-layout-s1-the-standard` â€” are deleted on disk and still tracked
on `main`, the normal shape after the watcher consumes a prompt, since arming is a `git mv` to a
gitignored `-ready.md`. Retiring them is a decision rather than bookkeeping, and this lane is not
making it inside a merge receipt either. It is the next single-actor run's, alongside the seven SPENT
retirements that run already has recorded.

**Its reading of this lane is accurate on every point checkable from here**, including that
`#1998` was not touched by it and that `pr-transport-capacity-column-order` was armed from the same
`ADMIT` triage it had listed.
