---
pr: 2002
approved_by: marco
approved_at: 2026-09-17T09:50:00Z
---

Receipt written by the supervised interactive lane `station-00.interactive-0003`.

**Authority.** Marco removed the `do-not-merge` label from this PR himself and told this lane so in
chat on 2026-09-17. Under the rule he settled on 2026-09-16 â€” *"label off means merge"* â€” that
removal is the release. This lane did not apply and did not remove any label here.

**What it is.** The `rt-tc` transport-capacity rate table reordered: Transport type becomes the outer
group, Material class the inner, and Capacity (mÂ³) shows before Capacity (tonnes). Two files,
`+62 / -0` â€” one migration and one canonical spec. No code path, no schema shape, no `sot/`.

**`done_when` satisfied:** `ls apps/api/prisma/migrations | grep -q "transport_capacity_column_order"`
matches `20260917193000_transport_capacity_column_order/`, and `pnpm build` is CI's own green job.

**CP-11 saw the migration and allowed it â€” read from CI's log, not a local run:**

```
ALLOWED - CP-11 migrations [apps/api/prisma/migrations/20260917193000_transport_capacity_column_order/migration.sql]
PASS    - CP-12, CP-13, CP-17, CP-23, CP-24, CP-25
SKIP    - CP-09/10 (opt-in), CP-22 (no Verification section)
FAIL    - CP-26 do-not-merge [PR carries the do-not-merge label (escalates:true)]
```

`ALLOWED` rather than `PASS` is the gate working as designed: the prompt's `gate_allow: migrations`
carried through to the PR body as a bare `GATE-ALLOW: migrations`, so CP-11 declared the migration
instead of failing it undeclared. CP-26 was the sole failure, and it was the label.

**The migration was read, not counted.** It is a single guarded `DO $$` block that rewrites
`sort_order` on four `rate_columns` rows. It is idempotent by construction â€” it guards on the current
values, so a re-run against already-correct rows updates zero rows â€” and it matches on
`(rate_table_id, name)` rather than literal column ids, consistent with the rest of this cluster. It
raises a named exception and aborts if it updates anything other than 0 or 4 rows, so a drifted
seeding migration cannot leave a half-applied reorder. No cell data is touched and no row identity
depends on column order, which is why the prompt's rollback is four values to put back.

**One behaviour worth recording, raised with Marco before the merge rather than found later.** The
`WHERE` clause matches only rows sitting in the *exact* current swapped state. A table that has
drifted to any other ordering updates zero rows â€” which is indistinguishable from "already at the
target". The abort guard catches a *partial* match, not a total miss, so the no-op case is silent.
Benign for `rt-tc` today, where the order is known; it is a fact about the guard, not a defect in it.

**Untouched, per the prompt's Do NOTs:** no cutting rate resolvers, no waste cost engine, no `sot/`,
no quote code.
