---
pr: 2005
approved_by: marco
approved_at: 2026-09-21T05:12:01Z
---

Released by Marco and merged by the Station 00 supervised interactive lane (DOCTRINE section 10.2.1;
actor `station-00.interactive-0004`; session
https://claude.ai/code/session_01M2BoeL5NEFRzZyQNeaFTn7).

**Authority.** Marco removed `do-not-merge` from this PR at 2026-09-21T05:12:01Z (timeline
`unlabeled` event, actor `GH-Mantova`), and said so in this chat in his own words: *"labels removed
from 2005 & 2017"*. The removal is the release (ruled 2026-09-17, recorded in receipt 2014.md); the
chat message identifies the actor, which the shared `GH-Mantova` account cannot.

**Lane.** Watcher-opened. Verdict in `processed/pr-scopecards-s3-line-markup-all-types-ready.md.log`:
`{"ok":false,"marco":true,...,"reason":"escalates:true — held for Marco, labelled do-not-merge"}`.
The same log carries its own `**PR #2005 opened**` line, so it is a genuine routing (section 10.1
scrape guard). RULE 2 bound until the release above. Review verdict (archived,
`C:\po-watcher\verdicts-archive\pr-2005-review.md`): MERGE.

**History.** Closed unmerged 2026-09-17T17:25:24Z by an unattributable actor. Reopened 2026-09-21 by
another chat on Marco's instruction ("reopen it") at the same head `d92fb4d2`. This lane then updated
the branch to main (`9042ed15`), and the watcher updated it twice more (`ad7286c7`, `4f74308c`).

**Migration.** `20260917122654_scopecards_s3_line_markup` is older by name than main's
`20260917193000_transport_capacity_column_order`. The lane read both: S3 adds two nullable
`markup_override DECIMAL(5,2)` columns to `scope_waste_items` and `cutting_sheet_items`, with no
default and no UPDATE; the transport migration is an idempotent `sort_order` data update on
rate-table columns. They touch disjoint tables, so out-of-name-order application is safe, and no
rename was made.

Merge path: Assert-SmokedOrEscalate -PR 2005 -MustContain 'SCOPE_LINE_MARKUP_ALL_TYPES_V1' then
Merge-Pr, on a head whose full check set is green.
