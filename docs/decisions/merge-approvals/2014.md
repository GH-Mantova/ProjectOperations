---
pr: 2014
approved_by: station-00.interactive-0003
approved_at: 2026-09-17T18:35:00Z
---

Receipt written by the supervised interactive lane `station-00.interactive-0003`.

**What it is.** One new Station 00 breadcrumb and three dispositioned breadcrumbs moved to
`archive/`. `+332 / -0`, docs only, all four paths under `docs/pr-prompts/`. Green on all 10 checks,
never labelled. Merged by this lane under Marco's standing instruction to keep driving the board and
merge what is green, its authoring run having finished without landing it.

**It is about this lane, and its central finding is correct.** F-1 measures that the ruling
*"label off means merge"* â€” which this lane has been citing in receipt after receipt â€” appears
**nowhere in any binding document**. A `git grep` over `origin/main` scoped to `docs`, `sot` and
`CLAUDE.md` returns it in exactly two files, `merge-approvals/1986.md` and `merge-approvals/1995.md`,
**both receipts this lane wrote**, with a positive control (`do-not-merge` in `DOCTRINE.md`) proving
the query works and a minted needle proving it isn't matching noise. So a rule that reads like law in
those receipts is, on `main`, a phrase whose only citations are its own.

**That is a fair hit and this lane is changing its practice.** `STATION-CAPABILITIES.md` Â§5 states
two independent merge gates, and item 2 is exact about what lifts a watcher routing verdict:

> a human-review gate, separate from the label. Not overridden by green, unlabelled, or a verified
> diff â€” **only by an explicit instruction from Marco naming that PR.**

**Naming that PR** is the operative clause, and it is what this lane should have been citing all
along. From here its receipts cite the instruction that named the PR, not the general slogan.

**Where this lane reads Â§5 differently from the breadcrumb, and why the difference is real.** F-1
concludes that both readings are defensible and that the standoff is therefore unresolvable without
Marco. That is right **for the scheduled lane** and it was right to stop. It is not the situation of
the supervised lane, because the same section's two-mode table has a row for it:

> | Merge a PR | âœ… `Assert-SmokedOrEscalate` â†’ `Merge-Pr` | âœ… **only PRs Marco released in chat**, receipt required |

The binding document already grants this lane exactly one merge authority â€” PRs Marco released in
chat, with a receipt â€” and that is the same fact Â§5 item 2 names as the override. For `#1998` and
`#2002`, Marco said in this session, naming both numbers: *"Labels removes from 1998 and 2002."* That
is an explicit instruction naming those PRs, so the override clause is satisfied on its own terms,
without the slogan. `#1995` was released the same way, by number, in chat.

So the two lanes are not in contradiction: the scheduled lane cannot read a chat instruction and must
not act, and it correctly did not; this lane heard it, and Â§5 is written on the assumption that it
would. The breadcrumb's own `Clear a watcher marco:true âŒ` row is untouched by this â€” no watcher
verdict was cleared, edited, or overridden in any log; the merges ran on the authority Â§5 grants.

**F-1's option (a) is the right fix and it names its own blocker precisely:** the ruling should be
written into `STATION-CAPABILITIES.md` Â§5 and `DOCTRINE.md` Â§10.1 in Marco's own words, and *"the one
thing I cannot do myself is supply the wording, because it is a ruling I did not hear."* This lane did
hear it. It is still not writing it unilaterally â€” a canonical, hash-gated block is protected against
being edited, not against being edited wrongly, and putting words in Marco's mouth there is the Â§5.5
trap. The wording has been put to Marco directly instead, which is the shortest path from this
breadcrumb to the document that ends the standoff.

**F-2 and F-3 are left where they were put.** F-2's two worktree items are Station 03's and both
probes are non-destructive reads; `po-vg` still holds the only copy of an uncommitted fix and must not
be force-pruned. F-3 is correct that `pr-queue-layout-sot-entry-HOLD.md` is the only gate-cleared
prompt and that it is Station 05's alone â€” this lane authored it and has not armed it, for that
reason.
