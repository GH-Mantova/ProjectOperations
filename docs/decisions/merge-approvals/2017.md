---
pr: 2017
approved_by: marco
approved_at: 2026-09-21T05:12:30Z
---

Released by Marco and merged by the Station 00 supervised interactive lane (DOCTRINE section 10.2.1;
actor `station-00.interactive-0004`; session
https://claude.ai/code/session_01M2BoeL5NEFRzZyQNeaFTn7).

**Authority.** Marco removed `do-not-merge` from this PR at 2026-09-21T05:12:30Z (timeline
`unlabeled` event, actor `GH-Mantova`), and said so in this chat: *"labels removed from 2005 &
2017"*. The removal is the release (ruled 2026-09-17, recorded in receipt 2014.md).

**Lane.** Watcher-opened. Verdict in `processed/pr-crmvis-s6-bulk-link-ready.md.log`:
`{"ok":false,"marco":true,"fixLane":false,"reason":"escalates:true - held for Marco, labelled do-not-merge"}`.
The same log carries `PR opened and left unmerged: …/pull/2017`, so it is a genuine routing, not a
prose scrape (section 10.1). RULE 2 bound until the release above.

**Review.** This PR had never been reviewed. Its review job was never queued: the watcher's
`reviewedSet` skipped it, and the neighbouring `rev-2016` job failed on 2026-09-17. This lane
enqueued `rev-2017-ready.md` from `scripts/pr-watcher/review-prompt-template.md` (the same render
`pollForNewPrs` performs) and merges only on a MERGE verdict.

**What it is.** Presentation-only restyle of `AccountLinkPreview.tsx` to the `BulkLink.dc.html`
artboard, plus `crm.css` dialog classes and a 10-assertion source-read test. The body states that
all logic is unchanged and that the `ambiguousCount > 0` guard is preserved. It carries the
`CRM_PARITY_BULKLINK_V1` marker, which releases `pr-crmvis-s7-comms-inbox`.

Merge path: Assert-SmokedOrEscalate -PR 2017 -MustContain 'CRM_PARITY_BULKLINK_V1' then Merge-Pr,
on a head whose full check set is green and whose review verdict is MERGE.
