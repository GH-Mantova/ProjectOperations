---
pr: 2038
approved_by: marco
approved_at: 2026-09-21T08:46:06Z
---

Released by Marco and merged by the Station 00 supervised interactive lane (DOCTRINE section 10.2.1;
actor `station-00.interactive-0004`; session
https://claude.ai/code/session_01M2BoeL5NEFRzZyQNeaFTn7). Receipt written by this lane at Marco's
request ("I'm on my mobile, so if a receipt is required, please enter it for me").

**Authority.** Marco removed `do-not-merge` at 2026-09-21T08:46:06Z (timeline `unlabeled`, actor
`GH-Mantova`) and confirmed in this chat: *"Labels removed"*. The removal is the release.

**Lane.** Watcher-opened from `pr-crmvis-s7-comms-inbox`. Verdict line in `processed/`:
`{"ok":false,"marco":true,...,"reason":"escalates:true — held for Marco, labelled do-not-merge"}`,
with the watcher's own `opened PR #2038` line in the same log. RULE 2 bound until the release.

**What it is.** CRM s7 (CRM_PARITY_INBOX_V1): the Comms hub frame and Inbox on the s7 kit, with New
thread on demand. `AnchorPicker.tsx`, `CommsHubPage.tsx`, `CommsInboxTriage.tsx`, `CommsPage.tsx` and a
new source-read test. Web only; no API, schema or migration.

**Driven to green by this lane.** One test-only commit, `b26dc966`: the new test asserted three retired
chip colours were absent using literal hex strings, which check-hex-ratchet counts (0 -> 3 in a new
file). Same assertions, with the strings built as "#" + digits. The ratchet was run locally with the CI
command: OK.

**Review.** First verdict REJECT-AND-REDO (on the red head); re-queued by this lane, verdict **MERGE**.

Merge path: Assert-SmokedOrEscalate -PR 2038 -MustContain 'CRM_PARITY_INBOX_V1' then Merge-Pr, on a
head whose full check set is green. Releases `pr-crmvis-s8-comms-threads`.
