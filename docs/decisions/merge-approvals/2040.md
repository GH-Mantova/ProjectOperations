---
pr: 2040
approved_by: marco
approved_at: 2026-09-21T10:05:00Z
---

Released by Marco in chat and merged by the Station 00 supervised interactive lane (DOCTRINE section
10.2.1; actor `station-00.interactive-0004`; session
https://claude.ai/code/session_01M2BoeL5NEFRzZyQNeaFTn7). Receipt written by this lane at Marco's
standing request to enter receipts for him.

**Authority.** Marco, in this chat on 2026-09-21 ~10:05Z: *"release 2040"*. Personal release. Never
labelled `do-not-merge`.

**Lane.** Watcher-opened from `pr-company-manage-s2-retire-adminonly`. Watcher verdict in
`processed/`: `{"ok":false,"marco":true,"reason":"outside tests/ or docs/: apps/web/src/App.tsx"}`.
RULE 2 bound until the release above.

**What it is.** Company-manage S2 (SLICE 17 S2): the last `<AdminOnly>` route guard in
`apps/web/src/App.tsx` is swapped for `RequirePermissions` on `company.manage`. One file. No API,
schema or migration change.

**Review.** The first `rev-2040` run reached MERGE but wrote no verdict file, so the watcher quarantined
it. This lane re-queued it on the current head. The PR title was also corrected (scope `rbac` fails
TITLE_SCOPE_UNRESOLVED).

Merge path: Assert-SmokedOrEscalate -PR 2040 -MustContain 'company.manage' then Merge-Pr, on a green
head.
