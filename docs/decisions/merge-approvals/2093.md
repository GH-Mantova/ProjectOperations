---
pr: 2093
approved_by: marco
approved_at: 2026-09-22T21:03:48Z
---

Released by Marco and merged by the Station 00 supervised interactive lane (DOCTRINE section
10.2.1; actor `station-00.interactive-0004`; session
https://claude.ai/code/session_01M2BoeL5NEFRzZyQNeaFTn7). Receipt written by this lane at Marco's
standing request to enter receipts for him.

**Authority.** Marco removed the `do-not-merge` label himself at 2026-09-22T21:03:48Z and said
"Label removed" in chat.

**Lane.** Watcher-opened from the `escalates: true` prompt `pr-scopecards-s7-one-cutting-total`,
labelled `do-not-merge` on open (RULE 2) and bound until the release above.

**What it is.** Scopecards S7 (CUTTING_ONE_TOTAL_V1): one cutting total on the server. A new
`cutting-line-pricing.ts` owns `cuttingLineTotal()` and `priceCuttingLine()`; `EstimateCuttingLine`
gains a stored `lineTotal` (DDL, backfill, then NOT NULL, in three migrations); six readers sum the
stored total instead of recomputing qty x rate; the private `cuttingMatch` row-picker in
scope-of-works.service.ts is gone.

**Known, intended behaviour change.** Wall saw cuts that the old private picker could not match
priced at 0; they now resolve through the shared resolver (the deepest at-or-above rule Marco ruled
on 2026-09-22) and price properly - the reviewer's worked example moves from 0 to $264.28. Rounding
moves to Decimal half-up, which matches the database ROUND(), so a few half-cent cases move a cent.

**Review.** rev-2093 returned REJECT-AND-REDO on one blocker: estimates.service.spec.ts mocked
cutting lines without the new `lineTotal`, so the summary read 0 where it expected 800. This lane
fixed it forward in 814c310f (the fixture now carries its own total) and retitled the PR to
feat(tendering) for the title gate. Re-review at the fixed head: VERDICT MERGE, with API,
tendering-e2e and the pipeline tests all green.

Merge path: Assert-SmokedOrEscalate then Merge-Pr, on a green head.