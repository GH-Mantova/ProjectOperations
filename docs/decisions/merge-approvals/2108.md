---
pr: 2108
approved_by: marco
approved_at: 2026-09-23T04:58:38Z
---

Released by Marco and merged by the Station 00 supervised interactive lane (DOCTRINE section
10.2.1; actor `station-00.interactive-0004`; session
https://claude.ai/code/session_01M2BoeL5NEFRzZyQNeaFTn7). Receipt written by this lane at Marco's
standing request to enter receipts for him.

**Authority.** Marco removed the `do-not-merge` label himself at 2026-09-23T04:58:38Z and said
"2107, 2108, 2109 labels removed" in chat.

**Lane.** Watcher-opened from the `escalates: true` prompt
`pr-scopecards-s7b-cutting-in-the-card-fold`, labelled `do-not-merge` on open at 01:31:04Z (RULE 2)
and bound until the release above.

**What it is.** Scope-cards S7b (CUTTING_IN_THE_FOLD_V1): cutting joins the card fold on the same
terms as every other section. Before this, the card total counted cutting at cost and counted every
destination, while the server's tender summary applied each cutting line's markup and left
Internal-only lines out of the price - so the card and the tender disagreed about what a card was
worth whenever a cutting line carried markup or was not In the price. Four files, all inside the
declared scope: `ScopeCuttingSheet.tsx`, `scope-cards/ScopeCardsTab.tsx`, and two tests, including
the new 194-line `cutting-card-fold.test.tsx`.

**Title gate.** The watcher opened this PR as `feat(scopecards-s7b): ...`, which
`check-pr-title.mjs` rejects with `TITLE_SCOPE_UNRESOLVED` - "scopecards-s7b" names nothing the
repo can point at. This lane reproduced the rejection locally, then retitled to the prompt's own
`module: tendering` value, which the same checker passes. That is the third PR in this cluster to
hit the trap.

**A CI mechanism worth recording.** `ci.yml` named its `pull_request` types explicitly and did not
include `edited`, so the rename could not re-run the job that had failed on the old title. The
scheduled Station 00 hit the same wall in the same hour and fixed it in PR 2110, which adds
`edited` to the trigger. This PR's stale title failure was cleared here by an update-branch
(`synchronize`), which re-ran CI against the corrected title.

**One infrastructure flake, re-run not excused.** `tendering-e2e` failed once with
`page.goto: WebKit encountered an internal error`, 19 of 20 tests passing. Re-run: green.

**Review.** rev-2108 returned VERDICT MERGE - nothing out of scope - recommending merge once Marco
removed the label after reviewing the before/after figures in the PR body.

Merge path: Assert-SmokedOrEscalate then Merge-Pr, on a green head.
