---
pr: 2164
approved_by: marco
approved_at: 2026-09-24T23:16:31Z
---

Released by Marco and merged by the Station 00 supervised interactive lane (DOCTRINE section
10.2.1; actor `station-00.interactive-0004`; session
https://claude.ai/code/session_01M2BoeL5NEFRzZyQNeaFTn7). Receipt written by this lane at Marco's
standing request to enter receipts for him.

**Authority.** Marco removed the `do-not-merge` label himself at 2026-09-24T23:16:31Z and said
"2164 released" in chat. The watcher had applied it at 12:48:33Z on open, from the prompt's
`escalates: true`.

**Lane.** Watcher-opened from `pr-scopecards-s8h-traffic-index-ui`, Station 06's slice. This lane
drove it to green and merged it, per the board split.

**What it is.** Scope-cards S8h, the web half of the Geoapify slice that #2161 landed on the server.
The estimator can now see the modelled traffic allowance behind a waste line's travel time, override
it on the line, and tell a derived figure from one they typed. Four files: `ScopeWasteTab.tsx`
(travel strip, editable allowance, provenance chips, totals, infeasible-cycle and fallback states,
and the `WASTE_TRAVEL_INDEX_UI_V1` export), `TipFinderDrawer.tsx` (distance note under the finder
list), plus a new `waste-travel-index.test.tsx` and a regression test added to
`waste-section.test.tsx`.

**The find-tip defect it closes.** `ScopeWasteTab` accepted a tip from the finder, took the map
location id it had just resolved under the name `_mapLocationId`, and discarded it - writing the
facility name only. The row's own tip dropdown already patched `mapLocationId`, so the same row
behaved differently depending on which control set it. The discard is gone and the accept path now
patches the id.

**Arithmetic stays on the server.** No API change, no migration, no recomputation in the web. Every
figure on the strip is one the server already returns.

**Three defects fixed forward before this merged, none of them cosmetic.**

1. **A hard-coded colour.** The 12:55Z review returned BLOCK on a new `#0369a1` literal in
   `ScopeWasteTab.tsx`, which the hex ratchet rejects - a file absent from the baseline must be
   clean. It was cleared on the branch before this lane re-reviewed, and the ratchet now reads
   `OK - no file grew, no new file carries one`, 3873 literals in 247 files against a baseline of
   4353 in 263. A related supervisor commit (`beb3894f`) mapped 40 literals of the form
   `var(--token, #hex)` onto tokens that exist, rather than leaving fallbacks doing the work.

2. **An invalid gate marker.** The PR body carried `GATE-ALLOW: ui-only`. CP-11 recognises only
   `migrations`, `env-vars` and `dependencies`, and the prompt declared `gate_allow: none`, so the
   line should never have been written. Removed by this lane; CP-11 reads
   `PASS - no migration changes`.

3. **A false red on `tendering-e2e`.** It failed twice with the WebKit `IS kanban stage columns`
   signature - `locator.waitFor: Test timeout of 60000ms exceeded`, 19 passed, 1 failed - and passed
   on re-run at 20:42Z. S8h touches the waste tab and the tip drawer; nothing near the IS kanban.
   That test has now flaked on four branches inside twelve hours and is on the pipeline-defect
   cluster list. Called a flake on the re-run evidence, not on convenience.

**Review.** The 12:55Z BLOCK was stale on its first point once the colour was cleared, so this lane
re-queued rather than merge on a stale verdict. rev-2164 returned VERDICT MERGE at 20:57:54Z:
scope exactly the four files the prompt named, no API, schema, migration, env var, `tokens.css` or
`sot/` touched, web build and lint green, and 3548 of 3548 tests passing.

**Head check before merge.** Every commit added to the branch between that verdict and the merged
head is a merge of `main`. The branch's own diff against `main` is unchanged - the same four files,
+1196 / -261 - so the verdict applies to the head that merged.

Merge path: Assert-SmokedOrEscalate then Merge-Pr, on a green head.
