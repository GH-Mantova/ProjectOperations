---
pr: 2167
approved_by: marco
approved_at: 2026-09-25T04:01:17Z
---

Released by Marco and merged by the Station 00 supervised interactive lane (DOCTRINE section
10.2.1; actor `station-00.interactive-0004`; session
https://claude.ai/code/session_01M2BoeL5NEFRzZyQNeaFTn7). Receipt written by this lane at Marco's
standing request to enter receipts for him.

**Authority.** Marco removed the `do-not-merge` label himself at 2026-09-25T04:01:17Z, in a sweep
that cleared the whole board, and said "all labels removed / keep driving the board" in chat.

**Lane.** Watcher-opened from `pr-verdictguard-spaced-path-candidates`, armed 2026-09-24T13:35:34Z
by the scheduled Station 00.

**What it is.** SPACED_PATH_CANDIDATES_V1. The verdict guard's `PATH_TOKEN_RE` excludes whitespace
from both segments, so an un-backticked citation of a file under a top-level directory whose name
contains a space - this repo has `Claude Design/` and `Claude outputs/` - was truncated to its tail.
`Claude Design/proposed/x.html` matched only `Design/proposed/x.html`, the guard called a real file
a phantom, and the operator note then blamed a stale clone and advised deleting the reviewer's
citations to satisfy the parser. Measured twice, twenty days apart: PR #1573 and PR #2157.

The rescue is derived from `prFiles` rather than hard-coded: `spacedPrefixesFromPrFiles` collects
the spaced top-level directories the PR actually touches, `extractPaths` offers the fully-prefixed
variant as an ADDITIONAL candidate, and `pathMatches` also accepts a `" " + candidate` suffix. The
guard only ever gains candidates, and a candidate is accepted only when it resolves to a real file
in the PR, so this cannot weaken it. Widening the regex was rejected in the code comments with the
reason: it would swallow the prose before any path.

**The duplicate this lane resolved.** #2166 changed the same three files with overlapping hunks,
ten minutes apart, and `git merge-base --is-ancestor` showed neither branch contained the other.
The `needs-marco/` escalation had correctly withheld closure because #2166 came from no prompt and
no arm and might have been Marco's own supervised-lane work. Marco delegated the decision in chat
on 2026-09-25. Both were then diffed line by line against `origin/main`: the `pathMatches` space
branch is byte-identical in both; #2166's candidate expansion covers the same inputs by a different
route; its block-note diagnosis exists here as an EXPORTED and tested `isLikelySpaceTruncation`
rather than a `const` inside `drain()` that no unit test can reach; and this PR carries 32 passing
tests against #2166's 29. Nothing #2166 implemented is uncovered here. #2166 was closed as
superseded, its branch left in place, and the escalation discharged into `needs-marco/discharged/`
with a note recording the comparison.

**Review.** The 14:00Z FIX-FORWARD was about the PR title scope (`fix(verdict-guard)` is not a
resolvable module); the title was corrected to `fix(pr-watcher)` before this lane re-reviewed.
rev-2167 returned VERDICT MERGE at 20:23:59Z.

**Head check before merge.** Measured across the branch: zero non-merge commits landed after that
verdict - every head move since is a merge of `main` - and the branch's own diff against `main` is
unchanged at 3 files, +257 / -11. `tendering-e2e` passed at 03:50Z on the final head.

Merge path: Assert-SmokedOrEscalate then Merge-Pr, on a green head.
