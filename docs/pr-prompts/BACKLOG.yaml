# =============================================================================================
# THE BACKLOG REGISTER Ã”Ã‡Ã¶ work that is REAL but NOT YET IN THE QUEUE.
#
# WHY THIS EXISTS
# ---------------
# On 2026-07-14 an audit found SEVEN pieces of designed work that had quietly fallen on the floor.
# They lived in CHAT MEMORIES Ã”Ã‡Ã¶ which no agent can read Ã”Ã‡Ã¶ or in a HOLD file nobody re-evaluated.
#
# The worst case: the **Branding manager** was HOLD-gated on the quote-PDF fix. That fix MERGED on
# 2026-07-08. The gate released SIX DAYS EARLIER and nothing noticed, because a HOLD is a passive
# note Ã”Ã‡Ã¶ something has to come back and ASK "is it unblocked yet?" Nothing ever did.
# Meanwhile `AdminCompanyPage.tsx` still ships a comment promising the tables.
#
# Second worst: `pr-524` names `pr-rates-b2-ratetable-canonical-cutover` as its precondition.
# THAT PROMPT WAS NEVER WRITTEN. An entire workstream was blocked on a step that did not exist.
#
# HOW IT WORKS Ã”Ã‡Ã¶ the gate is EXECUTABLE, exactly like the lint's `premise`
# -----------------------------------------------------------------------
#   gate:        a shell command. EXIT 0 => the blocker is GONE and this item is READY TO STAGE.
#   gate_means:  plain English, so a human can sanity-check the command.
#   needs_marco: true  => a decision only Marco can make. NEVER auto-stage. Surface it, and stop.
#
# `scripts/pipeline/check-backlog.mjs` runs every gate against `main`. **STATION 04-SCANNER runs it
# every 4 hours** and reports anything that just became ready. That is the whole point: a blocker
# that clears must be NOTICED, not waited on.
#
#   > If you cannot express the gate as a command, you do not understand the blocker well enough.
#   > (Same rule as the lint's premise. It is not a formality.)
#
# RULES
# -----
# * Add an item here THE MOMENT it is discussed and not immediately staged. Chat memory is NOT a
#   backlog Ã”Ã‡Ã¶ agents cannot read it, and it is where these seven items went to die.
# * `needs_marco: true` items are NEVER auto-staged. The scanner surfaces them; Marco decides.
# * When an item IS staged, delete it here. The queue is then its home. One place, never two.
# * A GATE MUST BE ABLE TO FAIL. The literal word `true` is not a gate; nor is a command that
#   ends in a `[space][pipe][pipe][space]true` swallow-the-error suffix. Both always exit 0 and
#   can never notice that the work is done Ã”Ã‡Ã¶ which is the entire job of this file. If you cannot
#   express the blocker as a command that EXITS NON-ZERO once the work has shipped, you do not
#   understand the item well enough to register it. On 2026-07-14 the P0 tender-PATCH item sat
#   at the top of this register as READY for a full day after PR #577 fixed it, because its
#   gate was set to always-pass.
# =============================================================================================

items:

  # -------------------------------------------------------------------------------------------
  # DISCHARGED 2026-07-14 (this hygiene PR): two items lived here with tautological gates and
  # both had ALREADY SHIPPED on main. Removed rather than left with a working gate, per the
  # rule at the top of this file: "When an item IS staged, delete it here."
  #   * the P0 slot (tender PATCH data-loss) shipped as PR #577 (cb9ae39) on 2026-07-14
  #   * the P1 slot (RATES_CANONICAL_SOURCE cutover) shipped as PR #579 (c5fad84) on 2026-07-14
  # The next-priority items now start at P2.
  # -------------------------------------------------------------------------------------------

  # -------------------------------------------------------------------------------------------
  # P2 Ã”Ã‡Ã¶ gate ALREADY RELEASED and nobody noticed. This is the item that proves the point.
  # -------------------------------------------------------------------------------------------
  # -------------------------------------------------------------------------------------------
  # DISCHARGED 2026-07-15 (04-scanner): branding-manager STAGED as pr-branding-manager-ready.md.
  # Its gate had released (quote-PDF fix on main) and it was the only clean, un-staged ready item.
  # -------------------------------------------------------------------------------------------

  - id: swms-interactive-module
    title: "Interactive SWMS module Ã”Ã‡Ã¶ office wizard + control-mapping tool"
    priority: P3
    why: >
      The largest body of unshipped design work. Backbone fully extracted from the Rev 5 template
      (7 sections, 102 controls, 410 control rows, 31 SOP-SWMS). TWO PR prompts were WRITTEN in
      `SWMS-PR-prompts.md` and never copied into the queue. Zero SWMS files exist on main.
      Prototype still only in `C:\ProjectOperations-Reference\Interactive SWMS\`.
    gate: 'test -z "$(git ls-tree -r --name-only origin/main | grep -i swms)"'
    gate_means: No SWMS artifacts on main Ã”Ã‡Ã¶ the work is entirely unstarted.
    needs_marco: false
    decided_in_BACKLOG_DECISIONS_md: >
      This is big. Do you want it as ONE module, or split (a) the static SWMS wizard from
      (b) the control-mapping tool? And is it Compliance-module or its own thing?
    marco_note: >
      RESOLVED 2026-07-17 (Marco): SPLIT into two independently-shippable tracks -- (A) the static
      SWMS wizard and (B) the control-mapping tool; (A) ships without (B). Module-home (Compliance
      vs standalone) still open -- the plan defaults to a standalone SWMS module surfaced under
      Compliance and flags it for confirmation. SLICE 0 (ordered build plan, no code) STAGED as
      pr-swms-build-plan-ready.md. Stays in-register until the build ships. One place, never two.
    order: 5

  # DISCHARGED 2026-07-16 (Cowork): STAGED + ARMED as pr-smart-wizard-runtime-catalog-ready.md
  # (per BACKLOG-DECISIONS #4: reads the metadata catalog at RUNTIME via a GET /meta/catalog seam --
  # no codegen, no baked-in model list. Catalog review remains a separate parallel task). One place.
  # (its home is now the prompt file.)

  # -------------------------------------------------------------------------------------------
  # P4 Ã”Ã‡Ã¶ real, but genuinely waiting on Marco
  # -------------------------------------------------------------------------------------------
  # DISCHARGED 2026-07-16 (Cowork): STAGED + ARMED as pr-qa-workstream-c-audit-ready.md
  # (per BACKLOG-DECISIONS #5: re-review the 2026-07-02 plan against today's suite FIRST, then fill
  # only genuine gaps; deliverable is docs/qa/workstream-c-coverage-audit.md). One place, never two.

  # DISCHARGED 2026-07-16 (Cowork): STAGED + ARMED as pr-field-worker-otp-auth-ready.md
  # (escalates: OTP provider + hashed-code model; delivery is a PLUGGABLE PORT that only logs the code
  # -- NO prod email/Graph/Azure wiring, per the mail hard-stop. Marco confirmed IN SCOPE 2026-07-16).
  # One place, never two.

  # DISCHARGED 2026-07-16 (Cowork): STAGED + ARMED as pr-estimate-density-ratetable-ready.md
  # (escalates: own reviewable PR, regenerates the data-model map in-PR, keeps prices byte-identical,
  # deprecates the old model in-place rather than dropping it to stay <=10 files). One place, never two.

  # DISCHARGED 2026-07-16 (Cowork): STAGED + ARMED as pr-estimate-task-waste-calculators-ready.md
  # (escalates: own reviewable PR, service+display only, PR body carries a before/after worked example
  # per BACKLOG-DECISIONS #7). One place, never two.

  # -------------------------------------------------------------------------------------------
  # ADDED 2026-07-14, second pass. THE REGISTER ITSELF HAD GAPS.
  #
  # Within an hour of building this file to stop work falling on the floor, an audit of the same
  # conversation found FOUR MORE items that had been flagged out loud and never written down.
  # That is the whole lesson, twice: if it is not in a file an agent reads, it does not exist.
  # -------------------------------------------------------------------------------------------

  # DISCHARGED 2026-07-16 (Cowork): STAGED. Foundation armed as pr-dialogs-foundation-ready.md
  # (ConfirmDialog + useConfirm primitive). The 43 remaining call sites were split into SIX staged,
  # foundation-gated prompts: docs/pr-prompts/staged/pr-dialogs-migrate-{tendering-a,tendering-b,
  # admin,personas-dashboards,misc-a,misc-b}.md -- arm each only AFTER the foundation merges to main.
  # One place, never two.

  - id: job-project-model-merge
    title: "Job / Project model merge (B-P0a)"
    priority: P1
    why: >
      TWO models for one concept Ã”Ã‡Ã¶ `model Job` and `model Project` both still on main. This is a
      duplicated data-model split that GATES the scheduler and allocation work. Phase A (the links)
      merged in #500; slices 3-9 never shipped. A hard rule was set: no PRs on this without Marco
      present, because it reshapes the spine of the data model.
    gate: 'grep -q "^model Job " apps/api/prisma/schema.prisma && grep -q "^model Project " apps/api/prisma/schema.prisma'
    gate_means: Both models still exist on main - the merge has not happened.
    needs_marco: false
    marco_note: >
      RESOLVED 2026-07-16 (Marco): JOB is canonical; everything relevant folds into Job; Project dropped
      last. This REVERSES the shipped Phase-A direction (#500 built Project-as-survivor) and sot/02's
      "survivor Project" -- both must be reconciled to Job (flag for 05-sot-keeper). SLICE 0 (the ordered
      slice plan, no code) STAGED as pr-job-project-merge-plan-ready.md; code slices follow the plan doc,
      one <=10-file PR each, additive->backfill->re-point->drop-Project-last. B-P0b (Worker) stays after
      this, never concurrent.
    order: 13

  - id: worker-workerprofile-merge
    title: "Worker / WorkerProfile model merge (B-P0b)"
    priority: P1
    why: >
      The twin of the above: `model Worker` and `model WorkerProfile` both still on main.
      WorkerProfile was agreed to be canonical. Also gates scheduler/allocation work.
    gate: 'grep -q "^model Worker " apps/api/prisma/schema.prisma && grep -q "^model WorkerProfile " apps/api/prisma/schema.prisma'
    gate_means: Both models still exist on main - the merge has not happened.
    needs_marco: false
    decided_in_BACKLOG_DECISIONS_md: "Same gate as B-P0a. Restart it? WorkerProfile was agreed canonical."
    marco_note: >
      RESOLVED 2026-07-17 (Marco): RESTART B-P0b. WorkerProfile is canonical; everything relevant folds
      into WorkerProfile; Worker dropped last. SLICE 0 (ordered slice plan, no code) STAGED as
      pr-worker-workerprofile-merge-plan-ready.md. HARD SEQUENCING: code slices must NOT run
      concurrently with B-P0a (Job/Project) -- both regenerate the data-model map. Flag sot reconcile
      for 05-sot-keeper.
    order: 14

  # DISCHARGED 2026-07-17 (04-scanner): the CODE work is ALREADY AN OPEN PR -- #602
  # "feat(email): MAIL_AUTH_MODE managed-identity for Outlook Graph (mirror #547)" touches exactly
  # apps/api/src/modules/email/providers/outlook.provider.ts + .env.example + a new
  # apps/api/src/modules/email/__tests__/outlook.auth-mode.spec.ts. The absence-gate
  # ('! grep MAIL_AUTH_MODE apps/api/src .env.example') fired only because #602 has not merged yet --
  # a false-ready of the SAME CLASS as the siteid / geoapify entries already discharged above.
  # Marco's 2026-07-15 "BUILD THE CODE PR NOW, hold ops" decision is satisfied by #602 (escalates:
  # Marco reviews; the ops sequence deploy -> real access-request -> confirm email LANDS -> only THEN
  # delete the old app-registration secrets is Marco-supervised, never alongside; LL-36).
  # One place, never two -- its home is #602.

  - id: graphify-knowledge-graph
    title: "Graphify knowledge graph -- blocked on PATH, not on code"
    priority: P4
    why: >
      Blocked because the watcher's shell does not resolve the graphify binary
      (C:\Users\Marco\.local\bin is not on its PATH). This is an ENVIRONMENT blocker, not a code
      one, so it cannot clear itself -- the gate below only exits 0 once graphify actually resolves.
      Prompt body is in the untracked HOLD file pr-graphify-setup-HOLD.md (size 10).
    gate: 'command -v graphify > /dev/null 2>&1 && ! test -f .graphifyignore'
    gate_means: The graphify binary resolves in the agent's shell AND the graph has not been built yet.
    needs_marco: false
    order: 18

  - id: role-default-dashboards
    title: "User default dashboard (global Home + per-user override) [was: role/module mapping]"
    priority: P4
    why: >
      No role-to-default-dashboard concept exists in the schema. The HOLD file carries a
      watcher:do-not-arm marker -- it was parked on a design question, not a technical blocker.
      Prompt body is in the untracked HOLD file pr-role-default-dashboards-HOLD.md
      (size 9, gate_allow migrations).
    gate: '! grep -qE "RoleDefaultDashboard|defaultDashboardId" apps/api/prisma/schema.prisma'
    gate_means: No role -> default-dashboard concept in the schema yet.
    needs_marco: false
    decided: >
      2026-07-15 (Marco, SUPERSEDES the earlier per-role/per-module answer): PER USER, not per
      role and not per module. ONE global default dashboard called "Home" that every user starts
      on, plus a per-user override (User.defaultDashboardId) each user sets for themselves. NO
      RoleDefaultDashboard table. Also DELETE the two generic dashboards "Operations" and
      "Tendering" and replace them with "Home". Staged prompts:
      docs/pr-prompts/staged/pr-user-default-dashboard.md (backend, escalates - deletes dashboard
      rows) and pr-user-default-dashboard-ui.md (frontend, GATED on the backend). Arm the backend
      first; arm the UI only after it merges.
    order: 19

  # DISCHARGED 2026-07-16 (04-scanner): its prompt body ALREADY EXISTS at
  # docs/pr-prompts/staged/pr-siteid-notnull-job-project.md (committed to main). False-ready: the
  # absence-gate (no migration yet) fired every scanner run because the staged prompt has not merged.
  # escalates:true (production-data backfill); Marco arms/reviews it. Removed per "one place, never
  # two." Related staged PRs: pr-tender-required-site.md (GATED on the Geoapify wizard).
  # UPDATE 2026-07-17: staged/pr-siteid-notnull-job-project.md REMOVED - it is now open PR #642
  # (green + mergeable, left unmerged for Marco). See BACKLOG-DECISIONS.md section B (RESOLVED).

  # -------------------------------------------------------------------------------------------
  # ADDED 2026-07-15 by STATION 03. Found by 02-board-driver on live CI, not by design review.
  # -------------------------------------------------------------------------------------------

  - id: flaky-batch5-sites-post-delete-race
    title: "Flaky e2e: batch5-sites post-delete empty-state race reddens unrelated PRs"
    priority: P2
    why: >
      In tests/e2e/pr-acceptance/batch5-sites.spec.ts (test "fresh site lifecycle - empty states,
      edit in place, delete") the post-delete assertion
      `expect(page.getByText("No sites match the current filters.")).toBeVisible()` times out after
      10s with "element(s) not found". It reddened PR #583 (1 failed / 141 passed) whose diff was
      THREE DOCS-ONLY FILES that cannot reach the Sites page, and it passed on a bare re-run.
      MECHANISM (a race, not a flake to be re-run away): `toHaveURL(/\/sites$/)` resolves the instant
      the URL changes - BEFORE the Sites register finishes its post-delete fetch. The `fill()` on the
      search box is then clobbered by the list re-render, the filter never applies, and the
      empty-state text never appears. FIX DIRECTION: after the post-delete navigation, wait for the
      sites list itself to settle (rows / loading state), not merely the URL, before filling search.
      COST IF LEFT: it will keep randomly reddening unrelated PRs and burning e2e cycles, and it
      trains agents to re-run for green - which DOCTRINE section 2 explicitly forbids.
    gate: "grep -A1 -F 'await expect(page).toHaveURL(/\/sites$/);' tests/e2e/pr-acceptance/batch5-sites.spec.ts | grep -q 'getByPlaceholder'"
    gate_means: The search-box fill() still runs on the line IMMEDIATELY after the toHaveURL assertion - i.e. nothing waits for the sites list to settle, so the race is still there. Once a settle-wait is inserted between them the grep finds no getByPlaceholder on the next line and the gate exits 1.
    needs_marco: false
    order: 15

  # -------------------------------------------------------------------------------------------
  # ADDED 2026-07-15. Spun off from the siteid decision -- FormSubmission is NOT a DB constraint.
  # -------------------------------------------------------------------------------------------

  # DISCHARGED 2026-07-16 (Cowork): STAGED + ARMED as pr-forms-existing-sites-field-ready.md. The
  # registered gate was BROKEN -- it grepped apps/web/src broadly and false-matched dashboard widget
  # code (WidgetSettingsPopover.tsx), which would have binned the prompt as stale. The armed prompt's
  # premise is rescoped to the forms module only. One place, never two.

  - id: wire-bring-up-to-speed-into-boot
    title: "Wire scripts/pipeline/bring-up-to-speed.ps1 into the SoT boot sequence"
    priority: P3
    why: >
      status-sweep.ps1 + bring-up-to-speed.ps1 give every chat one deterministic,
      GitHub-authoritative sweep (with a stale-claim cross-check) to run BEFORE any status report.
      They shipped as tracked scripts. Make it LAW, not convention: sot/README.md's boot sequence
      should tell every chat to run bring-up-to-speed.ps1 first and report only from its [LIVE]
      lines. Sot-only PR (CP-24 forbids mixing code and sot/), so this is its own PR.
    gate: 'git ls-files --error-unmatch scripts/pipeline/bring-up-to-speed.ps1 > /dev/null 2>&1 && ! grep -q "bring-up-to-speed" sot/README.md'
    gate_means: The script is on main (so the boot sequence can reference it) AND sot/README.md does not mention it yet.
    needs_marco: false
    order: 22

  # DISCHARGED 2026-07-16 (04-scanner): its prompt body ALREADY EXISTS at
  # docs/pr-prompts/staged/pr-tender-geoapify-site-autocomplete.md (committed to main). Per the rule
  # at the top of this file ("When an item IS staged, delete it here. One place, never two.") this
  # entry was a false-ready -- its absence-gate fired every scanner run because the staged prompt has
  # not merged yet. escalates:true; Marco arms it when ready (it gates pr-tender-required-site).
  # UPDATE 2026-07-17: staged/pr-tender-geoapify-site-autocomplete.md REMOVED - shipped as #641
  # (MERGED); staged/pr-tender-required-site.md REMOVED - now open PR #646 (green + mergeable, left
  # unmerged for Marco). See BACKLOG-DECISIONS.md section B (RESOLVED).

  # DISCHARGED 2026-07-16 (04-scanner): STAGED as pr-qa-flaky-batch8-archive-race-ready.md
  # (lint ADMIT, size 2). Its prompt file is now its home. One place, never two.
