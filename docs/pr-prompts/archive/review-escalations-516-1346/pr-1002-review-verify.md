# PR #1002 — Prisma Compute deploy config — NEEDS VERIFICATION

**Auto-generated by:** Prisma[bot]
**Status:** Blocked on CI (Prisma Compute Deploy check failure)
**Review:** docs/pr-reviews/pr-1002-review.md

## Escalation reason

This PR adds deployment configuration for Prisma Compute but:
1. Falls outside the normal pr-prompts workflow (no originating prompt)
2. Is blocked by a CI check (Prisma Compute Deploy) with no accessible error log
3. Claims "every push deploys with the config" but has no validated build/region/port checks

## Action for Marco

1. Check console.prisma.io for the Prisma Compute Deploy check failure reason
2. Verify region ap-southeast-1 matches the Prisma org's actual region
3. Confirm the build command and output directory are correct (align with CI)
4. Decide: should tool-generated config PRs skip pr-prompts workflow, or does this need a retro-fit prompt?

If config is valid, clear the CI check (may require re-run or manual approval on Prisma side) and merge. If config is invalid, reject and request a fix from Prisma's bot or manual adjustment.
