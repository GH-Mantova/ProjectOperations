PR #1015 (RC-1 subcontractor rate cards API) has an unresolved merge conflict in `apps/api/src/app.module.ts`. The PR was created off PR #1013 (docs queue), but PR #1012 (SoR S1 schema) merged to main first and also modified app.module.ts to register ScheduleOfRatesModule. The PR branch only has SubcontractorRatesModule, missing the ScheduleOfRatesModule import and registration. Resolution: rebase branch onto current main, resolve conflict by including both modules, re-run pnpm build && pnpm lint, then merge. Scope is otherwise clean and tests pass.
