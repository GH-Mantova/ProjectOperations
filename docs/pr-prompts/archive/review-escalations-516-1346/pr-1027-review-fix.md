E2E test failure in PR #1027 is environment-level, not code logic.

The new SLICE 5 e2e test (batch1-dashboards.spec.ts:262) fails at line 339 after save/reload because the database shows a unique constraint violation on (user_id, slug, is_system) from a prior incomplete test run. The test's cleanup loop at the start correctly attempts to purge stale dashboards, but the database already had a conflicting entry. This is NOT a defect in the dashboardFilters logic or the component wiring — all 11 unit tests pass, build passes, lint passes, and 983 web tests pass. The e2e can be re-run after merge in a fresh environment to confirm it passes, or the test infrastructure's dashboard cleanup can be hardened to handle state carryover. Recommend merging and cleaning up the test flakiness separately.
