PR #1031 needs test maintenance before merge. The PR adds "Schedule of Rates" as a new nav entry in the Estimating group, but the ShellLayout.nav.test.ts snapshots weren't updated. Two test assertions now fail: the Estimating group structure check expects 4 items but receives 5, and the permission gates test is missing the new entry. Fix: add "Schedule of Rates" entry to the group structure assertion (line 97) and add { label: "Schedule of Rates", permission: "rates.manage" } to EXPECTED_GATES (line 179). Also, pr-gates job failed due to GitHub API 502 (transient) — will pass on retry. The substantive work (UI component, routes, nav entry) is correctly scoped and implemented.
