Prompt scope conflict: PR #1036 agent correctly followed "Do not add a migration", but house rule CP-23 gate requires either a migration alongside seed changes or a SEED-ONLY: dev marker in the PR body. The trigger config IS needed in production (not dev-only), making the constraint impossible to satisfy without violating the prompt's explicit directive. Re-fire with scope permitting idempotent migration insert.
