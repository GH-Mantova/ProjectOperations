PR #1095 fails CP-23 (seed-without-migration) gate: the seed.ts changes add scoped-admin@projectops.local test user but no migration and no SEED-ONLY marker. Add "SEED-ONLY: dev -- scoped-admin test user for SLICE 17 e2e acceptance tests" to PR body, then re-run. Web build/lint also failing (logs pending); once that clears with the body edit, scope and security mapping are verified and ready to merge.
