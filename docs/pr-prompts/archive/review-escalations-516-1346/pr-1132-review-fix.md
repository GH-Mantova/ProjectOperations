PR #1132 (MT-4: tenant assignment UI) ships value but has a test coverage gap. MasterDataService.validateTenantId (and its call sites in upsertClient/upsertWorker) lack unit test coverage in master-data.service.spec.ts, though the identical validation pattern is thoroughly tested in contacts.service.spec.ts. The logic is minimal (one Tenant lookup + isActive check + BadRequestException throw) so this is low risk but violates the prompt's guardrail. Request: after merge, create a follow-up PR to add ~5–10 unit tests to master-data.service.spec.ts mirroring the contacts pattern (create valid/null/invalid tenant, update valid/invalid). Can be marked as P4 / nice-to-have since the pattern is proven and low-complexity.
