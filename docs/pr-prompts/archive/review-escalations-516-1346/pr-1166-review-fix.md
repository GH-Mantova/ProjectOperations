PR #1166 flags FIX-FORWARD: CP-23 gate initially failed because the PR body's SEED-ONLY marker was not present when the gate first ran (00:35:59Z). The marker is now in the body, so re-running the PR gates job should flip it to ALLOWED. The substantive work (all Password123 literals removed, e2e field rotated to unique credential, helpers.ts updated) is complete and verified. The only blocker is a CI timing artifact from the body edit. Re-run the "PR gates" job and merge if it passes.
