PR #1198 has one test failure that needs a one-line fix. The test at tender-number.service.spec.ts:132 expects "Hello   World" (three spaces) but the sanitiseSharePointName function correctly collapses consecutive spaces to one. Change the expected value from "Hello   World" to "Hello World" on line 132, re-run pnpm test:api:serial to confirm green, then CI will pass and the PR can merge after E2E completes. The implementation and all other scope items are correct per the prompt (pr-tfm-s2-folder-naming-HOLD.md).
