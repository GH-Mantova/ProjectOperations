PR #1259 merged with incomplete work. The prompt `pr-tipfinder-tender-only-ready.md` required exactly two deletions in MapLocationsTab.tsx (lines 7 and 718-719), and the `done_when` check verifies they are gone. The merged commit only added documentation (BACKLOG.yaml + the prompt file itself) but did NOT perform the code deletions. Both lines still exist on main. The substantive decision (consolidate Tip Finder to tender waste card only) is documented and measured, but the code execution is missing. Recommend re-fire to complete the two-line deletion.
