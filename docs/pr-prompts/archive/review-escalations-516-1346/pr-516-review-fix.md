PR #516 ships valid scope and passes API + Web CI, but the PR gates check CP-11 (migrations) failed because the GATE-ALLOW marker in the PR body is formatted as inline code (`` `GATE-ALLOW: migrations` ``) instead of a bare line. The pr-gates.mjs script uses regex `^GATE-ALLOW:` which requires column-0 position and does not match backtick-quoted text. The migration itself is legitimate and correctly ordered (20260708150000 sorts after existing same-day migrations). Fix: edit the PR body to change the line from `` `GATE-ALLOW: migrations` `` to `GATE-ALLOW: migrations` (remove backticks), then either close/reopen the PR or let the gates re-run on next push. As of PR #350 the gates script fetches the body live by PR number, so a body edit alone should unblock CI without a new commit.
