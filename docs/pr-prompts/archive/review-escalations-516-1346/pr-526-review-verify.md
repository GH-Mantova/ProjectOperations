PR #526 review: NEEDS-MARCO-VERIFY

The reviewer could not locate the originating prompt file for PR #526 ("[Forms FV2] Basic / survey / layout field wire-ups") in docs/pr-prompts/ (queued, ready, processed, failed, or paused). CI is green across the board, scope reads clean against the PR body (palette/renderer/config wire only, no schema, no rules/repeating/lookup/AI), self-verification claims in the PR body all check out, and 35 new specs were added. The substantive work is done, but without the prompt file the reviewer cannot cross-check against the prompt's explicit "do NOT do" list or verify the assumption the PR body flags under "Assumption noted for the human loop" (Site & WHS group preservation vs folding into Basic). Marco to eyeball that assumption and either locate the prompt or accept the substantive review and merge. Full findings in docs/pr-reviews/pr-526-review.md.
