PR #561 is blocked by a false-positive gate failure. The test file documents the OLD broken behavior (silent bounce via Navigate) that was fixed. Add `// eslint-ok: not-a-permission-redirect` escape-hatch comment to AdminCompanyPage.test.ts line 4 to satisfy CP-25, then push to re-trigger gates. Substantive fixes (admin nav parity, orphan routes, no more silent bounces, 404 UX) are all correct. API and E2E checks still running.
