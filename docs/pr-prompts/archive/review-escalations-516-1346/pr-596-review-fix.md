PR #596 (feat(rates): add MaterialKind enum) requires one commit before merge: regenerate the data-model drift artifact. Run `node scripts/data-model/build-relationship-map.mjs` and commit the updated relationship-map.json. All other work (schema, migration backfill, seed logic, DTOs) is correct and CI-passing. This is a required generated artifact, not optional. Additionally, the originating prompt was not found in docs/pr-prompts/ — if this was auto-fired, log its location for future reviews.
