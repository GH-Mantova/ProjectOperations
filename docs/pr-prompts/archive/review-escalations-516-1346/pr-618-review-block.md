PR #618 fails CP-23 seed-without-migration gate. The two new NotificationTriggerConfig rows (compliance.expiry_reminder, competency.expiry_digest) are added only to the seed file, not a migration. Since production runs prisma migrate deploy (not TypeScript seed), these configs will never reach prod — the digest will silently not run. Either add an idempotent migration to create the rows, or add SEED-ONLY: dev -- <reason> to the PR body if they truly are dev-only. The originating prompt said "No migration expected" but did not anticipate the seeding philosophy gate.
