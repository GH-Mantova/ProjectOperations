# PR #622 — Reject and re-fire

PR builds and passes lint but fails TypeScript compilation with 15 errors in contracts.service.ts. The decorator helper `decoratePaymentScheduleStatus<T>()` has a generic type constraint that is too narrow and causes the return type to lose all claim properties except paymentSchedule. Methods like submitClaim/approveClaim/payClaim then fail to access claim.status and claim.contract because TypeScript believes the type only has paymentSchedule. Root cause: the generic constraint should not narrow the type. Fix: rethink the type signature (remove constraint, use cast, or re-spread fields). Substantive work is complete; TypeScript annotation needs repair. Also note: PR has merge conflict with origin/main (DIRTY state); rebase needed after type fix.
