PR #624 ships all four WHS template packs correctly seeded with correct scope and no schema changes. CI gate CP-23 fails because the PR modifies a seed file without a migration, which is correct per the prompt's "SEED ONLY — no schema/migration" directive. The fix is to add a single line to the PR body: "SEED-ONLY: dev -- seeded WHS form templates are dev/test data providing users with realistic starting points; prod uses prod-seeded master templates." This acknowledges the seed-only nature per the gate's documented exemption path. Once added, CI will pass and the PR can merge per shepherd policy.
