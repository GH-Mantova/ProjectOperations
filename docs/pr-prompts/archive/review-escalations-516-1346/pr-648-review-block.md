PR #648 stages a prompt file (docs-only), but its branch includes 3 unmerged commits from PRs #602, #621, #647. Merging it would pull those PRs into main as a side effect, violating the "single commit per prompt-staging PR" rule and the "no direct commits to main" policy. The branch needs to be rebased on clean main, or closed and re-opened with only the prompt file diff. One CI check (tendering-e2e) is still pending; verify that completes before merge.
