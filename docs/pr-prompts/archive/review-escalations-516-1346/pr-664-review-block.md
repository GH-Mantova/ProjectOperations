PR #664 blocked on missing GATE-ALLOW declaration. The agent shipped a Prisma migration (20260717130000_feat_business_process_flow) but did not add the required GATE-ALLOW: migrations line to the PR body. The PR gates CP-11 check fails with "undeclared" error. The prompt specified gate_allow: migrations, which is a house rule: migrations must be declared in the PR body to pass the diff-check gate. The implementation itself is correct—engine is Tender-only with proper whitelist, schema is well-formed, seed uses insert-if-absent, and API/Web integration is scoped properly. Recommend amending the PR body to add GATE-ALLOW: migrations and re-running CI. Also wait for tendering-e2e smoke to complete.
