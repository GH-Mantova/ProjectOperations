PR #691 ships the substantive fix (register workers.manage code + workers module), but the permissions test (PR #689) has an anti-rot check that now fires. The test's KNOWN_UNREGISTERED allowlist still contains "workers.manage" because PR #691 did not include the cleanup step. This is a one-line fix: remove "workers.manage" from the Set in apps/api/src/common/__tests__/permission-registry-coverage.guard.spec.ts (line ~51). The guard is working as designed. Recommend: merge PR #691 as-is (value shipped) + fire a trivial follow-up PR to remove the stale allowlist entry and unblock the test.
