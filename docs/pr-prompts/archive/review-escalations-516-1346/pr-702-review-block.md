PR #702 claims to be "sot-only — no code touched" (only 6 lines added to sot/05-decisions-and-lessons.md for LL-40/LL-41 incidents), but the branch contains 7 upstream commits totaling 744 insertions/820 deletions across 20 files (permission registry test, email diagnostics, web components, pipeline fixes, prompt deletions). This is either a mislabeled multi-PR merge (which needs explicit gate-scope documentation per house rule) or an accidental stale base (which needs rebase to main). The LL-40/LL-41 incidents themselves are correct, but cannot be merged under false scope claims.
