Migration P3006 error: `updated_at` column omitted from INSERT into rate_columns. Schema requires NOT NULL updated_at with no default; migration does not provide it. The migration cannot apply to shadow database or production. Correct the migration by adding `updated_at NOW()` to all INSERT rows in the rate_columns and rate_rows inserts, or use `prisma migrate create` to regenerate with proper column handling, then re-fire the prompt.
