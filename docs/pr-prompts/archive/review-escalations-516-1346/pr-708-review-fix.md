PR #708 is FIX-FORWARD: OTP auth implementation is complete, in-scope, CI-green, and secure. However, the merge commit introduced unrelated web files (ConfirmDialog + useConfirm from PR #692) because main progressed while the OTP branch was in CI. The OTP author's actual commit (ba3f1823) touches only in-scope API/auth files. Marco should review whether the merged-in web changes (apps/web/ ConfirmDialog, useConfirm, Timeline updates) conflict with any in-flight work, then merge if clear.
