PR #720 bundles two separate features: (1) the reporting layer slice 1 (reporting module, web page, nav), which is in scope and well-implemented, and (2) a material densities rate table projection (migration, RateResolverService, estimates coupling), which was not in the originating prompt and violates "no migrations / no new tables". The commit message claims "no migrations" but includes one. Recommend splitting into two PRs so each feature can be reviewed on its merits and the scope boundary clarified. The reporting layer code itself is sound.
