CP-11 gates check failed because the PR body lacks the mandatory `GATE-ALLOW: migrations` fence marker. The substantive work (schema, migration, API, web pages, permissions) is complete and correct. Marco should add the marker to the PR body (e.g., as a separate line or fence in the body) and re-run jobs to unblock the gates check. Once that marker is present, the PR is safe to merge per the originating prompt house rules.
