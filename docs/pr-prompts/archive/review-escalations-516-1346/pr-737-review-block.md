PR #737 breaks 13 E2E tests (exit code 1 in tendering-e2e job). The route removal in apps/web/src/** was correct, but E2E test files (batch1-auth-shell.spec.ts, batch1-dashboards.spec.ts, batch2-tendering.spec.ts, batch9a-forms-widgets.spec.ts) still navigate to the deleted /tenders/dashboard route. Tests must be updated to use "/" (Home) instead before the PR can merge. The prompt's scope "apps/web/src/**" did not include E2E test updates — re-fire the prompt to include tests/** coverage or mark this as a follow-up.
