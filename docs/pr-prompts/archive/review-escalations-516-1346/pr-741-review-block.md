PR #741 (feat(ops): map locations register / feat/ops-m1-locations-register) is BLOCKED for two reasons: (1) it force-adds three gitignored files (docs/data-model/relationship-map.json, relationship-map.md, metadata-catalog.json) that per .gitignore lines 111-119 and the PR-#662 incident (2026-07-17) must not be committed - these account for the entire +88,559 diff and will re-conflict on every open feature PR the moment main is merged into them; (2) the originating prompt is not present in docs/pr-prompts/ or any subdirectory, so scope compliance cannot be verified. Adjacent prompts (pr-ops-m1b-map-page-HOLD.md, pr-ops-m2-*) confirm the work is real. Also noted: pr-untrack-data-model-map-HOLD.md sits in binned-shipped-20260720/, so there was already a planned effort to remove these artifacts from tracking - this PR moves the opposite direction. Fix: have the author `git rm --cached` the three files and force-push; then supply the originating prompt so a second-pass scope check can run. Consider auditing pr-* prompt templates for stale "regenerate and commit docs/data-model/**" text.
