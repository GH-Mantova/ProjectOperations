PR #742 "fix(permissions): register clients.view / clients.manage" has correct core work — the two permission codes are properly registered in permission-registry.ts and the clients module is registered in module-registry.ts. However, the permission-registry-coverage.guard test fails because the KNOWN_UNREGISTERED allowlist in apps/api/src/common/__tests__/permission-registry-coverage.guard.spec.ts still contains "clients.view" and "clients.manage" (lines 28-31). The test's second check ("allowlist cannot rot") intentionally fails to prevent exactly this: entries remaining on an allowlist after their armed fix PR merges. Single-line fix: delete those two entries from the Set, or ask the agent to amend the commit with that one-line change.
