PR #779 needs a one-line body edit to pass the CP-13 gate. The commit has `GATE-ALLOW: dependencies` at column 0 (correct), but pr-gates reads the marker from the PR body via live `gh pr view` fetch, not from commit messages. The PR body mentions "GATE-ALLOW: dependencies" inside documentation, not as a standalone column-0 line. Add this to the PR body as a new line at column 0 (not indented): `GATE-ALLOW: dependencies`. The code is complete and correct; only gate marker placement needs fixing.
