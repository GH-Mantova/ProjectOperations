PR #789 has two stacked branches (PR #788 + PR #789 commits in one branch). House rules require single-commit PRs, each based on main. The feature substance is correct, but git structure violates the rule. Close and ask to split: create PR #788 from main with commit a0dc4772 (generate-draft-tender), then PR #789 from main with commit 858cd999 (tab-on-tenders) after #788 merges. Both prompts already exist and expect independent PRs.
