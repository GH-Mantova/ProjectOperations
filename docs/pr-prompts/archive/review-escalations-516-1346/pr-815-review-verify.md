PR #815 cannot be verified against originating prompt — no prompt file exists for this PR across all prompt directories. PR appears manually created rather than auto-fired by watcher. Marco should confirm: (1) whether this PR was intended to be prompt-driven or is a manual fix; (2) CI still in progress (API lint/test, tendering-e2e). PR is currently "behind" main and requires rebase before merge.
