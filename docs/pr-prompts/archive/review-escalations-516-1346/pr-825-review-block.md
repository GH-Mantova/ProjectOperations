PR #825 is docs-only (staging two prompts) with correct scope and no code changes, but the tendering-e2e CI check is failing, blocking merge. The test failure is not caused by the documentation changes and likely indicates a pre-existing issue with the test infrastructure or main branch. Verify whether the e2e failure is unrelated before deciding to force-merge or require a separate fix.
