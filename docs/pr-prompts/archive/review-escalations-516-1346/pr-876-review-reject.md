PR #876 "seed(field-worker): grant expenses.view + expenses.manage" needs Marco's attention: CP-23 seed-without-migration gate failed because the PR body lacks the required `SEED-ONLY: dev` marker. The originating prompt specified seed_only: true and the PROD STEP note is present, but the CI gate requires an explicit marker line. Edit the PR body to add `SEED-ONLY: dev -- <reason>` and the gate will pass; the substantive work (adding permissions to seed) is complete and correct.
