PR #933 (F-2b — one rule evaluator) has complete, correct implementation but is blocked by CP-13 gate (undeclared workspace dependencies). The PR added @project-ops/config as workspace:* deps to apps/api and apps/web, which are necessary and correct, but the originating prompt had `gate_allow: none`. Re-fire pr-fv2-rules-eval-unify-ready.md with `gate_allow: CP-13` to permit these workspace dependencies, or merge the dependency declaration as a follow-up PR.
