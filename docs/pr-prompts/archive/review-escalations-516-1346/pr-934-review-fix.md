PR #934 (feat(web): SLICE 7 — consolidate Users onto AdminUsersTab, delete UsersPage) ships correct, complete work within scope. Code review passes; diff is clean; scope compliance verified against settings-restructure-plan.md SLICE 7. FIX-FORWARD because API + tendering-e2e CI jobs are still in progress (mergeable_state: blocked); once those complete green, PR is ready to merge. No code fixes needed. Work quality is good; architectural improvement over plan (simpler inlining vs wrapper file).
