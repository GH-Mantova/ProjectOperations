PR #981 blocked: F-4 wave 1 implementation ships correct business logic (Unique ID sequencing, live Calculation, nested Lookup) but fails CI due to incomplete test updates. Web tests fail because formDesignerState.test.ts expectations still assume 4 Advanced palette types (lookup, calculation, table, terms) before unique_id was added—now 5. API tests fail because 4 forms-engine test specs don't inject the new FormNumberSequenceService into their test module setup, so FormsEngineService instantiation throws "An argument for 'formNumberSequence' was not provided." The code changes are sound; tests need the service mocked and the palette expectation widened from 4 to 5 types.
