PR #988 (feat(api): handover template CRUD + explicit publish/versioning B-HW-2) is BLOCKED on CodeQL failure. The "CodeQL" CI check (run #92547544364) completed with status: failure, conclusion: failure. Scope, implementation, and all functional tests (27/27 unit tests, lint, API smoke) are clean — no code security anti-patterns detected in diff. Root cause of CodeQL alert is not visible in the PR interface. Must investigate the failing job log before merging. If pre-existing, escalate as infra issue; if code-specific, PR must be re-fired with fix.
