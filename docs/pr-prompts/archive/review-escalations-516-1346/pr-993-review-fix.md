PR #993 is scope-clean and all self-verification steps passed locally (9/9 tests green, no production imports, fixture mirrors slice-1 exactly). The sole blocker is tendering-e2e in_progress. This job runs browser E2E tests and should not be affected by test-only changes in apps/api/testing/**. Recommendation: wait for tendering-e2e to complete; if it passes (expected), merge immediately. If it fails, inspect the log — the failure is unlikely to be caused by this PR.
