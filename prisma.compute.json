{
  "region": "ap-southeast-1",
  "app": {
    "name": "api",
    "root": "apps/api",
    "framework": "nestjs",
    "httpPort": 3000,
    "build": {
      "command": "pnpm run build",
      "outputDirectory": "dist"
    }
  }
}
